apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weka-app-store-operator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "weka-app-store-operator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weka-app-store-operator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
