apiVersion: v1
kind: Service
metadata:
  name: {{ include "weka-app-store-operator-chart.fullname" . }}
  labels:
    {{- include "weka-app-store-operator-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weka-app-store-operator-chart.selectorLabels" . | nindent 4 }}
