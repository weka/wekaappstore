# Milestone v7.0: Secret Management, WEKA Storage Integration & Dynamic Blueprint System

**Status:** ✅ SHIPPED 2026-06-17
**Phases:** 21-26
**Total Plans:** 18
**Commits:** 176
**Files changed:** 95 | +21,355 / -576 lines
**Timeline:** 2026-06-11 → 2026-06-17 (6 days)

## Overview

This milestone shipped two major feature sets as a continuous sprint:

**v6.0 — Secret Management & WEKA Storage Integration (Phases 21-25):**
A first-class credential management system for App Store administrators. Named, multi-key storage for NGC/HuggingFace/WEKA credentials via a new `WarpCredential` CRD, automatic secret derivation by the operator, a blueprint Jinja2 macro SDK for credential selection, and live WEKA storage visibility on the Settings page.

**v7.0 — Dynamic Blueprint System (Phase 26):**
Blueprint YAML files become self-describing via an `x-variables` metadata block. The GUI scans `BLUEPRINTS_DIR` at request time to discover blueprints automatically, renders a single generic install form from the schema, and the deploy route accepts a generic variable dict validated against the schema. No `main.py` or template change is required to add a new blueprint with new variables.

## Phases

### Phase 21: WarpCredential CRD and Helm RBAC

**Goal:** `WarpCredential` CRD defined in Helm chart; operator service account has Secret CRUD permissions scoped to the App Store namespace
**Depends on:** Nothing (independently deployable)
**Plans:** 2/2 complete (completed 2026-06-11)

Plans:
- [x] 21-01: WarpCredential CRD schema (spec + status subresource)
- [x] 21-02: Namespace-scoped Secret CRUD Role + RoleBinding + Chart.yaml 0.1.64 bump

**Details:**
- `warpcredentials.warp.io` CRD with typed `openAPIV3Schema`: `spec.type` enum (nvidia-ngc | huggingface | weka-storage), `spec.displayName`, `spec.secretRef`, optional `spec.endpoint`
- `status` subresource: `conditions` array (KeyReady, DockerSecretReady), `derivedSecrets` list, `lastSyncTime`, `wekaEndpoint`
- Namespace-scoped `Role` + `RoleBinding` granting operator Secret CRUD in App Store namespace only

---

### Phase 22: Operator WarpCredential Reconciler

**Goal:** Operator reconciles `WarpCredential` CRs, deriving correct secrets per type and maintaining status conditions; idempotent
**Depends on:** Phase 21 (CRD must exist before handler can be registered)
**Plans:** 3/3 complete (completed 2026-06-11)

Plans:
- [x] 22-01: Pure derivation helpers, kr8s I/O wrappers, status-condition builder, delete handler
- [x] 22-02: reconcile_warpcredential stacked-decorator handler (create/update/resume) with type dispatch and status writes
- [x] 22-03: operator_module/tests/test_warp_credential.py — pure-helper + idempotency + handler + delete + caplog no-leak tests

**Details:**
- NGC: derives `warp-<name>-apikey` (Opaque, `NGC_API_KEY`) + `warp-<name>-docker` (dockerconfigjson for nvcr.io)
- HuggingFace: derives `warp-<name>-token` (Opaque, `HF_API_KEY`)
- WEKA Storage: derives `warp-<name>-token` (Opaque, `WEKA_API_USERNAME` / `WEKA_API_TOKEN` / `WEKA_API_ENDPOINT`); sets `status.wekaEndpoint`
- Delete handler leaves derived secrets intact (operator logs warning; admin must clean up manually)
- No key values appear in operator logs at any log level

---

### Phase 23: Backend Credentials API and WEKA Overview Proxy

**Goal:** GUI backend exposes `/api/credentials` CRUD and `/api/weka/overview` proxy; old secret endpoints removed
**Depends on:** Phase 22
**Plans:** 4/4 complete (completed 2026-06-11)

Plans:
- [x] 23-01: Remove deprecated `/api/secret/{huggingface,nvidia}` handlers; strip old settings.html sections; rewire loadBlueprints namespace fallback
- [x] 23-02: `/api/credentials` CRUD: GET list + ?type filter, POST with slug + collision + raw Secret + WarpCredential CR, DELETE preserving derived secrets
- [x] 23-03: `/api/weka/overview` proxy: credential resolve + WEKA login + parallel data fetch + `_assemble_weka_overview`; 60s namespace-scoped cache; `?bust=1` bypass; 502 on WEKA failure
- [x] 23-04: `app-store-gui/tests/test_credentials_api.py`: pure-helper + handler + WEKA assembler + cache TTL + bust + no-token-leak tests

---

### Phase 24: Settings GUI Overhaul

**Goal:** Settings page restructured with Credential Management first, per-type credential lists with traffic-light states, inline add forms, WEKA Storage Overview panel
**Depends on:** Phase 23
**Plans:** 3/3 complete (completed 2026-06-12)

Plans:
- [x] 24-01: Extend /settings route; restructure settings.html section order; Credential Management + WEKA Storage Overview shells
- [x] 24-02: Credential Management JS — inline forms, traffic-light state machine, 2s polling with 30s timeout, browser-confirm delete
- [x] 24-03: WEKA Storage Overview JS — header controls, four-state machine, humanBytes capacity row, filesystem table with sort and Show-all toggle, backend node grid

---

### Phase 25: Blueprint Credential Selector SDK

**Goal:** Blueprint install pages render credential dropdowns and WEKA endpoint fields using Jinja2 macros; `credentials_by_type` injected automatically into all blueprint template contexts
**Depends on:** Phase 23
**Plans:** 3/3 complete (completed 2026-06-12)

Plans:
- [x] 25-01: Extract `_get_credentials_by_type(ns)` helper; wire into `blueprint_detail`; refactor `settings_page` to call helper
- [x] 25-02: New `_credential_macros.html` (`credential_select` + `weka_storage_select` with `warpSyncEndpoint` JS); update `blueprint_neuralmesh-aidp.html` as reference example
- [x] 25-03: `test_credential_macros.py` — helper grouping/dropping/error-fallback + `blueprint_detail` context injection + namespace fallback tests

---

### Phase 26: Dynamic Blueprint Discovery and Self-Describing Variable Schema

**Goal:** Blueprint YAML files with `x-variables` block discovered automatically from `BLUEPRINTS_DIR`; single generic `blueprint.html` renders install form dynamically; `type: credential` variables render WarpCredential dropdowns; deploy route accepts all variables as generic dict; zero code change to add new blueprint
**Depends on:** Phase 25
**Plans:** 3/3 complete (completed 2026-06-17)

Plans:
- [x] 26-01: `parse_x_variables` + `find_blueprint` helpers; `blueprint_detail` route updated; generic `blueprint.html` with dynamic form loop
- [x] 26-02: Generic `/deploy-stream` with `variables` JSON param, required-field validation, `[[var]]` substitution; `app_map` and all 7 per-variable positional params removed; sample fixture `x-variables` blocks
- [x] 26-03: Gap closure — `ns_creds_missing` 500 fix (moved before `{% if variable_schema %}`); `[[namespace]]` tokens in fixture YAMLs; `find_blueprint` cluster-init `os.path.abspath` normalization; dead code guard removed; REQUIREMENTS.md DYN-07 partial note

**Verification:** 6/6 success criteria passed (2026-06-17)

---

## Milestone Summary

### Key Decisions

- **WarpCredential is multi-instance** (one CR per named credential, not per type) — enables multiple NGC keys, multiple WEKA clusters per namespace
- **Derived secrets are never deleted on WarpCredential delete** — operator logs warning; admin must clean up manually; avoids accidental removal of secrets in use by running workloads
- **`_get_credentials_by_type` ready-filter at helper level** — macros stay logic-free; a single source of truth for the `ready: true` filter
- **`[[var]]` Jinja2 syntax for dynamic blueprint substitution** — chosen over `${var}` to avoid conflicts with shell variable expansion in YAML and with the operator's existing `render()` function
- **`parse_x_variables` returns `{}` on all failure paths** — schema parse errors degrade gracefully to a static install page rather than 500
- **DYN-07 production blueprint migration deferred to external `warp-blueprints` repo** — fixture YAMLs migrated; production blueprints (oss-rag, openfold, nvidia) are in external repo and tracked as follow-on work

### Issues Resolved

- `ns_creds_missing` Jinja2 scoping bug caused 500 on any blueprint page without x-variables — fixed by moving `{% set %}` before the `{% if variable_schema %}` block
- Fixture YAML `[[namespace]]` tokens absent — silent variable discard; all fixtures now have substitution tokens
- `find_blueprint` cluster-init path missing `os.path.abspath()` normalization — normalized
- Dead code guard `if app_name == "cluster-init" and not namespace` was unreachable — removed

### Issues Deferred

- **v3.1 E2E Chat Validation** — inspect tool `load_incluster_config`, init container config gap, NIM model reliability, OpenClaw version upgrade (see `.planning/v3.0-KNOWN-ISSUES.md`)
- **v5.0 Phases 19-20** — Validator Soft-Warning and AIDP Migration Smoke Test remain unstarted
- **DYN-07 production blueprint migration** — external `warp-blueprints` repo; tracked as follow-on

### Technical Debt Incurred

- Settings page JS credential polling is inline (not extracted to a module) — acceptable for single-file FastAPI/Jinja pattern
- WEKA Overview JS `humanBytes` helper is duplicated — present in both settings.html and blueprint.html; candidate for extraction to a shared JS file

---

*For current project status, see .planning/ROADMAP.md*
