---
phase: 09-deployment-and-registration
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .github/workflows/mcp-server.yml
  - mcp-server/README.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-04]

must_haves:
  truths:
    - "CI runs pytest on every PR touching mcp-server/"
    - "CI builds and pushes Docker image to wekachrisjen/weka-app-store-mcp on v* tag"
    - "README documents every step from build to register with copy-paste commands"
    - "NemoClaw registration section has a TODO marker for unpublished schema"
  artifacts:
    - path: ".github/workflows/mcp-server.yml"
      provides: "CI/CD pipeline"
      contains: "mcp-server"
    - path: "mcp-server/README.md"
      provides: "Deployment and registration documentation"
      contains: "openclaw.json"
  key_links:
    - from: ".github/workflows/mcp-server.yml"
      to: "mcp-server/Dockerfile"
      via: "docker/build-push-action references Dockerfile"
      pattern: "file: mcp-server/Dockerfile"
    - from: "mcp-server/README.md"
      to: "mcp-server/openclaw.json"
      via: "Registration docs reference config file"
      pattern: "openclaw.json"
---

<objective>
GitHub Actions CI/CD workflow and comprehensive README with OpenClaw/NemoClaw registration documentation.

Purpose: Enable automated testing and image publishing on every release, and provide operators with copy-paste instructions for registering the MCP server.
Output: CI workflow file and README.md with quick start, env vars, registration, and troubleshooting.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deployment-and-registration/09-01-SUMMARY.md
@mcp-server/openclaw.json
@mcp-server/server.py

<interfaces>
<!-- From Plan 01 outputs -->

mcp-server/Dockerfile:
- Built from repo root: `docker build -f mcp-server/Dockerfile .`
- Base: python:3.10-slim
- CMD: python -m server (stdio transport)
- Non-root user: mcpuser

mcp-server/config.py env vars:
```python
BLUEPRINTS_DIR: str  # REQUIRED — validated at startup
KUBERNETES_AUTH_MODE: str  # optional, default "in-cluster"
LOG_LEVEL: str  # optional, default "INFO"
KUBECONFIG: str | None  # optional, None default
WEKA_ENDPOINT: str | None  # optional, None default (future use)
```

mcp-server/openclaw.json (already exists):
```json
{
  "name": "weka-app-store-mcp",
  "transport": "stdio",
  "startup": { "command": "python", "args": ["-m", "server"], "cwd": "mcp-server/" },
  "env": { "required": ["BLUEPRINTS_DIR"], "optional": ["KUBERNETES_AUTH_MODE", "LOG_LEVEL", "KUBECONFIG"] },
  "container": "weka-app-store-mcp:latest",
  "skill": "mcp-server/SKILL.md"
}
```

Test run command (from all previous phases):
```bash
PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest mcp-server/tests/ -v
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI/CD workflow</name>
  <files>.github/workflows/mcp-server.yml</files>
  <action>
    Create `.github/workflows/` directory if it does not exist.

    Create `.github/workflows/mcp-server.yml` with two jobs:

    **Job: test**
    - Trigger: pull_request with paths `mcp-server/**`, push with paths `mcp-server/**` and tags `v*`
    - Runner: ubuntu-latest
    - Steps: checkout (actions/checkout@v4), setup-python 3.10 (actions/setup-python@v5), pip install -r mcp-server/requirements.txt, run pytest
    - Pytest command: `pytest mcp-server/tests/ -v`
    - Env for pytest: `PYTHONPATH: mcp-server:app-store-gui` and `BLUEPRINTS_DIR: mcp-server/tests/fixtures/sample_blueprints`

    **Job: build-push**
    - Trigger: only on push with tags matching `v*` (use `if: startsWith(github.ref, 'refs/tags/v')`)
    - Depends on: test job (`needs: test`)
    - Steps:
      1. actions/checkout@v4
      2. docker/login-action@v3 with `secrets.DOCKERHUB_USERNAME` and `secrets.DOCKERHUB_TOKEN`
      3. docker/metadata-action@v5 with image `wekachrisjen/weka-app-store-mcp`, tags: `type=semver,pattern={{version}}` and `type=raw,value=latest`
      4. docker/build-push-action@v6 with `context: .`, `file: mcp-server/Dockerfile`, `push: true`, tags from metadata step

    Per user decision: "CI triggers: run tests on every PR touching mcp-server/; build and push image only on version tag push (v*)"
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore && python -c "import yaml; y=yaml.safe_load(open('.github/workflows/mcp-server.yml')); assert 'test' in y['jobs']; assert 'build-push' in y['jobs']; print('Valid workflow with test and build-push jobs')"</automated>
  </verify>
  <done>CI workflow file exists with test job (PR trigger, pytest) and build-push job (v* tag trigger, Docker Hub push to wekachrisjen/weka-app-store-mcp).</done>
</task>

<task type="auto">
  <name>Task 2: README with registration documentation</name>
  <files>mcp-server/README.md</files>
  <action>
    Create mcp-server/README.md with the following sections per user decision ("single README.md in mcp-server/ with sections: quick start, Dockerfile build, env vars reference, OpenClaw registration, NemoClaw registration, troubleshooting"):

    **1. Header** — WEKA App Store MCP Server, one-line description

    **2. Quick Start** — Minimal steps to build and run locally:
    ```bash
    docker build -f mcp-server/Dockerfile . -t weka-app-store-mcp
    echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' | \
      docker run --rm -i -e BLUEPRINTS_DIR=/app/manifests/manifest weka-app-store-mcp
    ```

    **3. Building the Image** — Full copy-paste commands:
    - Build: `docker build -f mcp-server/Dockerfile . -t wekachrisjen/weka-app-store-mcp:v2.0.0`
    - Tag latest: `docker tag wekachrisjen/weka-app-store-mcp:v2.0.0 wekachrisjen/weka-app-store-mcp:latest`
    - Push: `docker push wekachrisjen/weka-app-store-mcp:v2.0.0 && docker push wekachrisjen/weka-app-store-mcp:latest`

    **4. Environment Variables Reference** — Table with all 5 env vars:
    | Variable | Required | Default | Description |
    BLUEPRINTS_DIR (required, no default, validated at startup), KUBECONFIG (optional, none, override for local dev), KUBERNETES_AUTH_MODE (optional, "in-cluster"), LOG_LEVEL (optional, "INFO"), WEKA_ENDPOINT (optional, none, future use for non-K8s WEKA)

    **5. OpenClaw Registration** — Reference `openclaw.json` directly:
    - Explain that `mcp-server/openclaw.json` contains the registration config
    - Show the file contents or point to it
    - Explain: place this in OpenClaw's MCP server config directory
    - Note: the `container` field should be updated to the full image reference (`wekachrisjen/weka-app-store-mcp:v2.0.0`)
    - Note: `skill` field points to SKILL.md which defines the agent workflow

    **6. NemoClaw Registration** — Placeholder per user decision:
    - State that NemoClaw uses similar stdio transport configuration
    - Show the known fields: transport, command, env vars, SKILL.md path
    - Include visible marker: `> TODO: Update this section when the NemoClaw alpha config schema is published.`

    **7. Verify It Works** — Smoke test per user decision:
    - Pipe a JSON-RPC tools/list request to container stdin
    - Command: `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' | docker run --rm -i -e BLUEPRINTS_DIR=/tmp wekachrisjen/weka-app-store-mcp:latest`
    - Expected: JSON response containing all 8 tool names

    **8. CI/CD** — Brief description:
    - Tests run on every PR touching mcp-server/
    - Image built and pushed to Docker Hub on v* tag push
    - Required GitHub Secrets: DOCKERHUB_USERNAME, DOCKERHUB_TOKEN (one-time setup in repo Settings > Secrets)

    **9. Troubleshooting** — Common issues:
    - "FATAL: BLUEPRINTS_DIR environment variable is required" — set the env var
    - Import errors at startup — check PYTHONPATH or image build
    - stdout pollution breaking stdio — ensure no print() in server code, use logging to stderr
    - Empty blueprint list — verify BLUEPRINTS_DIR volume mount contains YAML files

    Per user decision: "Full copy-paste command blocks for every step (build, tag, push, register) -- operators follow end-to-end without guessing"
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore && python -c "
content = open('mcp-server/README.md').read()
sections = ['Quick Start', 'Environment Variables', 'OpenClaw', 'NemoClaw', 'Verify', 'Troubleshooting']
missing = [s for s in sections if s.lower() not in content.lower()]
assert not missing, f'Missing sections: {missing}'
assert 'TODO' in content, 'Missing NemoClaw TODO marker'
assert 'openclaw.json' in content, 'Missing openclaw.json reference'
print('README has all required sections')
"</automated>
  </verify>
  <done>README.md exists with all 9 sections. Every step has copy-paste commands. OpenClaw section references openclaw.json. NemoClaw section has TODO marker. Troubleshooting covers common failure modes.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/mcp-server.yml` is valid YAML with test and build-push jobs
2. `mcp-server/README.md` contains all required sections with copy-paste commands
3. README references `openclaw.json` for OpenClaw registration
4. README NemoClaw section includes TODO marker
5. Full test suite still passes: `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest mcp-server/tests/ -q`
</verification>

<success_criteria>
- CI workflow runs tests on PR and builds/pushes image on v* tag
- README documents every step from build to register
- OpenClaw registration references existing openclaw.json
- NemoClaw registration has placeholder with TODO marker
- Smoke test command documented for verification
- GitHub Secrets setup documented
</success_criteria>

<output>
After completion, create `.planning/phases/09-deployment-and-registration/09-02-SUMMARY.md`
</output>
