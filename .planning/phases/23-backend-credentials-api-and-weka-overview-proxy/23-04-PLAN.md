---
phase: 23
plan: 04
type: execute
wave: 4
depends_on:
  - "23-02"
  - "23-03"
files_modified:
  - app-store-gui/tests/test_credentials_api.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-08
must_haves:
  truths:
    - "Pytest test file exists at app-store-gui/tests/test_credentials_api.py and is collected by the existing pytest harness"
    - "Tests cover GET shape, POST slug + collision, DELETE CR+raw Secret while preserving derived secrets, ?type filter ready=true only, WEKA overview cache 60s + bust=1, no credential values in any response body"
    - "All tests pass via PYTHONPATH=mcp-server:app-store-gui pytest app-store-gui/tests/test_credentials_api.py -v"
  artifacts:
    - path: app-store-gui/tests/test_credentials_api.py
      provides: "Behavioral tests using monkeypatch.setattr injection of CustomObjectsApi, CoreV1Api, urllib.request.urlopen, and _resolve_weka_credential_secret"
      contains: "def test_get_credentials_shape_excludes_secret_values"
      min_lines: 200
  key_links:
    - from: "app-store-gui/tests/test_credentials_api.py"
      to: "app-store-gui/webapp/main.py"
      via: "import webapp.main as main; monkeypatch.setattr(main.client, 'CustomObjectsApi', ...)"
      pattern: "monkeypatch\\.setattr\\(main"
---

<objective>
Create `app-store-gui/tests/test_credentials_api.py` covering all behavioral acceptance criteria from Plans 02 and 03, using the canonical stub-injection pattern from `app-store-gui/tests/planning/test_apply_gateway.py`. Tests must run without a live Kubernetes cluster or live WEKA endpoint — all I/O is monkeypatched.

Purpose: Without automated tests, the no-credential-values-in-responses-or-logs guarantee (API-08) cannot be verified. This file is the regression net for the entire credentials surface and also provides the executable proof of cache TTL behavior (API-05) and BACKEND filter behavior (API-06).

Output: One new test file with at minimum the test cases enumerated in D-16 of CONTEXT.md plus the pure-helper tests called out in Plan 02 Task 1 and Plan 03 Task 1 acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@app-store-gui/tests/planning/test_apply_gateway.py

<interfaces>
<!-- Symbols imported and exercised by the new test file -->

From app-store-gui/webapp/main.py (after Plans 01–03):
- module attribute: `client` (the kubernetes.client module, re-exported by import) — monkeypatch `main.client.CustomObjectsApi` and `main.client.CoreV1Api`
- module function: `load_kube_config` — monkeypatch to no-op via `monkeypatch.setattr(main, "load_kube_config", lambda: None)`
- module functions: `_make_credential_slug`, `_allocate_unique_credential_slug`, `_build_credential_response_item` (Plan 02 Task 1)
- module functions: `_assemble_weka_overview`, `_resolve_weka_credential_secret`, `_weka_login`, `_weka_get_json`, `_weka_post_json` (Plan 03 Task 1)
- module dict: `_weka_overview_cache` — clear between tests via `main._weka_overview_cache.clear()`
- handler coroutines: `list_credentials`, `create_credential`, `delete_credential`, `get_weka_overview` (the route handlers — call via asyncio.run on the coroutine, or use FastAPI's `TestClient` if simpler; CONTEXT.md D-15 forbids FastAPI TestClient — use direct coroutine invocation)

<!-- Test invocation -->

Per CLAUDE.md, run with: `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v`

The `BLUEPRINTS_DIR` env var is required because importing `webapp.main` may pull modules that read it. Tests themselves do not exercise blueprints.

<!-- Stub fixture shape — one fully-populated WarpCredential CR dict -->

def make_warpcred_cr_nvidia_ready(name="my-ngc", ns="default") -> dict:
    return {
        "apiVersion": "warp.io/v1alpha1",
        "kind": "WarpCredential",
        "metadata": {"name": name, "namespace": ns},
        "spec": {
            "type": "nvidia-ngc",
            "displayName": "My NGC",
            "secretRef": {"name": f"warp-cred-{name}", "key": "NGC_API_KEY"},
        },
        "status": {
            "conditions": [
                {"type": "KeyReady", "status": "True"},
                {"type": "DockerSecretReady", "status": "True"},
            ],
            "derivedSecrets": [
                {"name": f"warp-{name}-apikey", "type": "Opaque"},
                {"name": f"warp-{name}-docker", "type": "kubernetes.io/dockerconfigjson"},
            ],
            "lastSyncTime": "2026-06-11T00:00:00Z",
        },
    }

def make_warpcred_cr_weka_ready(name="primary", ns="default", endpoint="https://weka:14000") -> dict:
    return {
        "apiVersion": "warp.io/v1alpha1",
        "kind": "WarpCredential",
        "metadata": {"name": name, "namespace": ns},
        "spec": {
            "type": "weka-storage",
            "displayName": "WEKA Primary",
            "secretRef": {"name": f"warp-cred-{name}", "key": "WEKA_API_TOKEN"},
            "endpoint": endpoint,
        },
        "status": {
            "conditions": [{"type": "KeyReady", "status": "True"}],
            "derivedSecrets": [{"name": f"warp-{name}-token", "type": "Opaque"}],
            "lastSyncTime": "2026-06-11T00:00:00Z",
            "wekaEndpoint": endpoint,
        },
    }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_credentials_api.py with helper-unit tests and credentials CRUD handler tests</name>
  <files>app-store-gui/tests/test_credentials_api.py</files>
  <read_first>
    - app-store-gui/tests/planning/test_apply_gateway.py (canonical stub-injection pattern; copy the SimpleNamespace + monkeypatch + plain-class stub approach)
    - app-store-gui/tests/conftest.py (existing fixtures; do not modify, but understand what is already in scope)
    - app-store-gui/webapp/main.py (specifically the new helpers and routes from Plans 02 and 03)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (D-15, D-16 enumerating required coverage)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md (test file structure pattern at lines 297-353)
  </read_first>
  <action>
    Create the file `app-store-gui/tests/test_credentials_api.py` with the following structure. Use `import webapp.main as main` and patch via `monkeypatch.setattr`. Do NOT use FastAPI `TestClient` (per D-15). Invoke route coroutines directly via `asyncio.run(main.list_credentials(...))` (FastAPI does not require dependency-injection for plain Query/Form params when the coroutine is awaited directly with explicit kwargs).

    Structure:

    1. Module header: `from __future__ import annotations`, `import asyncio`, `import json`, `import pytest`, `import webapp.main as main`. Do NOT mark any test as `skip` or `xfail`.

    2. Two factory functions matching the `<interfaces>` block above: `make_warpcred_cr_nvidia_ready` and `make_warpcred_cr_weka_ready`. Also a `make_warpcred_cr_nvidia_not_ready(name)` that returns a similar CR with `KeyReady.status == "False"` and an `error` reason field.

    3. Three pure-helper tests (no monkeypatching needed):
       - `test_make_credential_slug_normalizes_and_truncates`: asserts `main._make_credential_slug("My NGC Key #1") == "my-ngc-key-1"`, `main._make_credential_slug("a"*60).startswith("a"*52) and len(main._make_credential_slug("a"*60)) == 52`, and `pytest.raises(ValueError, main._make_credential_slug, "---")`.
       - `test_build_credential_response_item_omits_secret_fields`: feed `make_warpcred_cr_nvidia_ready()` to `_build_credential_response_item`, assert the returned dict has keys exactly `{"name","namespace","type","displayName","ready","lastSyncTime","derivedSecrets","dockerSecretReady"}`, assert `ready is True` and `dockerSecretReady is True`, and assert no key in the dict matches any of `{"key","apiKey","token","secretRef","NGC_API_KEY","WEKA_API_TOKEN","WEKA_API_USERNAME","password"}`.
       - `test_build_credential_response_item_weka_storage_exposes_endpoint_only_from_status`: feed `make_warpcred_cr_weka_ready(endpoint="https://w:14000")` and assert `item["endpoint"] == "https://w:14000"` AND `"WEKA_API_TOKEN" not in json.dumps(item)`.

    4. GET handler tests (monkeypatch `main.client.CustomObjectsApi`, `main.client.CoreV1Api`, `main.load_kube_config`):
       - `test_list_credentials_returns_shape_without_secret_values(monkeypatch)`: stub returns one nvidia-ready + one weka-ready CR; invoke `asyncio.run(main.list_credentials(namespace="default", type=None))`; the underlying coroutine returns a `JSONResponse`. Extract body via `json.loads(response.body)`; assert `body["ok"] is True`, `len(body["items"]) == 2`, and assert `json.dumps(body)` contains no occurrence of `"NGC_API_KEY"`, `"WEKA_API_TOKEN"`, `"WEKA_API_USERNAME"`, `"secretRef"`, or `"apiKey"`.
       - `test_list_credentials_type_filter_returns_only_ready(monkeypatch)`: stub returns two nvidia CRs (one ready, one not-ready) and one weka-ready; invoke with `type="nvidia-ngc"`; assert `len(items) == 1` and `items[0]["ready"] is True`.

    5. POST handler tests:
       - `test_post_credential_nvidia_creates_secret_and_cr(monkeypatch)`:
         - Stub `CustomObjectsApi.list_namespaced_custom_object` to return `{"items": []}` (no existing slugs)
         - Stub `CustomObjectsApi.create_namespaced_custom_object` to record kwargs into a list `ops`
         - Stub `CoreV1Api.create_namespaced_secret` to record kwargs into `ops`
         - Stub `CoreV1Api.read_namespace` to return a SimpleNamespace (so ensure_namespace_exists is a no-op)
         - Invoke `asyncio.run(main.create_credential(display_name="My NGC", type="nvidia-ngc", namespace="default", key="nvapi_secret", username=None, endpoint=None))`
         - Assert response body has `name == "my-ngc"`, `type == "nvidia-ngc"`, `displayName == "My NGC"`, and no key in the body containing `"nvapi_"` (assert `"nvapi_secret" not in json.dumps(body)`)
         - Assert `ops` contains a `create_namespaced_secret` call with `body.metadata.name == "warp-cred-my-ngc"` and `body.string_data == {"NGC_API_KEY": "nvapi_secret"}`
         - Assert `ops` contains a `create_namespaced_custom_object` call with `plural="warpcredentials"`, `body["spec"]["type"] == "nvidia-ngc"`, `body["spec"]["secretRef"] == {"name": "warp-cred-my-ngc", "key": "NGC_API_KEY"}`
       - `test_post_credential_slug_collision_appends_suffix(monkeypatch)`:
         - Stub list returns `{"items": [{"metadata": {"name": "my-ngc"}}]}`
         - Invoke with `display_name="My NGC"`, `type="nvidia-ngc"`
         - Assert response `name == "my-ngc-2"` and the create_namespaced_secret call used `warp-cred-my-ngc-2`
       - `test_post_credential_weka_storage_persists_three_keys(monkeypatch)`:
         - Invoke with `type="weka-storage"`, `display_name="WEKA Primary"`, `key="weka-tok-xyz"`, `username="admin"`, `endpoint="https://w:14000"`
         - Assert the recorded `create_namespaced_secret` call has `string_data == {"WEKA_API_USERNAME": "admin", "WEKA_API_TOKEN": "weka-tok-xyz", "WEKA_API_ENDPOINT": "https://w:14000"}`
         - Assert the recorded `create_namespaced_custom_object` body has `spec.endpoint == "https://w:14000"` and `spec.secretRef.key == "WEKA_API_TOKEN"`
         - Assert the response body does NOT contain `"weka-tok-xyz"` or `"WEKA_API_TOKEN"` as substrings
       - `test_post_credential_invalid_type_returns_400(monkeypatch)`: invoke with `type="bogus"`; assert status_code == 400 and the error message does NOT contain the value of `key`.
       - `test_post_credential_weka_missing_username_returns_400(monkeypatch)`: invoke with `type="weka-storage"`, `key="tok"`, `username=None`, `endpoint="https://w:14000"`; assert status_code == 400 and error references the missing field name without echoing `key`.

    6. DELETE handler tests:
       - `test_delete_credential_deletes_cr_then_raw_secret_preserves_derived(monkeypatch)`:
         - Order-recording stubs: `CustomObjectsApi.delete_namespaced_custom_object` and `CoreV1Api.delete_namespaced_secret` both append `(call_name, kwargs)` to a shared `ops` list
         - Invoke `asyncio.run(main.delete_credential(name="my-ngc", namespace="default"))`
         - Assert `len(ops) == 2`
         - Assert `ops[0][0] == "delete_namespaced_custom_object"` and `ops[0][1]["plural"] == "warpcredentials"` and `ops[0][1]["name"] == "my-ngc"`
         - Assert `ops[1][0] == "delete_namespaced_secret"` and `ops[1][1]["name"] == "warp-cred-my-ngc"`
         - Assert NO ops entry has a name matching any of `warp-my-ngc-apikey`, `warp-my-ngc-docker`, `warp-my-ngc-token` (derived secrets preserved)
       - `test_delete_credential_idempotent_on_secret_404(monkeypatch)`: stub raises `ApiException(status=404)` from `delete_namespaced_secret`; assert handler returns `{"ok": True}` with 200.
       - `test_delete_credential_invalid_name_returns_400_without_io(monkeypatch)`: stub all client factories to raise if called; invoke with `name="Bad..Name"`; assert 400 and the stubs were never called.

    Add fixtures or helper functions as needed to keep individual test bodies under ~30 lines. Tests must be deterministic and use `monkeypatch.setattr(main, "load_kube_config", lambda: None)` in every test that touches handler code.

    Apply the existing `BLUEPRINTS_DIR` env var via `os.environ.setdefault("BLUEPRINTS_DIR", "/tmp")` at the top of the file IF importing `webapp.main` fails otherwise. Verify by running the file once before finalizing.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v -k "credential and not weka_overview"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `app-store-gui/tests/test_credentials_api.py`
    - File has at least one occurrence of each test name listed in `<action>`: `test_make_credential_slug_normalizes_and_truncates`, `test_build_credential_response_item_omits_secret_fields`, `test_build_credential_response_item_weka_storage_exposes_endpoint_only_from_status`, `test_list_credentials_returns_shape_without_secret_values`, `test_list_credentials_type_filter_returns_only_ready`, `test_post_credential_nvidia_creates_secret_and_cr`, `test_post_credential_slug_collision_appends_suffix`, `test_post_credential_weka_storage_persists_three_keys`, `test_post_credential_invalid_type_returns_400`, `test_post_credential_weka_missing_username_returns_400`, `test_delete_credential_deletes_cr_then_raw_secret_preserves_derived`, `test_delete_credential_idempotent_on_secret_404`, `test_delete_credential_invalid_name_returns_400_without_io`
    - `grep -c '^def test_' app-store-gui/tests/test_credentials_api.py` returns at least 13
    - `grep -F "from fastapi.testclient import TestClient" app-store-gui/tests/test_credentials_api.py` returns no match (D-15)
    - `grep -F "monkeypatch.setattr(main" app-store-gui/tests/test_credentials_api.py` returns at least three matches
    - `grep -F "nvapi_secret" app-store-gui/tests/test_credentials_api.py` returns matches only inside test bodies that ALSO assert the value is absent from the response body (verifiable by grepping nearby `not in json.dumps`)
    - Test command exit code 0: `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v -k "credential and not weka_overview"`
  </acceptance_criteria>
  <done>13+ tests cover helper purity, GET shape + filter, POST slug/collision/per-type-keys/validation, DELETE order + idempotency + path validation; entire credentials suite passes; no FastAPI TestClient used; no credential values leak into response assertions without an accompanying `not in` assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Add WEKA overview tests covering assembler purity, cache TTL, bust=1 bypass, and no-token leakage</name>
  <files>app-store-gui/tests/test_credentials_api.py</files>
  <read_first>
    - app-store-gui/tests/test_credentials_api.py (the file just created in Task 1 — append, do not rewrite)
    - app-store-gui/webapp/main.py (the WEKA helpers and `/api/weka/overview` route from Plan 03)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (D-04..D-07, D-16 cache + bust + no-leak coverage)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md (the urllib.request usage pattern in the "No Analog Found" section)
  </read_first>
  <action>
    Append the following tests to the file from Task 1. Each test must clear `main._weka_overview_cache` at entry (use a pytest fixture or call `main._weka_overview_cache.clear()` at the top of the test body).

    1. `test_assemble_weka_overview_pure_transform_shape`: call `main._assemble_weka_overview` directly with:
       - `filesystems_resp=[{"name":"fs1","uid":"deadbeef","total_budget":1000,"used_total":500}]`
       - `cluster_resp={"capacity":{"total_bytes":2000,"used_bytes":800}}`
       - `containers_resp=[{"role":"BACKEND","ip":"10.0.0.1"},{"role":"FRONTEND","ip":"10.0.0.2"},{"role":"BACKEND","ip":"127.0.0.1"}]`
       - `fetched_at_iso="2026-06-11T00:00:00Z"`
       Assert the returned dict equals:
       `{"capacity":{"totalBytes":2000,"usedBytes":800,"availableBytes":1200,"usedPercent":40.0},"filesystems":[{"name":"fs1","totalBytes":1000,"usedBytes":500,"usedPercent":50.0}],"backendNodes":[{"ip":"10.0.0.1"}],"fetchedAt":"2026-06-11T00:00:00Z"}`
       (No `uid` in filesystems; loopback 127.0.0.1 filtered out; FRONTEND skipped.)

    2. `test_assemble_weka_overview_tolerates_alt_field_names`: feed `{"name":"fs2","size":4000,"used_size":1000}` and `{"capacity":{"total":8000,"used":2000}}` and `[{"mode":"BACKEND","ip_address":"10.0.0.5"}]`; assert the response uses the alternate keys correctly.

    3. `test_assemble_weka_overview_capacity_fallback_when_no_cluster_capacity_dict`: feed `cluster_resp={"unrelated":"data"}` and two filesystems totaling 3000/1200; assert `capacity.totalBytes == 3000`, `capacity.usedBytes == 1200`, and `capacity.get("capacity_source") == "fallback-sum"`.

    4. `test_weka_overview_cache_hit_avoids_refetch(monkeypatch)`:
       - `main._weka_overview_cache.clear()`
       - Counter `call_count = {"get": 0}`
       - Monkeypatch `main._resolve_weka_credential_secret` to return `("https://w:14000", "admin", "weka-tok")`
       - Monkeypatch `main._weka_login` to return `"BEARER-TOKEN-DO-NOT-LEAK"`
       - Monkeypatch `main._weka_get_json` to a function that increments `call_count["get"]` and returns a minimal shape (`[]`, `{"capacity":{"total_bytes":1,"used_bytes":0}}`, `[]` cycling per URL — match on the URL substring)
       - Monkeypatch `main.load_kube_config` to no-op
       - First call: `r1 = asyncio.run(main.get_weka_overview(credential="primary", namespace="default", bust=0))`
       - Second call (immediately): `r2 = asyncio.run(main.get_weka_overview(credential="primary", namespace="default", bust=0))`
       - Assert `call_count["get"] == 3` (three calls on the cache miss; zero on the cache hit)
       - Assert `json.loads(r1.body)["data"]["fetchedAt"] == json.loads(r2.body)["data"]["fetchedAt"]`
       - Assert `json.loads(r2.body).get("cached") is True`
       - Assert `"BEARER-TOKEN-DO-NOT-LEAK" not in r2.body.decode("utf-8")` and `"weka-tok" not in r2.body.decode("utf-8")` and `"admin" not in r2.body.decode("utf-8")`

    5. `test_weka_overview_bust_query_bypasses_cache(monkeypatch)`:
       - Same setup as test 4
       - First call with `bust=0` populates the cache; assert `call_count["get"] == 3`
       - Second call with `bust=1`; assert `call_count["get"] == 6` (three more) and the response's `fetchedAt` differs from the first call's (control time via `monkeypatch.setattr(main.datetime.datetime, "now", classmethod(lambda cls, tz=None: <returnable controlled value>))` OR by sleeping 1 second — prefer monkeypatching `datetime.now` for determinism)

    6. `test_weka_overview_namespace_scoped_cache(monkeypatch)`:
       - Two calls: `(credential="primary", namespace="default")` then `(credential="primary", namespace="aidp")`
       - Assert `call_count["get"] == 6` (two fully separate cache misses — namespace scoping)
       - Assert `main._weka_overview_cache` has exactly two entries with keys `"default/primary"` and `"aidp/primary"`

    7. `test_weka_overview_invalid_credential_name_returns_400_without_io(monkeypatch)`:
       - Monkeypatch every helper to raise if called
       - Invoke with `credential="Bad..Name"`; assert status_code == 400 and no helper was called

    8. `test_weka_overview_login_failure_returns_502_without_leak(monkeypatch)`:
       - Monkeypatch `_resolve_weka_credential_secret` to return tuple OK
       - Monkeypatch `_weka_login` to raise `RuntimeError("WEKA login failed: 401 Unauthorized https://w:14000/api/v2/login?session=verysecret")`
       - Assert the response is `status_code == 502` and the body is the fixed string `{"ok": false, "error": "WEKA login failed"}` — the body MUST NOT contain `verysecret` or the full URL with query string

    9. `test_resolve_weka_credential_secret_decodes_base64_secret(monkeypatch)`:
       - Build a stub `CoreV1Api` whose `read_namespaced_secret` returns a SimpleNamespace with `.data` = dict mapping the three required keys to base64-encoded values (e.g. `base64.b64encode(b"admin").decode()`, etc.)
       - Build a stub `CustomObjectsApi` whose `get_namespaced_custom_object` returns `make_warpcred_cr_weka_ready()`
       - Monkeypatch `main.client.CustomObjectsApi` and `main.client.CoreV1Api`
       - Call `main._resolve_weka_credential_secret("primary", "default")`
       - Assert returns the tuple `("https://w:14000", "admin", <decoded token>)` and the function did not call `print()` or `logger.*` with the token value

    10. `test_resolve_weka_credential_secret_missing_key_raises_runtime(monkeypatch)`:
        - Stub secret has only `WEKA_API_TOKEN` (missing username + endpoint)
        - `pytest.raises(RuntimeError, match="WEKA_API_USERNAME")` (or whichever key the helper names first)

    Append, don't rewrite. After both tasks the file should contain ~20 tests total.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v</automated>
  </verify>
  <acceptance_criteria>
    - The full pytest invocation `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v` exits 0
    - `grep -c '^def test_' app-store-gui/tests/test_credentials_api.py` returns at least 20
    - File contains the test names: `test_assemble_weka_overview_pure_transform_shape`, `test_assemble_weka_overview_tolerates_alt_field_names`, `test_assemble_weka_overview_capacity_fallback_when_no_cluster_capacity_dict`, `test_weka_overview_cache_hit_avoids_refetch`, `test_weka_overview_bust_query_bypasses_cache`, `test_weka_overview_namespace_scoped_cache`, `test_weka_overview_invalid_credential_name_returns_400_without_io`, `test_weka_overview_login_failure_returns_502_without_leak`, `test_resolve_weka_credential_secret_decodes_base64_secret`, `test_resolve_weka_credential_secret_missing_key_raises_runtime`
    - `grep -F 'BEARER-TOKEN-DO-NOT-LEAK' app-store-gui/tests/test_credentials_api.py` matches must each appear within 5 lines of a `not in` assertion
    - The cache-hit test asserts exactly three calls happened on the miss, zero on the hit
    - The bust test asserts exactly three additional calls happened
    - The namespace-scoped test asserts exactly six calls happened
    - The login-failure test asserts the body does NOT contain the substring `verysecret`
  </acceptance_criteria>
  <done>10 additional WEKA-focused tests cover assembler purity (3), cache + bust + namespace scoping (3), invalid-name guard (1), login-failure no-leak (1), and base64-decode + missing-key (2). Total suite ≥ 20 tests; full suite passes; no real network or K8s I/O.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runtime → main.py module | Tests monkeypatch K8s clients and HTTP helpers; no real I/O crosses this boundary. |
| test assertions → response bodies | Tests treat response bodies as untrusted-text and explicitly assert credential-value substrings are absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Information Disclosure | regression of API-08 in future edits to main.py | mitigate | Tests assert `"nvapi_"`, `"weka-tok"`, `"admin"`, `"BEARER-TOKEN-DO-NOT-LEAK"`, `"WEKA_API_TOKEN"`, `"verysecret"` substrings are absent from response bodies. Any future regression that pipes a credential value into a response or error message fails these assertions. |
| T-23-04-02 | Tampering | tests that accidentally do real K8s or WEKA I/O | mitigate | Every handler test monkeypatches `main.load_kube_config` to no-op AND injects stub clients via `monkeypatch.setattr(main.client, ...)`. The Plan 03 helpers (`_resolve_weka_credential_secret`, `_weka_login`, `_weka_get_json`) are themselves monkeypatched in the cache/bust/namespace tests. The pure-helper tests (`_assemble_weka_overview`, `_make_credential_slug`, `_build_credential_response_item`) have no I/O surface at all. |
| T-23-04-03 | Repudiation | flaky tests | mitigate | `datetime.datetime.now` is controlled via monkeypatch in the bust test for deterministic `fetchedAt` advancement. Cache is cleared at every test entry. No tests rely on real sleep beyond the explicit 0-1s control. |
| T-23-04-04 | DoS | slow test suite | accept | Suite is mock-driven; no real network. Expected runtime < 5 seconds for 20+ tests. |
</threat_model>

<verification>
- `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest app-store-gui/tests/test_credentials_api.py -v` exits 0 with all tests passing
- Coverage matches D-16 fully: GET shape + filter, POST slug/collision, DELETE preservation of derived secrets, WEKA cache 60s + bust=1, no credential values in any response
- Pure-helper tests can run standalone (no monkeypatching) — they double as documentation of the API-06 shape
</verification>

<success_criteria>
- All seven phase requirement IDs (API-01 through API-08) have at least one passing test that proves the behavior
- The negation tests for API-08 (no credential values in response bodies; no bearer token in logs) are concrete substring asserts, not "looks correct" prose
- Test file is the regression net: future edits to the credentials API that violate any acceptance criterion above will fail this suite
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-04-SUMMARY.md` listing: (1) the final list of test names; (2) any deviation from the spec (e.g. extra helper tests added for clarity); (3) the actual pytest invocation used and its runtime.
</output>
