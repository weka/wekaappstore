---
phase: 23
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - app-store-gui/webapp/main.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-08
must_haves:
  truths:
    - "GET /api/credentials returns a JSON array shape with name, displayName, type, ready, lastSyncTime per WarpCredential CR — no raw credential values in the response"
    - "GET /api/credentials?type=<t> returns only credentials of that type with ready=true"
    - "POST /api/credentials creates one warp-cred-<slug> Secret AND one WarpCredential CR pointing at it"
    - "POST /api/credentials slug collisions append -2, -3, ... until unique"
    - "DELETE /api/credentials/<name> deletes the WarpCredential CR first then the warp-cred-<name> raw Secret; derived warp-<name>-* secrets are NOT touched"
    - "No raw key, username, token, or endpoint value appears anywhere in INFO or DEBUG log records emitted by these handlers"
  artifacts:
    - path: app-store-gui/webapp/main.py
      provides: "/api/credentials GET/POST/DELETE route handlers, _make_credential_slug helper, _allocate_unique_credential_slug helper, _build_credential_response_item helper, _CREDENTIAL_TYPE_KEYS constant"
      contains: "@app.get(\"/api/credentials\")"
  key_links:
    - from: "@app.get(\"/api/credentials\")"
      to: "CustomObjectsApi.list_namespaced_custom_object(group='warp.io', version='v1alpha1', plural='warpcredentials')"
      via: "load_kube_config() then async asyncio.to_thread wrapping the sync K8s call"
      pattern: "list_namespaced_custom_object.*warpcredentials"
    - from: "@app.post(\"/api/credentials\")"
      to: "create_or_update_secret then CustomObjectsApi.create_namespaced_custom_object"
      via: "warp-cred-<slug> Secret created first, then WarpCredential CR with spec.secretRef.name=warp-cred-<slug>"
      pattern: "create_or_update_secret\\(name=f\"warp-cred-"
    - from: "@app.delete(\"/api/credentials/{name}\")"
      to: "delete_namespaced_custom_object then CoreV1Api.delete_namespaced_secret"
      via: "CR deleted first to stop reconcile loop, raw Secret deleted second; derived warp-<name>-* untouched"
      pattern: "delete_namespaced_secret.*warp-cred-"
---

<objective>
Add the four `/api/credentials` route handlers to `app-store-gui/webapp/main.py`: `GET /api/credentials` (list), `GET /api/credentials?type=<t>` (filtered to ready=true; same handler, type parameter), `POST /api/credentials` (creates raw Secret + WarpCredential CR with slug collision handling), `DELETE /api/credentials/<name>` (deletes CR + raw Secret; preserves derived secrets). Cover API-01, API-02, API-03, API-04, and the GUI-backend portion of API-08 (no credential values logged).

Purpose: Phase 24 (Settings GUI) and Phase 25 (Blueprint SDK) consume this API. The CRD and operator (Phases 21-22) are already shipped and reconciling. This plan exposes the read/write surface from the GUI backend while keeping all credential values out of response bodies and logs.

Output: Four new FastAPI routes plus three private helpers added to the single-file `main.py`. No new modules or sub-packages (per CLAUDE.md). No new dependencies — `urllib`, `re`, `asyncio`, `time`, `base64`, `json`, `kubernetes` are already imported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@weka-app-store-operator-chart/templates/crd.yaml

<interfaces>
<!-- Existing helpers/clients in app-store-gui/webapp/main.py that this plan reuses. -->

From app-store-gui/webapp/main.py:
- def load_kube_config() -> None  (line 223) — call once at the top of each new async handler before instantiating clients
- def ensure_namespace_exists(namespace: Optional[str]) -> None  (line 277) — called transitively by create_or_update_secret
- def create_or_update_secret(name: str, namespace: str, string_data: Dict[str, str]) -> Dict[str, Any]  (line 535) — used by POST to create warp-cred-<slug>
- client.CustomObjectsApi (kubernetes) — used for warpcredentials CRUD: list_namespaced_custom_object, create_namespaced_custom_object, delete_namespaced_custom_object
- client.CoreV1Api (kubernetes) — used for delete_namespaced_secret(name=f"warp-cred-{slug}")
- kubernetes.client.rest.ApiException — caught first in the standard try/except block

<!-- WarpCredential CRD shape — locked by Phase 21, source weka-app-store-operator-chart/templates/crd.yaml lines 279-382 -->

CR group/version/plural: group="warp.io", version="v1alpha1", plural="warpcredentials", scope="Namespaced", kind="WarpCredential".

spec fields:
- type: enum string — "nvidia-ngc" | "huggingface" | "weka-storage"
- displayName: string (required)
- secretRef.name: string (required) — set to f"warp-cred-{slug}"
- secretRef.key: string (required) — see D-08/D-09: "NGC_API_KEY" for nvidia-ngc, "HF_API_KEY" for huggingface, "WEKA_API_TOKEN" for weka-storage
- endpoint: optional string (weka-storage only)

status fields read by GET handler:
- conditions: list of {type, status, reason, message, lastTransitionTime} — condition type "KeyReady" drives the `ready` flag; "DockerSecretReady" drives nvidia-ngc-only field
- derivedSecrets: list of {name, type}
- lastSyncTime: ISO timestamp
- wekaEndpoint: optional string (weka-storage only) — exposed as `endpoint` in API response

<!-- Per-type Secret key map (D-08, D-09, D-10) -->

_CREDENTIAL_TYPE_KEYS = {
    "nvidia-ngc": {"secret_ref_key": "NGC_API_KEY", "secret_data_keys": ["NGC_API_KEY"]},
    "huggingface": {"secret_ref_key": "HF_API_KEY", "secret_data_keys": ["HF_API_KEY"]},
    "weka-storage": {"secret_ref_key": "WEKA_API_TOKEN", "secret_data_keys": ["WEKA_API_USERNAME", "WEKA_API_TOKEN", "WEKA_API_ENDPOINT"]},
}

<!-- API response shape per item (from CONTEXT.md / PATTERNS.md) -->

Per-item JSON for GET /api/credentials:
- name: string (metadata.name)
- namespace: string
- type: string ("nvidia-ngc" | "huggingface" | "weka-storage")
- displayName: string (spec.displayName)
- ready: bool (true iff KeyReady condition.status == "True")
- lastSyncTime: string | null (status.lastSyncTime)
- derivedSecrets: list of {name, type} (status.derivedSecrets or [])
- error: string (only present when ready is false AND a KeyReady condition with status != "True" carries a message — use condition.message)
- dockerSecretReady: bool (nvidia-ngc only — DockerSecretReady condition.status == "True")
- endpoint: string | null (weka-storage only — status.wekaEndpoint, never from the raw Secret)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slug helpers, type-key constant, and GET /api/credentials handler (list + ?type filter)</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (read lines 1-30 for imports already present, lines 220-300 for load_kube_config and ensure_namespace_exists, lines 535-625 for the create_or_update_secret helper and the surviving /api/blueprints handlers that show the exact try/except/ApiException pattern)
    - weka-app-store-operator-chart/templates/crd.yaml (lines 279-382 for WarpCredential schema)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (D-01, D-03, D-11, D-12, and `<specifics>` section for the ready/dockerSecretReady/endpoint derivation rules)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md (list_blueprints analog and "WarpCredential CR Schema Reference" final block showing canonical response shape)
  </read_first>
  <action>
    Insert the following into `app-store-gui/webapp/main.py` immediately AFTER the surviving `@app.get("/api/secrets")` handler (which currently begins at line 641). All identifiers below are concrete and required — do not abbreviate or rename.

    A. Module-level constants and helpers (place above the new route handlers; keep PEP 8 two-blank-line spacing):

    1. `_CREDENTIAL_TYPE_KEYS: Dict[str, Dict[str, Any]]` — dict literal mapping the three valid type strings to per-type secret layout, matching the structure in the `<interfaces>` block above. Use exactly the keys `"secret_ref_key"` and `"secret_data_keys"`.

    2. `_VALID_CREDENTIAL_TYPES: tuple = tuple(_CREDENTIAL_TYPE_KEYS.keys())` — convenience tuple for POST validation.

    3. `def _make_credential_slug(display_name: str) -> str` — implements D-11 exactly: lowercase, replace `[^a-z0-9]+` with `-` via `re.sub`, strip leading/trailing `-`, then `return slug[:52]`. Empty result raises `ValueError("displayName produced an empty slug")` so POST can return 400.

    4. `def _allocate_unique_credential_slug(co_api, namespace: str, base_slug: str) -> str` — implements D-12: list existing warpcredentials in `namespace` via `co_api.list_namespaced_custom_object(group="warp.io", version="v1alpha1", plural="warpcredentials", namespace=namespace)`, build a `set(existing_names)`, then return `base_slug` if not in the set, else try `f"{base_slug}-2"`, `f"{base_slug}-3"`, ... up to suffix 99. Raise `RuntimeError("could not allocate unique credential slug after 99 attempts")` if exhausted.

    5. `def _build_credential_response_item(cr: Dict[str, Any]) -> Dict[str, Any]` — pure transform from a WarpCredential CR dict to the per-item API response shape defined in the `<interfaces>` block. Required logic:
       - `md = cr.get("metadata") or {}`; `spec = cr.get("spec") or {}`; `status = cr.get("status") or {}`
       - `conditions = status.get("conditions") or []`
       - Find the `KeyReady` condition (case-sensitive `type == "KeyReady"`); `ready = (key_ready_condition.get("status") == "True")` else `False`
       - For `nvidia-ngc`, also find `DockerSecretReady` and set `dockerSecretReady` boolean the same way
       - For `weka-storage`, set `endpoint = status.get("wekaEndpoint")` (never read from spec.endpoint; never read from raw Secret)
       - Build the item dict with keys in this order: `name`, `namespace`, `type`, `displayName`, `ready`, `lastSyncTime`, `derivedSecrets`
       - If `not ready` and the KeyReady condition has a `message`, add key `error` with that message
       - Add `dockerSecretReady` only when `spec.type == "nvidia-ngc"`
       - Add `endpoint` only when `spec.type == "weka-storage"`
       - The function MUST NOT include any key whose name suggests a token/key/username value; do NOT include `spec.secretRef.name` or `secretRef.key` in the response

    B. Route handler `@app.get("/api/credentials")` — `async def list_credentials(namespace: str = Query("default", description="Namespace to list WarpCredential CRs from; use 'all' for cluster-wide"), type: Optional[str] = Query(None, description="Filter by credential type; if set, only items of this type with ready=true are returned"))`:

    1. Wrap in `try/except ApiException/except Exception` block matching the convention at lines 568, 598, 620-623 of main.py.
    2. Call `load_kube_config()` (D-03).
    3. Define a sync inner function `_list()` that builds a `co_api = client.CustomObjectsApi()` and calls either `co_api.list_cluster_custom_object(...)` (when `namespace.strip().lower() in ("all", "*")`) or `co_api.list_namespaced_custom_object(..., namespace=namespace.strip())` with `group="warp.io"`, `version="v1alpha1"`, `plural="warpcredentials"`.
    4. `resp = await asyncio.to_thread(_list)` (per D-04 / asyncio.to_thread pattern).
    5. Iterate `(resp or {}).get("items", []) or []`, build items via `_build_credential_response_item(cr)`.
    6. If `type` query is provided: filter to items where `item["type"] == type AND item["ready"] is True` (API-02).
    7. Return `JSONResponse({"ok": True, "items": items})`.
    8. Error branches return `JSONResponse({"ok": False, "error": f"Kubernetes API error: {ae.status} {ae.reason}"}, status_code=500)` and `JSONResponse({"ok": False, "error": str(e)}, status_code=500)` exactly per the existing convention.
    9. No `logger.info/debug` calls that pass any value from `cr["spec"]` other than `type`, `displayName`, or `metadata.name`. Do NOT log the full CR dict (API-08).

    Confirm `re` is already imported at the top of main.py (line 14 area — verify). It is needed by `_make_credential_slug`.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; python -m py_compile app-store-gui/webapp/main.py &amp;&amp; grep -nE "^_CREDENTIAL_TYPE_KEYS\s*[:=]" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^_VALID_CREDENTIAL_TYPES\s*[:=]" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^def _make_credential_slug\(" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^def _allocate_unique_credential_slug\(" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^def _build_credential_response_item\(" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^@app\.get\(\"/api/credentials\"\)" app-store-gui/webapp/main.py &amp;&amp; grep -nE "plural=\"warpcredentials\"" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^import re$|^import re\b" app-store-gui/webapp/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0
    - `grep -n "_CREDENTIAL_TYPE_KEYS" app-store-gui/webapp/main.py` returns at least one match
    - `grep -n "_VALID_CREDENTIAL_TYPES" app-store-gui/webapp/main.py` returns at least one match
    - `grep -n "def _make_credential_slug" app-store-gui/webapp/main.py` returns exactly one definition
    - `grep -n "def _allocate_unique_credential_slug" app-store-gui/webapp/main.py` returns exactly one definition
    - `grep -n "def _build_credential_response_item" app-store-gui/webapp/main.py` returns exactly one definition
    - `grep -nE '@app\.get\("/api/credentials"\)' app-store-gui/webapp/main.py` returns exactly one match
    - `grep -nE 'plural="warpcredentials"' app-store-gui/webapp/main.py` returns at least two matches (list_cluster_custom_object and list_namespaced_custom_object branches)
    - `grep -nE 'group="warp.io".*version="v1alpha1"' app-store-gui/webapp/main.py` returns matches inside the new handler
    - Behavior (with stubbed CustomObjectsApi returning one nvidia-ngc CR with KeyReady=True): `GET /api/credentials?namespace=default` returns `{"ok": True, "items": [{...}]}` and the item dict contains keys `name, namespace, type, displayName, ready, lastSyncTime, derivedSecrets, dockerSecretReady` and DOES NOT contain any key named `key`, `apiKey`, `token`, `secretRef`, `username`, `password`, or `WEKA_API_TOKEN`
    - Behavior: `GET /api/credentials?type=nvidia-ngc` with stub data containing two NGC items (one ready, one not) returns exactly the ready item
    - Behavior: `_build_credential_response_item({"metadata":{"name":"x","namespace":"default"}, "spec":{"type":"weka-storage","displayName":"X"}, "status":{"wekaEndpoint":"https://w:14000","conditions":[{"type":"KeyReady","status":"True"}]}})` returns an item whose `endpoint == "https://w:14000"` and whose dict has no key matching `WEKA_API_TOKEN` or `secretRef`
    - Behavior: pytest-style direct call `_make_credential_slug("My NGC Key #1")` returns `"my-ngc-key-1"`; `_make_credential_slug("---")` raises `ValueError`
  </acceptance_criteria>
  <done>Helpers, constant, and GET handler added; module compiles; per-type response shape matches the `<interfaces>` contract; no credential-value fields leak into the response item dict.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /api/credentials (slug + collision + Secret + CR) and DELETE /api/credentials/{name}</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (read lines 535-555 for create_or_update_secret, lines 603-623 for delete_blueprint as the delete_namespaced_custom_object analog, and the new helpers from Task 1)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (D-02, D-08, D-09, D-10, D-11, D-12, plus `<specifics>` for Form() field style)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md (POST /api/secret/huggingface pattern showing Form-based handlers, delete_blueprint pattern, the WarpCredential CR shape reference at the bottom)
    - weka-app-store-operator-chart/templates/crd.yaml (lines 279-325 for spec field requirements)
  </read_first>
  <action>
    Append two new route handlers AFTER the GET handler created in Task 1. Continue PEP 8 two-blank-line spacing.

    A. `@app.post("/api/credentials")` — `async def create_credential(...)`:

    Use FastAPI `Form(...)` fields (per the `<specifics>` section of CONTEXT.md, matching the now-deleted /api/secret/* handler style):
    - `display_name: str = Form(...)` — required, non-empty after strip
    - `type: str = Form(...)` — required, must be in `_VALID_CREDENTIAL_TYPES`
    - `namespace: str = Form("default")` — defaults to "default"
    - `key: str = Form(...)` — required, the raw credential value
    - `username: Optional[str] = Form(None)` — required only when type == "weka-storage"
    - `endpoint: Optional[str] = Form(None)` — required only when type == "weka-storage"

    Handler steps (inside try/except ApiException/Exception per existing convention):

    1. Validate `display_name.strip()` non-empty; else return `JSONResponse({"ok": False, "error": "displayName is required"}, status_code=400)`.
    2. Validate `type in _VALID_CREDENTIAL_TYPES`; else return 400 with error `f"invalid credential type: {type}; must be one of {list(_VALID_CREDENTIAL_TYPES)}"`.
    3. If `type == "weka-storage"`: require `username` and `endpoint` non-empty (strip); else return 400 with the missing-field name. Do NOT echo the `key` value in the error.
    4. `load_kube_config()` (D-03).
    5. `ns = namespace.strip() or "default"`.
    6. `base_slug = _make_credential_slug(display_name)` — catch `ValueError` and return 400.
    7. Build `co_api = client.CustomObjectsApi()` and `slug = await asyncio.to_thread(_allocate_unique_credential_slug, co_api, ns, base_slug)`. (D-12 collision check via list)
    8. Build `string_data` from `_CREDENTIAL_TYPE_KEYS[type]["secret_data_keys"]` per D-08/D-09/D-10:
       - `nvidia-ngc` → `{"NGC_API_KEY": key}`
       - `huggingface` → `{"HF_API_KEY": key}`
       - `weka-storage` → `{"WEKA_API_USERNAME": username, "WEKA_API_TOKEN": key, "WEKA_API_ENDPOINT": endpoint}`
    9. Create the raw Secret: `await asyncio.to_thread(create_or_update_secret, f"warp-cred-{slug}", ns, string_data)`. The Secret name pattern is `warp-cred-{slug}` (matches D-11 and 23-CONTEXT.md).
    10. Build the WarpCredential CR body dict:
        ```
        body = {
            "apiVersion": "warp.io/v1alpha1",
            "kind": "WarpCredential",
            "metadata": {"name": slug, "namespace": ns},
            "spec": {
                "type": type,
                "displayName": display_name.strip(),
                "secretRef": {"name": f"warp-cred-{slug}", "key": _CREDENTIAL_TYPE_KEYS[type]["secret_ref_key"]},
            },
        }
        ```
        Then for `type == "weka-storage"` add `body["spec"]["endpoint"] = endpoint.strip()`.
    11. Create the CR: `await asyncio.to_thread(co_api.create_namespaced_custom_object, group="warp.io", version="v1alpha1", namespace=ns, plural="warpcredentials", body=body)`.
    12. Return `JSONResponse({"ok": True, "name": slug, "namespace": ns, "type": type, "displayName": display_name.strip()})`. DO NOT include `key`, `username`, `endpoint`, or any field that would echo a raw credential value (API-08; weka-storage endpoint specifically excluded from the response per CONTEXT.md `<specifics>` — frontend re-reads it via GET /api/credentials).
    13. ApiException 409 from the CR create (race after slug list) is converted to `JSONResponse({"ok": False, "error": f"slug {slug} already taken; retry"}, status_code=409)`.
    14. Logging: any `logger` call MUST reference only `slug`, `ns`, and `type`. Do not pass the `key`, `username`, `endpoint`, the full `body`, or the full `string_data` to any logger call.

    B. `@app.delete("/api/credentials/{name}")` — `async def delete_credential(name: str, namespace: str = Query("default", description="Namespace of the WarpCredential CR"))`:

    1. `load_kube_config()`.
    2. Validate `name` matches `^[a-z0-9](?:[a-z0-9-]{0,251}[a-z0-9])?$` (DNS-1123 subdomain — basic guard against arbitrary slug injection via the path parameter). On failure return `JSONResponse({"ok": False, "error": "invalid credential name"}, status_code=400)`.
    3. `ns = namespace.strip() or "default"`.
    4. Build `co_api = client.CustomObjectsApi()` and `core = client.CoreV1Api()`.
    5. Delete the WarpCredential CR first (per `<specifics>` rule — stops the operator reconcile loop before the raw Secret disappears):
       ```
       body = client.V1DeleteOptions(propagation_policy="Foreground")
       await asyncio.to_thread(
           co_api.delete_namespaced_custom_object,
           group="warp.io", version="v1alpha1", namespace=ns, plural="warpcredentials", name=name, body=body,
       )
       ```
       If the CR delete raises ApiException with status 404, continue to step 6 (Secret cleanup is still desirable for orphaned raw Secrets). Other ApiException values bubble to the outer except.
    6. Delete the raw Secret named `f"warp-cred-{name}"`:
       ```
       await asyncio.to_thread(core.delete_namespaced_secret, name=f"warp-cred-{name}", namespace=ns)
       ```
       Catch ApiException with status 404 and treat as a non-error (idempotent delete).
    7. EXPLICITLY do not call any delete on objects matching the pattern `warp-{name}-*` — those are the operator-derived secrets (e.g., `warp-{name}-apikey`, `warp-{name}-docker`, `warp-{name}-token`) and must remain after CR deletion per API-04 and OPS-08.
    8. Return `JSONResponse({"ok": True})`.
    9. ApiException error branch: `JSONResponse({"ok": False, "error": f"Kubernetes API error: {ae.status} {ae.reason}"}, status_code=500)`. Generic Exception branch: `JSONResponse({"ok": False, "error": str(e)}, status_code=500)`. No logger call passes the full credential body or the `name` as a credential-value field — `name` is metadata and is safe to log.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; python -m py_compile app-store-gui/webapp/main.py &amp;&amp; grep -nE "^@app\.post\(\"/api/credentials\"\)" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^@app\.delete\(\"/api/credentials/\{name\}\"\)" app-store-gui/webapp/main.py &amp;&amp; grep -nE "create_or_update_secret\(f?\"warp-cred-" app-store-gui/webapp/main.py &amp;&amp; grep -nE "delete_namespaced_secret\(name=f?\"warp-cred-" app-store-gui/webapp/main.py &amp;&amp; grep -nE "delete_namespaced_custom_object" app-store-gui/webapp/main.py &amp;&amp; grep -nE "_CREDENTIAL_TYPE_KEYS\[type\]\[\"secret_ref_key\"\]" app-store-gui/webapp/main.py &amp;&amp; ! grep -nE "logger\.(info|debug|warning|error).*(api_key|apikey|key=|key,|password|token=|token,|WEKA_API_TOKEN|HF_API_KEY|NGC_API_KEY)" app-store-gui/webapp/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0
    - `grep -nE '@app\.post\("/api/credentials"\)' app-store-gui/webapp/main.py` returns exactly one match
    - `grep -nE '@app\.delete\("/api/credentials/\{name\}"\)' app-store-gui/webapp/main.py` returns exactly one match
    - `grep -F 'create_or_update_secret(f"warp-cred-' app-store-gui/webapp/main.py` returns at least one match (POST uses the existing helper to create the raw Secret with the warp-cred- prefix)
    - `grep -F 'delete_namespaced_secret' app-store-gui/webapp/main.py` returns at least one match inside the new DELETE handler
    - `grep -E 'delete_namespaced_custom_object' app-store-gui/webapp/main.py` returns at least two matches (existing blueprints delete + new credentials delete)
    - No occurrence of any string literal matching `warp-{name}-apikey`, `warp-{name}-docker`, `warp-{name}-token` is passed to any `delete_namespaced_secret` call (verified by `grep`)
    - The grep gate in `<verify>` confirms no `logger` call line contains a token/key field name literal alongside a credential payload variable
    - Behavior (POST): with stubbed CustomObjectsApi and CoreV1Api, `create_credential` invoked with `display_name="My NGC Key"`, `type="nvidia-ngc"`, `namespace="default"`, `key="nvapi_xxx"` returns 200 with body `{"ok": True, "name": "my-ngc-key", "namespace": "default", "type": "nvidia-ngc", "displayName": "My NGC Key"}` and the response body contains NO substring matching `"nvapi_"` or the raw key
    - Behavior (POST collision): with stub listing an existing CR named `my-ngc-key`, a second POST with the same `display_name` returns `{"ok": True, "name": "my-ngc-key-2", ...}`
    - Behavior (POST weka-storage): missing `username` or `endpoint` returns 400 with an error message that does NOT contain the value of `key`
    - Behavior (POST invalid type): `type="bogus"` returns 400 with error `"invalid credential type: bogus; must be one of ['nvidia-ngc', 'huggingface', 'weka-storage']"`
    - Behavior (DELETE happy path): with stubs, `delete_credential(name="my-ngc-key", namespace="default")` issues exactly two K8s calls in order — `delete_namespaced_custom_object(...)` then `delete_namespaced_secret(name="warp-cred-my-ngc-key", ...)` — and zero calls touching names matching `warp-my-ngc-key-*` (derived secrets preserved)
    - Behavior (DELETE invalid path): `name="My/Bad..Name"` returns 400 without making any K8s call
    - Behavior (DELETE 404 on Secret): if `delete_namespaced_secret` raises ApiException(status=404), the handler still returns `{"ok": True}` (idempotent)
  </acceptance_criteria>
  <done>POST and DELETE handlers added; helper from Task 1 reused for slug allocation; raw Secret prefix `warp-cred-` enforced; derived `warp-{name}-*` Secrets are never touched by DELETE; no credential values leak into response bodies or log lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/credentials | Untrusted multipart form data crosses here on POST (key, username, endpoint as Form fields). Validation happens before any K8s call. |
| /api/credentials → kube-apiserver | App Store backend uses load_kube_config + CustomObjectsApi/CoreV1Api; auth is the operator's ServiceAccount (inherited from existing handlers). Namespace scoping is enforced by the explicit `namespace` parameter on every K8s call. |
| /api/credentials response → browser | Response bodies are JSONResponse and must never include raw credential values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | POST/DELETE/GET responses (API-08) | mitigate | `_build_credential_response_item` whitelists exactly the fields name, namespace, type, displayName, ready, lastSyncTime, derivedSecrets, dockerSecretReady, endpoint, error — and reads endpoint only from `status.wekaEndpoint`. POST response echoes only `name, namespace, type, displayName`. Acceptance criteria assert no `nvapi_`/`hf_`/`WEKA_API_TOKEN` substring appears in any response body. Grep gate forbids logger calls carrying credential payload variables. |
| T-23-02-02 | Information Disclosure | log lines | mitigate | Logger calls in the new handlers may reference only `slug`, `name`, `namespace`, `type` (all metadata). The `<verify>` grep gate rejects `logger.(info|debug|warning|error)` lines containing `api_key|password|token=|WEKA_API_TOKEN|HF_API_KEY|NGC_API_KEY` literals. |
| T-23-02-03 | Tampering | slug injection via displayName | mitigate | `_make_credential_slug` applies `re.sub(r"[^a-z0-9]+", "-", ...)` then strips and truncates to 52 chars, then validates non-empty. Result is DNS-1123-compatible and cannot inject `/`, `..`, whitespace, or shell metacharacters. Acceptance: `_make_credential_slug("../etc/passwd")` returns `"etc-passwd"`; `_make_credential_slug("---")` raises ValueError. |
| T-23-02-04 | Tampering | DELETE path parameter | mitigate | DELETE validates `name` against `^[a-z0-9](?:[a-z0-9-]{0,251}[a-z0-9])?$` before any K8s call. Acceptance criterion `name="My/Bad..Name" → 400 without K8s call` confirms. |
| T-23-02-05 | Tampering | Cross-namespace leak | mitigate | Every K8s call passes the explicit `namespace=ns` argument; no use of `list_secret_for_all_namespaces` or cluster-scoped delete. GET defaults to `namespace="default"`; `?namespace=all` uses `list_cluster_custom_object` which is read-only and returns only metadata-shape fields built by `_build_credential_response_item` (no Secret data crosses the boundary). |
| T-23-02-06 | Spoofing / unauthorized create | unauthenticated POST | accept | This API inherits the same `ClusterInitMiddleware`-gated FastAPI app as the rest of the GUI; there is no per-route authn beyond what exists today. Phase 23 does not introduce a new auth surface; Phase v2 (KCLO) defines group-scoped auth. Documented as accepted residual. |
| T-23-02-07 | Denial of Service | unbounded slug suffix loop | mitigate | `_allocate_unique_credential_slug` caps suffix at 99 and raises RuntimeError; outer except returns 500 cleanly. |
| T-23-02-08 | Tampering | accidental deletion of derived secrets | mitigate | DELETE handler deletes exactly the CR and exactly `warp-cred-{name}`. No code path constructs a name matching `warp-{name}-apikey`, `warp-{name}-docker`, or `warp-{name}-token`. Acceptance criteria assert this in stub-driven behavior. |
| T-23-02-09 | Information Disclosure | error messages echoing credential | mitigate | Validation errors for `type` echo only the type string. Validation errors for missing `username`/`endpoint` reference field names, never values. POST response does not include `key`, `username`, or `endpoint`. |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py` exits 0
- All grep gates in Task 1 and Task 2 `<verify>` blocks succeed
- Plan 04 supplies the behavioral test coverage (`pytest app-store-gui/tests/test_credentials_api.py`)
- The `<acceptance_criteria>` for each task can each be checked by direct invocation of the new helpers and handlers from a Python REPL with mocked clients (Plan 04 automates this)
</verification>

<success_criteria>
- API-01: `GET /api/credentials` returns the documented JSON shape per CR — verifiable via Plan 04 tests
- API-02: `GET /api/credentials?type=<t>` returns ready-only items of the matching type
- API-03: `POST /api/credentials` creates `warp-cred-<slug>` Secret + WarpCredential CR; slug collision handled
- API-04: `DELETE /api/credentials/<name>` removes CR + raw Secret; derived secrets preserved
- API-08 (GUI portion): no raw credential values appear in any response body or log line emitted by the new handlers (verified by both grep gates and behavioral assertions in Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-02-SUMMARY.md` listing: (1) exact line range where the new constants, helpers, and routes were inserted; (2) the final identifier names if any were adjusted from the spec; (3) any deviations from D-08/D-09/D-10/D-11/D-12 and why.
</output>
