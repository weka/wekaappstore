---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/templates/settings.html
autonomous: true
requirements:
  - API-07
must_haves:
  truths:
    - "GET /api/secret/huggingface returns 404 (route removed)"
    - "GET /api/secret/nvidia returns 404 (route removed)"
    - "settings.html loads without JavaScript console errors after removal"
    - "settings.html no longer renders the HuggingFace API Key card or the NVIDIA API Key card"
    - "loadBlueprints() with scope='current' still resolves a namespace via localStorage fallback (no ReferenceError on getSettingsNamespace)"
  artifacts:
    - path: app-store-gui/webapp/main.py
      provides: "removal of save_huggingface_key and save_nvidia_key route handlers"
      contains: "create_or_update_secret"
    - path: app-store-gui/webapp/templates/settings.html
      provides: "settings page cleaned of deprecated HF/NVIDIA UI"
  key_links:
    - from: app-store-gui/webapp/templates/settings.html
      to: localStorage.selectedNamespace
      via: "inline fallback inside loadBlueprints() scope==='current' branch"
      pattern: "localStorage.getItem\\('selectedNamespace'\\)"
---

<objective>
Remove the deprecated /api/secret/huggingface and /api/secret/nvidia FastAPI route handlers from `app-store-gui/webapp/main.py` and remove the corresponding HTML sections and JavaScript blocks from `app-store-gui/webapp/templates/settings.html`. This implements API-07 and clears the old API surface so Plan 02 can introduce the new `/api/credentials` endpoints on a clean baseline.

Purpose: API-07 requires the old single-key Secret endpoints to be gone. The UI Removal Contract (23-UI-SPEC.md) defines exactly which HTML and JS blocks must disappear and which must stay. This plan is removal-only — no new routes, no new HTML, no new JS.

Output: A `main.py` with no `/api/secret/huggingface` and no `/api/secret/nvidia` route handlers, and a `settings.html` whose remaining sections are Kubernetes Auth Status, Cluster Status, Blueprint Uninstall, Debug (in that order) and whose JavaScript references no removed DOM IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-UI-SPEC.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Existing helper RETAINED — Plan 02 reuses it. Do not remove. -->

From app-store-gui/webapp/main.py (lines 535-555, RETAINED):
- def create_or_update_secret(name: str, namespace: str, string_data: Dict[str, str]) -> Dict[str, Any]
  - Loads kube config, ensures namespace, creates or patches an Opaque Secret with the given string_data.
  - Returns {"name": ..., "namespace": ..., "action": "created" | "updated"}.

From app-store-gui/webapp/main.py (RETAINED handlers around this surgery):
- @app.get("/api/blueprints") at line 573 — unchanged
- @app.delete("/api/blueprints/{namespace}/{name}") at line 603 — unchanged
- @app.get("/api/secrets") at line 641 — unchanged (used by other code paths)

From app-store-gui/webapp/main.py (REMOVED in this plan):
- @app.post("/api/secret/huggingface") — save_huggingface_key (lines 558-570)
- @app.post("/api/secret/nvidia") — save_nvidia_key (lines 626-638)

From app-store-gui/webapp/templates/settings.html (REMOVED in this plan):
- Section 1: HuggingFace API Key (lines 53-81)
- Section 2: NVIDIA API Key (lines 83-112)
- function getSettingsNamespace() (lines 321-328)
- function setSettingsNamespace(ns) (lines 330-341)
- function renderSecrets(containerId, items) (lines 343-360)
- async function loadSecrets(ns) (lines 362-383)
- hfBtn click handler block (lines 386-412)
- nvBtn click handler block (lines 414-440)
- standalone call `loadSecrets();` at line 485

From app-store-gui/webapp/templates/settings.html (RETAINED but EDITED):
- async function loadBlueprints() at line 543 — its line 546 call to `getSettingsNamespace()` becomes an inline localStorage fallback (see Task 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /api/secret/huggingface and /api/secret/nvidia handlers from main.py</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (read lines 535-640 to see the two handlers, the retained `create_or_update_secret` helper between them, and the immediately adjacent `@app.get("/api/blueprints")` and `@app.get("/api/secrets")` handlers to confirm surgical boundaries)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-UI-SPEC.md (Removal Contract section — "Endpoints to Remove from `app-store-gui/webapp/main.py`" table)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (decisions D-13)
  </read_first>
  <action>
    Per D-13 of CONTEXT.md and the UI-SPEC Removal Contract, delete two FastAPI route handlers from `app-store-gui/webapp/main.py`:

    1. Delete the entire `@app.post("/api/secret/huggingface")` decorator and the `async def save_huggingface_key(api_key: str = Form(...), namespace: str = Form("default"))` function body (currently lines 558-570 inclusive). Remove the blank line that immediately precedes it only if needed to preserve a single blank line between `create_or_update_secret` (which ends at line 555) and the next retained block.
    2. Delete the entire `@app.post("/api/secret/nvidia")` decorator and the `async def save_nvidia_key(api_key: str = Form(...), namespace: str = Form("default"))` function body (currently lines 626-638 inclusive). The retained `@app.get("/api/secrets")` handler at line 641 must remain untouched.

    DO NOT remove:
    - The `create_or_update_secret()` helper at lines 535-555 — Plan 02 reuses it for `warp-cred-<slug>` Secret creation.
    - The `ensure_namespace_exists()` helper.
    - Any `import` line (Form, JSONResponse, ApiException are still needed by other handlers).
    - The `@app.get("/api/blueprints")`, `@app.delete("/api/blueprints/{namespace}/{name}")`, or `@app.get("/api/secrets")` handlers.

    Preserve PEP 8 spacing: leave exactly two blank lines between top-level function/decorator blocks. Match the project's existing double-quote style (do not reformat surviving lines).
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; python -m py_compile app-store-gui/webapp/main.py &amp;&amp; ! grep -nE "^@app\.post\(\"/api/secret/(huggingface|nvidia)\"\)" app-store-gui/webapp/main.py &amp;&amp; ! grep -nE "^async def (save_huggingface_key|save_nvidia_key)\b" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^def create_or_update_secret\b" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^@app\.get\(\"/api/blueprints\"\)" app-store-gui/webapp/main.py &amp;&amp; grep -nE "^@app\.get\(\"/api/secrets\"\)" app-store-gui/webapp/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0
    - `grep -n '/api/secret/huggingface' app-store-gui/webapp/main.py` returns no match
    - `grep -n '/api/secret/nvidia' app-store-gui/webapp/main.py` returns no match
    - `grep -n 'def save_huggingface_key' app-store-gui/webapp/main.py` returns no match
    - `grep -n 'def save_nvidia_key' app-store-gui/webapp/main.py` returns no match
    - `grep -n 'def create_or_update_secret' app-store-gui/webapp/main.py` returns exactly one match (helper retained)
    - `grep -n '@app.get("/api/blueprints")' app-store-gui/webapp/main.py` returns exactly one match
    - `grep -n '@app.get("/api/secrets")' app-store-gui/webapp/main.py` returns exactly one match
    - Behavior: starting the FastAPI app and issuing `curl -i -X POST http://localhost:8000/api/secret/huggingface` returns HTTP 404 (route no longer exists)
    - Behavior: starting the FastAPI app and issuing `curl -i -X POST http://localhost:8000/api/secret/nvidia` returns HTTP 404 (route no longer exists)
  </acceptance_criteria>
  <done>Both `/api/secret/*` handlers deleted from main.py; helper `create_or_update_secret` retained; module compiles; no other handlers touched.</done>
</task>

<task type="auto">
  <name>Task 2: Strip deprecated HTML sections and JS from settings.html and rewire loadBlueprints namespace fallback</name>
  <files>app-store-gui/webapp/templates/settings.html</files>
  <read_first>
    - app-store-gui/webapp/templates/settings.html (full file — confirm exact line ranges of the 8 removal targets; the line numbers in 23-UI-SPEC.md are authoritative but verify offsets after each removal)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-UI-SPEC.md (Removal Contract — "HTML Sections to Remove" and "JavaScript to Remove" tables, and the note about line 547 `loadBlueprints` inline fallback)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md (D-14)
    - .planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-PATTERNS.md (settings.html removal pattern, including the line 547 `loadBlueprints` note)
  </read_first>
  <action>
    Per D-14 and the UI-SPEC Removal Contract, perform an in-place edit of `app-store-gui/webapp/templates/settings.html`:

    REMOVALS (in order, top-to-bottom — re-locate each block by content if line numbers shift after earlier removals):

    1. Remove the `<!-- Section 1: HuggingFace API Key -->` `<section class="card rounded-lg p-5">...</section>` block (currently lines 53-81). The block contains DOM IDs `hf-api-key`, `hf-namespace`, `hf-save`, `hf-result`, `hf-secrets-list`, `hf-listing-scope`.
    2. Remove the `<!-- Section 2: NVIDIA API Key -->` `<section class="card rounded-lg p-5">...</section>` block (currently lines 83-112). The block contains DOM IDs `nvidia-api-key`, `nvidia-namespace`, `nvidia-save`, `nvidia-result`, `nvidia-secrets-list`, `nvidia-listing-scope`.
    3. Remove the `function getSettingsNamespace()` definition (currently lines 321-328).
    4. Remove the `function setSettingsNamespace(ns)` definition (currently lines 330-341).
    5. Remove the `function renderSecrets(containerId, items)` definition (currently lines 343-360).
    6. Remove the `async function loadSecrets(ns)` definition (currently lines 362-383).
    7. Remove the `// Save HF key` block including the `const hfBtn = document.getElementById('hf-save');` declaration and its `if (hfBtn) { ... }` body (currently lines 386-412).
    8. Remove the `// Save NVIDIA key` block including the `const nvBtn = document.getElementById('nvidia-save');` declaration and its `if (nvBtn) { ... }` body (currently lines 414-440).
    9. Remove the standalone `loadSecrets();` call (currently line 485). The line `refreshAuthStatus();` immediately following and the `setInterval(refreshAuthStatus, 10000);` on the next line MUST be retained.

    EDIT (rewire — required to avoid ReferenceError now that `getSettingsNamespace` is gone):

    10. Inside `async function loadBlueprints()` (currently line 543), replace the line that reads `const namespace = (scope === 'current') ? getSettingsNamespace() : 'all';` (currently line 546) with the inline fallback specified in UI-SPEC: `const namespace = (scope === 'current') ? (localStorage.getItem('selectedNamespace') || 'default') : 'all';`. This is the only reference site that survives outside the deletion set; the patterns map identifies it explicitly.

    RETENTION (do not modify):
    - Section 3 "Kubernetes Auth Status" through end of file (Cluster Status, Blueprint Uninstall, Debug)
    - `refreshAuthStatus()`, `setInterval(refreshAuthStatus, 10000)`, the entire blueprint listing/uninstall block including `renderBlueprints`, the `bp-refresh` and `bp-scope` event wiring, the initial `loadBlueprints()` call, and the Debug toggle block at the end
    - `get()`, `post()`, `del()` fetch helpers (used by `loadBlueprints` and the delete buttons)

    After removals the remaining section order in `<main>` must be:
    1. Kubernetes Auth Status
    2. Cluster Status
    3. Blueprint Uninstall
    4. Debug
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore &amp;&amp; ! grep -nE "Section 1: HuggingFace API Key|Section 2: NVIDIA API Key" app-store-gui/webapp/templates/settings.html &amp;&amp; ! grep -nE "id=\"(hf-api-key|hf-namespace|hf-save|hf-result|hf-secrets-list|hf-listing-scope|nvidia-api-key|nvidia-namespace|nvidia-save|nvidia-result|nvidia-secrets-list|nvidia-listing-scope)\"" app-store-gui/webapp/templates/settings.html &amp;&amp; ! grep -nE "function (getSettingsNamespace|setSettingsNamespace|renderSecrets)\b" app-store-gui/webapp/templates/settings.html &amp;&amp; ! grep -nE "async function loadSecrets\b" app-store-gui/webapp/templates/settings.html &amp;&amp; ! grep -nE "/api/secret/(huggingface|nvidia)" app-store-gui/webapp/templates/settings.html &amp;&amp; ! grep -nE "document\.getElementById\('hf-save'\)|document\.getElementById\('nvidia-save'\)" app-store-gui/webapp/templates/settings.html &amp;&amp; grep -nF "localStorage.getItem('selectedNamespace') || 'default'" app-store-gui/webapp/templates/settings.html &amp;&amp; grep -nF "refreshAuthStatus();" app-store-gui/webapp/templates/settings.html &amp;&amp; grep -nF "setInterval(refreshAuthStatus, 10000);" app-store-gui/webapp/templates/settings.html &amp;&amp; grep -nE "Kubernetes Auth Status" app-store-gui/webapp/templates/settings.html &amp;&amp; grep -nE "Blueprint" app-store-gui/webapp/templates/settings.html &amp;&amp; python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('app-store-gui/webapp/templates')); env.get_template('settings.html')"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Jinja2 (the python one-liner in `<verify>` exits 0)
    - `grep -nE "Section 1: HuggingFace API Key" app-store-gui/webapp/templates/settings.html` returns no match
    - `grep -nE "Section 2: NVIDIA API Key" app-store-gui/webapp/templates/settings.html` returns no match
    - None of the removed DOM IDs (`hf-api-key`, `hf-namespace`, `hf-save`, `hf-result`, `hf-secrets-list`, `hf-listing-scope`, `nvidia-api-key`, `nvidia-namespace`, `nvidia-save`, `nvidia-result`, `nvidia-secrets-list`, `nvidia-listing-scope`) appear anywhere in the file
    - None of the removed function names (`getSettingsNamespace`, `setSettingsNamespace`, `renderSecrets`, `loadSecrets`) appear anywhere in the file
    - `grep -n '/api/secret/' app-store-gui/webapp/templates/settings.html` returns no match
    - `grep -F "localStorage.getItem('selectedNamespace') || 'default'" app-store-gui/webapp/templates/settings.html` returns exactly one match (the new inline fallback inside `loadBlueprints()`)
    - `grep -F "refreshAuthStatus();" app-store-gui/webapp/templates/settings.html` returns at least one match (initial call retained)
    - `grep -F "setInterval(refreshAuthStatus, 10000);" app-store-gui/webapp/templates/settings.html` returns one match (polling retained)
    - Section headings appearing in document order are: Kubernetes Auth Status, Cluster Status, Blueprint, Debug (verifiable with `grep -nE "<h3 .*>(Kubernetes Auth Status|Cluster Status|Blueprint|Debug)"` or equivalent)
    - Behavior: rendering the template with a stub `auth` context yields HTML that, when loaded in a browser, throws no `ReferenceError` and shows the four surviving sections in the listed order
  </acceptance_criteria>
  <done>Two `<section>` cards removed, four JS functions removed, two button handlers removed, standalone `loadSecrets()` call removed, `loadBlueprints` namespace lookup rewired to inline localStorage fallback; template still parses; no removed DOM IDs or function names remain anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/secret/* | Pre-removal: untrusted browser submits a raw key to the server. Post-removal: surface is gone. |
| browser → /settings | Server-rendered HTML — Jinja2 is the only untrusted-input sink; this plan removes content, does not add any. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Information Disclosure | `settings.html` removed JS | mitigate | Removal eliminates the legacy `setSettingsNamespace` writer to `localStorage.selectedNamespace` for *unrelated* HF/NVIDIA UI; the surviving `loadBlueprints` fallback only *reads* the existing `selectedNamespace` key written by the main page — no new write path is introduced. Acceptance criterion confirms exactly one occurrence of the inline fallback. |
| T-23-01-02 | Tampering | residual references to removed DOM IDs | mitigate | Grep gates in `<acceptance_criteria>` assert that none of the 12 removed DOM IDs and none of the 4 removed function names remain in the file. A residual reference would either silently fail (function call on undefined) or `ReferenceError` at load time; both regress no-regression baseline. |
| T-23-01-03 | Denial of Service | broken JavaScript on settings page | mitigate | UI-SPEC No-Regression Baseline plus the `python -c "from jinja2 ... get_template('settings.html')"` Jinja parse check in `<verify>` catches template syntax errors. Functional smoke (the `refreshAuthStatus`, `loadBlueprints`, Debug toggle) is preserved by retention rules. |
| T-23-01-04 | Repudiation | git history | accept | Removal is a clean delete; git log preserves the prior implementation for forensic reference. No additional logging required. |
| T-23-01-05 | Elevation of Privilege | route surface reduction | accept | Removing two POST endpoints strictly reduces attack surface. No new permissions granted. |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py` exits 0 with the two handlers gone
- `grep -nE "/api/secret/(huggingface|nvidia)"` returns no matches in either modified file
- Template parses via Jinja2 `Environment(...).get_template('settings.html')`
- Manual smoke (covered by acceptance criteria above): starting the server and visiting `/settings` shows four sections in order, console is clean, blueprint list still loads, auth status polls every 10s
</verification>

<success_criteria>
- API-07 satisfied: both deprecated POST endpoints return 404
- UI Removal Contract satisfied: all 8 removal targets gone, `loadBlueprints` rewired with inline localStorage fallback
- `create_or_update_secret` helper retained and available for Plan 02
- Settings page loads and operates with the four surviving sections (no console errors, blueprint list and auth status polling unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-01-SUMMARY.md` summarizing exactly which line ranges were removed from each file and listing the surviving section order in `settings.html`.
</output>
