---
phase: 08-skill-md-agent-context-and-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/webapp/planning/session_service.py
  - app-store-gui/webapp/planning/session_store.py
  - app-store-gui/webapp/planning/family_matcher.py
  - app-store-gui/webapp/planning/compiler.py
  - app-store-gui/webapp/planning/inspection_tools.py
  - app-store-gui/webapp/planning/__init__.py
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/templates/planning_session.html
  - app-store-gui/tests/planning/test_planning_routes.py
  - app-store-gui/tests/planning/test_planning_session_integration.py
  - app-store-gui/tests/planning/test_planning_session_service.py
  - app-store-gui/tests/planning/test_planning_session_store.py
  - app-store-gui/tests/planning/test_compiler.py
  - app-store-gui/tests/test_weka_inspection.py
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "session_service.py, session_store.py, family_matcher.py, and compiler.py are absent from the repo"
    - "inspection_tools.py is absent from the repo"
    - "Planning session routes are fully removed from main.py (no 410 Gone stubs)"
    - "planning_session.html template is absent from the repo"
    - "planning/__init__.py exports only apply_gateway symbols"
    - "inspection/cluster.py, planning/apply_gateway.py, and planning/validator.py remain intact"
    - "All preserved module tests pass (apply_gateway, validator, cluster inspection)"
    - "MCP server test suite still passes after cleanup"
  artifacts:
    - path: "app-store-gui/webapp/planning/__init__.py"
      provides: "Cleaned exports - only apply_gateway symbols"
      contains: "from .apply_gateway import"
    - path: "app-store-gui/webapp/main.py"
      provides: "main.py with all planning session code removed"
  key_links:
    - from: "mcp-server/tools/apply_tool.py"
      to: "app-store-gui/webapp/planning/apply_gateway.py"
      via: "import apply_yaml_content_with_namespace"
      pattern: "from webapp.planning.apply_gateway import"
    - from: "app-store-gui/webapp/planning/validator.py"
      to: "app-store-gui/webapp/planning/models.py"
      via: "from .models import"
      pattern: "from \\.models import"
---

<objective>
Delete all deprecated v1.0 backend-brain files (session_service.py, session_store.py, family_matcher.py, compiler.py, inspection_tools.py), remove planning session routes and template from main.py, clean up __init__.py exports, and verify preserved modules still work.

Purpose: CLEAN-01/02/03 require removing deprecated code while preserving apply_gateway.py, validator.py (and its models.py dependency) as active MCP tool implementations.
Output: Clean codebase with only MCP-relevant planning modules remaining, all tests passing.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skill-md-agent-context-and-cleanup/08-CONTEXT.md
@.planning/phases/08-skill-md-agent-context-and-cleanup/08-RESEARCH.md

<interfaces>
<!-- Critical dependency chain - what must be preserved -->

From mcp-server/tools/apply_tool.py:
```python
from webapp.planning.apply_gateway import (
    apply_yaml_content_with_namespace,
    ApplyGatewayDependencies,
)
```

From app-store-gui/webapp/planning/validator.py line 6:
```python
from .models import (...)  # Hard dependency - models.py MUST stay
```

Target __init__.py after cleanup:
```python
from .apply_gateway import (
    ApplyGateway,
    ApplyGatewayDependencies,
    apply_yaml_content_with_namespace,
    apply_yaml_documents_with_namespace,
    apply_yaml_file_with_namespace,
)

__all__ = [
    "ApplyGateway",
    "ApplyGatewayDependencies",
    "apply_yaml_content_with_namespace",
    "apply_yaml_documents_with_namespace",
    "apply_yaml_file_with_namespace",
]
```

main.py planning removal scope (from RESEARCH.md):
- Lines 25-40: `from webapp.planning import (...)` block
- Line 41: `from webapp.planning.inspection_tools import PlanningInspectionTools`
- Line 195: `PLANNING_APPLY_GATEWAY = ApplyGateway(project_root=PROJECT_ROOT)`
- Line 196: `PLANNING_SESSIONS_DIR = os.path.join(PROJECT_ROOT, ".planning-sessions")`
- Lines ~270-418: 5 helper functions
- Lines 834-1028: 6 route handlers + events endpoint
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated source files and their tests</name>
  <files>
    app-store-gui/webapp/planning/session_service.py,
    app-store-gui/webapp/planning/session_store.py,
    app-store-gui/webapp/planning/family_matcher.py,
    app-store-gui/webapp/planning/compiler.py,
    app-store-gui/webapp/planning/inspection_tools.py,
    app-store-gui/webapp/templates/planning_session.html,
    app-store-gui/tests/planning/test_planning_routes.py,
    app-store-gui/tests/planning/test_planning_session_integration.py,
    app-store-gui/tests/planning/test_planning_session_service.py,
    app-store-gui/tests/planning/test_planning_session_store.py,
    app-store-gui/tests/planning/test_compiler.py,
    app-store-gui/tests/test_weka_inspection.py
  </files>
  <action>
    Delete the following files using `git rm`:

    **Source files to delete (CLEAN-01):**
    - app-store-gui/webapp/planning/session_service.py
    - app-store-gui/webapp/planning/session_store.py
    - app-store-gui/webapp/planning/family_matcher.py
    - app-store-gui/webapp/planning/compiler.py
    - app-store-gui/webapp/planning/inspection_tools.py (confirmed: only used by main.py planning routes and test_weka_inspection.py - both being removed)

    **Template to delete (CLEAN-02):**
    - app-store-gui/webapp/templates/planning_session.html

    **Test files to delete** (test deleted functionality only):
    - app-store-gui/tests/planning/test_planning_routes.py
    - app-store-gui/tests/planning/test_planning_session_integration.py
    - app-store-gui/tests/planning/test_planning_session_service.py
    - app-store-gui/tests/planning/test_planning_session_store.py
    - app-store-gui/tests/planning/test_compiler.py
    - app-store-gui/tests/test_weka_inspection.py (tests PlanningInspectionTools which is deleted)

    **DO NOT delete:**
    - app-store-gui/webapp/planning/models.py (validator.py depends on it)
    - app-store-gui/webapp/planning/apply_gateway.py (MCP server imports it)
    - app-store-gui/webapp/planning/validator.py (used by MCP validate_yaml tool tests)
    - app-store-gui/webapp/planning/__init__.py (still needed, edited in Task 2)
    - app-store-gui/tests/planning/test_apply_gateway.py (tests preserved module)
    - app-store-gui/tests/planning/test_plan_contract.py (tests preserved validator/models)
    - app-store-gui/tests/planning/test_inspection_contract.py (tests preserved inspection)

    Before deleting, verify each file exists. Use `git rm` so git tracks the deletions.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore && git diff --cached --name-only --diff-filter=D | grep -c "\.py\|\.html" | xargs -I{} test {} -ge 10 && echo "OK: 10+ files staged for deletion"</automated>
  </verify>
  <done>All 12 deprecated files (5 source + 1 template + 6 test files) are git-removed. models.py, apply_gateway.py, validator.py, and their tests remain.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up main.py and __init__.py, then verify all preserved tests pass</name>
  <files>
    app-store-gui/webapp/planning/__init__.py,
    app-store-gui/webapp/main.py
  </files>
  <action>
    **__init__.py cleanup:**
    Replace the entire contents of `app-store-gui/webapp/planning/__init__.py` with ONLY the apply_gateway exports:

    ```python
    from .apply_gateway import (
        ApplyGateway,
        ApplyGatewayDependencies,
        apply_yaml_content_with_namespace,
        apply_yaml_documents_with_namespace,
        apply_yaml_file_with_namespace,
    )

    __all__ = [
        "ApplyGateway",
        "ApplyGatewayDependencies",
        "apply_yaml_content_with_namespace",
        "apply_yaml_documents_with_namespace",
        "apply_yaml_file_with_namespace",
    ]
    ```

    Do NOT re-export models or validator — they can be imported directly by consumers that need them.

    **main.py cleanup (CLEAN-02):**
    Read main.py first to confirm line numbers from RESEARCH.md. Then remove ALL planning session code:

    1. Remove the `from webapp.planning import (...)` import block that imports planning session symbols (keep any imports of ApplyGateway if main.py uses it for non-planning purposes — but per CONTEXT.md, the full planning import block goes)
    2. Remove `from webapp.planning.inspection_tools import PlanningInspectionTools`
    3. Remove `PLANNING_APPLY_GATEWAY = ApplyGateway(project_root=PROJECT_ROOT)` and `PLANNING_SESSIONS_DIR = ...`
    4. Remove all helper functions: build_planning_inspection_snapshot, build_fit_findings_from_inspection, build_default_planning_draft, create_planning_session_service, get_planning_session_service, _planning_session_not_found, _planning_session_conflict, _planning_session_context
    5. Remove all planning session route handlers (the @app.get/@app.post routes for /planning/sessions/*)
    6. Remove the planning SSE events endpoint if present

    Be careful to preserve ALL non-planning code in main.py. The app still serves blueprint management, deployment, and other routes.

    **After both edits, verify:**
    1. `cd app-store-gui && PYTHONPATH=. pytest tests/planning/test_apply_gateway.py tests/planning/test_plan_contract.py -x -q` — preserved planning tests pass
    2. `cd mcp-server && PYTHONPATH=.:../app-store-gui pytest tests/ -x -q` — MCP server tests pass (they import from webapp.planning.apply_gateway)
    3. `cd app-store-gui && PYTHONPATH=. python -c "from webapp.planning import ApplyGateway; print('OK')"` — import still works
    4. `cd app-store-gui && PYTHONPATH=. python -c "from webapp.planning.validator import validate_structured_plan; print('OK')"` — validator direct import works
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore/mcp-server && PYTHONPATH=.:../app-store-gui pytest tests/ -x -q && cd ../app-store-gui && PYTHONPATH=. pytest tests/planning/test_apply_gateway.py tests/planning/test_plan_contract.py -x -q</automated>
  </verify>
  <done>__init__.py exports only apply_gateway symbols. main.py has zero planning session code. All MCP server tests pass (67+). Preserved planning tests (apply_gateway, plan_contract) pass. No ImportError from webapp.planning imports.</done>
</task>

</tasks>

<verification>
1. `ls app-store-gui/webapp/planning/session_service.py 2>/dev/null && echo FAIL || echo OK` - file deleted
2. `ls app-store-gui/webapp/planning/session_store.py 2>/dev/null && echo FAIL || echo OK` - file deleted
3. `ls app-store-gui/webapp/planning/family_matcher.py 2>/dev/null && echo FAIL || echo OK` - file deleted
4. `ls app-store-gui/webapp/planning/compiler.py 2>/dev/null && echo FAIL || echo OK` - file deleted
5. `grep -c "planning/sessions" app-store-gui/webapp/main.py` - should be 0
6. `grep -c "PlanningInspectionTools" app-store-gui/webapp/main.py` - should be 0
7. `cd mcp-server && PYTHONPATH=.:../app-store-gui pytest tests/ -x -q` - MCP tests pass
8. `cd app-store-gui && PYTHONPATH=. pytest tests/planning/test_apply_gateway.py -x -q` - preserved tests pass
</verification>

<success_criteria>
- 5 deprecated source files deleted (session_service, session_store, family_matcher, compiler, inspection_tools)
- planning_session.html template deleted
- 6 obsolete test files deleted
- main.py has zero planning session routes, helpers, or imports
- __init__.py exports only apply_gateway symbols
- models.py, apply_gateway.py, validator.py remain intact
- MCP server full test suite passes
- Preserved app-store-gui planning tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-md-agent-context-and-cleanup/08-03-SUMMARY.md`
</output>
