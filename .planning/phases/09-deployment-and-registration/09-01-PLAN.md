---
phase: 09-deployment-and-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-server/Dockerfile
  - mcp-server/.dockerignore
  - mcp-server/config.py
  - mcp-server/tests/test_config.py
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "docker build produces a valid image with no missing dependencies"
    - "Container starts MCP server on stdio transport with python -m server"
    - "BLUEPRINTS_DIR is validated at startup and fails fast with clear error if missing"
    - "All env vars (BLUEPRINTS_DIR, KUBECONFIG, KUBERNETES_AUTH_MODE, LOG_LEVEL, WEKA_ENDPOINT) are configurable"
  artifacts:
    - path: "mcp-server/Dockerfile"
      provides: "Container image definition"
      contains: "python:3.10-slim"
    - path: "mcp-server/.dockerignore"
      provides: "Build context exclusions"
      contains: "__pycache__"
    - path: "mcp-server/config.py"
      provides: "Env var config with startup validation"
      contains: "BLUEPRINTS_DIR"
    - path: "mcp-server/tests/test_config.py"
      provides: "Startup validation tests"
      exports: ["test_blueprints_dir_required"]
  key_links:
    - from: "mcp-server/Dockerfile"
      to: "mcp-server/config.py"
      via: "PYTHONPATH env resolves import"
      pattern: "PYTHONPATH=/app/mcp-server:/app/app-store-gui"
    - from: "mcp-server/Dockerfile"
      to: "mcp-server/server.py"
      via: "CMD python -m server"
      pattern: 'CMD.*python.*-m.*server'
---

<objective>
Dockerfile, .dockerignore, and startup env var validation for the MCP server container image.

Purpose: Package the MCP server as a self-contained container that OpenClaw can spawn via stdio transport, with fail-fast validation of required configuration.
Output: Buildable Dockerfile, .dockerignore, updated config.py with startup validation, and config unit tests.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mcp-server/server.py
@mcp-server/config.py
@mcp-server/requirements.txt
@mcp-server/openclaw.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mcp-server/config.py:
```python
BLUEPRINTS_DIR: str = os.environ.get("BLUEPRINTS_DIR", "/app/manifests/manifest")
KUBERNETES_AUTH_MODE: str = os.environ.get("KUBERNETES_AUTH_MODE", "in-cluster")
LOG_LEVEL: str = os.environ.get("LOG_LEVEL", "INFO")
```

From mcp-server/server.py:
```python
# Entry point: registers all 8 tools, runs on stdio
mcp = FastMCP("weka-app-store-mcp")
# ... register_* calls ...
if __name__ == "__main__":
    mcp.run()
```

From mcp-server/requirements.txt:
```
mcp[cli]>=1.26.0
kubernetes>=27.0.0
PyYAML>=6.0.1
pytest>=8.0.0
```

From mcp-server/tests/conftest.py (PYTHONPATH pattern):
```python
MCP_SERVER_ROOT = Path(__file__).resolve().parents[1]
APP_STORE_GUI_ROOT = MCP_SERVER_ROOT.parent / "app-store-gui"
for path in (str(MCP_SERVER_ROOT), str(APP_STORE_GUI_ROOT)):
    if path not in sys.path:
        sys.path.insert(0, path)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Startup env var validation in config.py and tests</name>
  <files>mcp-server/config.py, mcp-server/tests/test_config.py</files>
  <behavior>
    - Test 1: When BLUEPRINTS_DIR env var is unset and no default path exists, importing config raises SystemExit with message containing "BLUEPRINTS_DIR"
    - Test 2: When BLUEPRINTS_DIR is set via env var, config.BLUEPRINTS_DIR returns that value
    - Test 3: WEKA_ENDPOINT env var is available in config (None if unset, value if set)
    - Test 4: KUBECONFIG env var is available in config (None if unset)
  </behavior>
  <action>
    Update mcp-server/config.py to:
    1. Add `WEKA_ENDPOINT: str | None = os.environ.get("WEKA_ENDPOINT")` (optional, None default — future-proofing per user decision)
    2. Add `KUBECONFIG: str | None = os.environ.get("KUBECONFIG")` (optional, None default — local dev override)
    3. Add a `validate_required()` function that checks BLUEPRINTS_DIR was explicitly set via env var (not just defaulted). If `BLUEPRINTS_DIR` env var is not set, print a FATAL message to stderr and call `sys.exit(1)`. This function is NOT called at import time — it will be called from server.py's `__main__` block so tests can still import config without side effects.

    Per user decision: "Required env vars validated at startup (fail fast); optional vars checked lazily when tools are called."

    Create mcp-server/tests/test_config.py with tests for the above behaviors. Use `monkeypatch.delenv` / `monkeypatch.setenv` to control env vars. To test validate_required(), call it directly and assert SystemExit is raised when BLUEPRINTS_DIR is missing.

    IMPORTANT: Do NOT make config.py exit on import — that would break all existing tests. The validate_required() function is called explicitly from server.py's __main__ block only.
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore && PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest mcp-server/tests/test_config.py -v</automated>
  </verify>
  <done>config.py has WEKA_ENDPOINT, KUBECONFIG, and validate_required(). Tests prove fail-fast when BLUEPRINTS_DIR is missing and pass-through when set. Existing test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile and .dockerignore</name>
  <files>mcp-server/Dockerfile, mcp-server/.dockerignore</files>
  <action>
    Create mcp-server/Dockerfile per locked decisions:
    - Base: `python:3.10-slim`
    - `ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1 PIP_NO_CACHE_DIR=1`
    - `WORKDIR /app`
    - Copy `mcp-server/requirements.txt` first, `pip install` (cache layer optimization)
    - Copy `mcp-server/` to `/app/mcp-server/`
    - Copy `app-store-gui/webapp/` to `/app/app-store-gui/webapp/`
    - `ENV PYTHONPATH=/app/mcp-server:/app/app-store-gui`
    - `WORKDIR /app/mcp-server`
    - `RUN python -c "import server"` (import sanity check per user decision)
    - `RUN useradd -u 10001 -m mcpuser` then `USER mcpuser`
    - `CMD ["python", "-m", "server"]`

    IMPORTANT: This Dockerfile must be built from the repo root: `docker build -f mcp-server/Dockerfile .`
    Do NOT set BLUEPRINTS_DIR as ENV in the Dockerfile — it must be provided at runtime.

    Create mcp-server/.dockerignore to exclude:
    - `__pycache__`, `*.pyc`, `.pytest_cache`
    - `tests/` (not needed in production image, but since .dockerignore is relative to build context and we build from repo root, use paths like `**/__pycache__`, `**/.pytest_cache`, `.git`, `.planning`, `docker/`, `.github/`)
    - `.env`, `.env.*`
    - `.git`, `.planning`

    Also update server.py's `__main__` block to call `validate_required()` before `mcp.run()`:
    ```python
    if __name__ == "__main__":
        from config import validate_required
        validate_required()
        mcp.run()
    ```
  </action>
  <verify>
    <automated>cd /Users/christopherjenkins/git/wekaappstore && PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest mcp-server/tests/ -q</automated>
  </verify>
  <done>Dockerfile builds with `docker build -f mcp-server/Dockerfile .` (manual verify). .dockerignore excludes build artifacts. server.py calls validate_required() at startup. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=mcp-server:app-store-gui BLUEPRINTS_DIR=mcp-server/tests/fixtures/sample_blueprints pytest mcp-server/tests/ -v` — all tests pass including new config tests
2. `docker build -f mcp-server/Dockerfile . -t mcp-test` — builds without error (requires Docker)
3. `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' | docker run --rm -i -e BLUEPRINTS_DIR=/tmp mcp-test` — returns tool list (requires Docker)
</verification>

<success_criteria>
- Dockerfile exists at mcp-server/Dockerfile using python:3.10-slim
- .dockerignore excludes build artifacts
- config.py validates BLUEPRINTS_DIR at startup via validate_required()
- config.py includes WEKA_ENDPOINT and KUBECONFIG optional vars
- server.py calls validate_required() in __main__
- All existing tests pass
- New config tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-deployment-and-registration/09-01-SUMMARY.md`
</output>
