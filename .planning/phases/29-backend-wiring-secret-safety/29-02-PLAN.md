---
phase: 29-backend-wiring-secret-safety
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/tests/test_dynamic_blueprint.py
autonomous: true
requirements: [PROG-02]
must_haves:
  truths:
    - "The server builds quay_dockerconfigjson from posted quay creds such that auths[\"quay.io\"][\"auth\"] base64-decodes to exactly user:pass with no trailing bytes"
    - "split_endpoints produces both the joinIpPorts YAML-list form and the comma-joined endpoints form, both rendering to valid YAML and verified by unit tests"
  artifacts:
    - path: "app-store-gui/webapp/main.py"
      provides: "build_quay_dockerconfigjson + split_endpoints pure helpers, merged into user_vars before render"
      contains: "def build_quay_dockerconfigjson"
  key_links:
    - from: "deploy_stream user_vars"
      to: "build_quay_dockerconfigjson / split_endpoints"
      via: "user_vars.update(...) before template.render(**user_vars)"
      pattern: "user_vars\\.update"
    - from: "rendered blueprint joinIpPorts"
      to: "join_ip_ports_list"
      via: "Jinja2 [[ join_ip_ports_list ]] -> double-quoted JSON array"
      pattern: "join_ip_ports_list"
---

<objective>
Add server-side variable derivation: `build_quay_dockerconfigjson(user, password)` and `split_endpoints(join_ip_ports)`, merge their outputs into `user_vars` BEFORE the Jinja2 render pass, and prove byte-exact encoding plus dual-form endpoint output with unit tests. Closes ROADMAP Phase 29 SC2.

Purpose: The blueprint references `[[ quay_dockerconfigjson ]]`, `[[ join_ip_ports_list ]]`, and `[[ endpoints_csv ]]` — none of which are in the blueprint x-variables. They must be derived server-side from posted creds/endpoints before render (Phase 27 D-06 contract). The quay `auth` byte-exactness is load-bearing for the operator's `helm --registry-config` OCI pull (Phase 28 D-02).
Output: two pure module-level helpers, a one-line `user_vars.update(...)` merge before render, and unit tests asserting `base64.b64decode(auth) == b"user:pass"` and both endpoint forms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-backend-wiring-secret-safety/29-CONTEXT.md
@.planning/phases/27-install-blueprint-authoring/27-CONTEXT.md
@.planning/phases/28-operator-helm-auth-crd-discovery/28-CONTEXT.md

<interfaces>
<!-- Blueprint token sites the derived vars feed (cluster_init/app-store-install.yaml). -->
- .dockerconfigjson stringData (~60, ~75):   `'[[ quay_dockerconfigjson ]]'`  (quay pull secret, two namespaces)
- WekaClient joinIpPorts (~291):             `joinIpPorts: [[ join_ip_ports_list ]]`  (YAML flow-sequence)
- CSI api-secret endpoints (~256):           `endpoints: "[[ endpoints_csv ]]"`  (comma-joined string)

<!-- Posted creds the helpers consume. The wizard form (Phase 30) posts quay_username + a quay password field and join_ip_ports; for THIS phase the derivation must read whatever user_vars keys carry them. Per the blueprint x-variables, weka creds use weka_username/weka_password; the quay creds arrive as quay_username + quay_password (the raw password, distinct from the derived quay_dockerconfigjson). Confirm the posted key names against the variables dict in deploy_stream; do NOT invent new x-variables. -->

<!-- RESOLVED render question (D-05 open item): a Python list rendered via Jinja2 [[ ]] uses repr() (single quotes) which IS valid YAML for simple host:port entries, BUT the deterministic, content-robust choice is to render join_ip_ports_list as a json.dumps(list) STRING so the template emits an explicit double-quoted JSON array. Verified: `joinIpPorts: ["192.168.1.1:14000","192.168.1.2:14000"]` parses via yaml.safe_load. Use the JSON-string form. -->

deploy_stream merge point: `rendered = template.render(**user_vars)` at ~2920. Merge derived vars into user_vars on the line(s) immediately before this (after namespace extraction, before render). parse_x_variables (~1694) is the helper-placement neighborhood.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add build_quay_dockerconfigjson and split_endpoints helpers</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (parse_x_variables ~1694 neighborhood; confirm base64 and json are already imported at top of file)
    - cluster_init/app-store-install.yaml (~60, ~256, ~291 token sites)
    - .planning/phases/29-backend-wiring-secret-safety/29-CONTEXT.md (D-03, D-04, D-05)
    - .planning/phases/28-operator-helm-auth-crd-discovery/28-CONTEXT.md (D-02 — consumed dockerconfigjson shape)
  </read_first>
  <behavior>
    - build_quay_dockerconfigjson(user, password): result is a JSON string; `json.loads(result)["auths"]["quay.io"]["auth"]` base64-decodes to EXACTLY `f"{user}:{password}".encode()` with NO trailing newline or extra bytes. Encoding uses `base64.b64encode(...).decode("ascii")` — never base64.encodebytes/encodestring. Wrapped via `json.dumps({"auths": {"quay.io": {"auth": <b64>}}}, separators=(",", ":"))`.
    - split_endpoints(join_ip_ports): given `"192.168.1.1:14000"` returns `{"join_ip_ports_list": "[\"192.168.1.1:14000\"]", "endpoints_csv": "192.168.1.1:14000"}` (list form is a json.dumps STRING). Given `"a:1, b:2 , c:3"` (surrounding whitespace, multiple) trims each entry, drops empties, and produces both forms with three entries. join_ip_ports_list rendered into `joinIpPorts: [[ join_ip_ports_list ]]` parses as a valid YAML list.
  </behavior>
  <action>
    Add two pure module-level functions near parse_x_variables (~1694) using double quotes. `build_quay_dockerconfigjson(user, password)` encodes per D-04 exactly: `base64.b64encode(f"{user}:{password}".encode()).decode("ascii")`, wrapped as `json.dumps({"auths": {"quay.io": {"auth": <b64>}}}, separators=(",", ":"))`. `split_endpoints(join_ip_ports)` splits the comma-delimited string, strips whitespace per entry, drops empty entries, and returns a dict `{"join_ip_ports_list": json.dumps(entries), "endpoints_csv": ",".join(entries)}` — the list form is a json.dumps STRING (resolved D-05: explicit double-quoted JSON array renders valid YAML and is content-robust). Do NOT add these to the blueprint x-variables. Confirm `import base64` and `import json` exist at the top of main.py (add base64 if missing).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'app-store-gui'); import webapp.main as m; import base64,json; a=json.loads(m.build_quay_dockerconfigjson('u','p'))['auths']['quay.io']['auth']; assert base64.b64decode(a)==b'u:p', base64.b64decode(a)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def build_quay_dockerconfigjson' app-store-gui/webapp/main.py` exits 0
    - `grep -q 'def split_endpoints' app-store-gui/webapp/main.py` exits 0
    - `grep -c 'encodebytes\|encodestring' app-store-gui/webapp/main.py` returns 0
    - the inline python verify command above exits 0 (auth decodes to exactly `b"u:p"`)
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0
  </acceptance_criteria>
  <done>Both helpers exist, encode byte-exactly with b64encode (no encodebytes), and split_endpoints returns both forms with whitespace-trimmed, empty-dropped entries.</done>
</task>

<task type="auto">
  <name>Task 2: Merge derived vars into user_vars before the Jinja2 render</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (deploy_stream ~2857 user_vars parse; ~2871 namespace extraction; ~2920 template.render)
    - .planning/phases/29-backend-wiring-secret-safety/29-CONTEXT.md (D-03)
  </read_first>
  <action>
    In deploy_stream, immediately before `rendered = template.render(**user_vars)` (~2920), derive and merge the server-side vars into user_vars: build quay_dockerconfigjson from the posted quay username + quay password keys present in user_vars and assign `user_vars["quay_dockerconfigjson"] = build_quay_dockerconfigjson(<user>, <password>)`; and `user_vars.update(split_endpoints(user_vars.get("join_ip_ports", "")))`. Read the actual posted key names for the quay creds from the variables dict (confirm against the live deploy_stream variables JSON; the wizard posts quay_username + a quay password field) — do NOT add them to the blueprint x-variables. Guard against missing keys so a blueprint that does not use these vars (e.g. cluster-init or other blueprints) is unaffected: only derive quay_dockerconfigjson when the quay cred keys are present, and only update endpoints when join_ip_ports is present.
  </action>
  <verify>
    <automated>grep -c 'user_vars.update(split_endpoints' app-store-gui/webapp/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'build_quay_dockerconfigjson(' app-store-gui/webapp/main.py` shows a call site inside deploy_stream (not only the def)
    - `grep -q 'split_endpoints(' app-store-gui/webapp/main.py` shows a call site inside deploy_stream
    - the merge appears BEFORE `template.render` — `awk` / manual confirm the call-site line numbers are less than the render line number
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0
  </acceptance_criteria>
  <done>Derived vars are merged into user_vars before render; derivation is guarded so blueprints without quay/endpoint vars are unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for derivation correctness and render validity</name>
  <files>app-store-gui/tests/test_dynamic_blueprint.py</files>
  <read_first>
    - app-store-gui/tests/test_dynamic_blueprint.py (import style, existing helpers)
    - app-store-gui/tests/conftest.py
    - .planning/phases/29-backend-wiring-secret-safety/29-CONTEXT.md (D-06)
  </read_first>
  <behavior>
    - Test: `base64.b64decode(json.loads(main.build_quay_dockerconfigjson("alice","s3cr3t"))["auths"]["quay.io"]["auth"]) == b"alice:s3cr3t"` (byte-exact, no trailing newline). Assert the decoded bytes do NOT end with `b"\n"`.
    - Test: split_endpoints single endpoint -> endpoints_csv == "h:1" and json.loads(join_ip_ports_list) == ["h:1"].
    - Test: split_endpoints multiple with surrounding whitespace ("a:1, b:2 , c:3") -> endpoints_csv == "a:1,b:2,c:3" and json.loads(list) == ["a:1","b:2","c:3"]; empty entries (e.g. trailing comma) dropped.
    - Test: rendering `joinIpPorts: [[ join_ip_ports_list ]]` with the derived list-string via the same `[[ ]]` Jinja2 Environment that deploy_stream uses (variable_start_string="[[", variable_end_string="]]") yaml.safe_loads to `{"joinIpPorts": ["a:1","b:2"]}` — proves valid YAML flow-sequence.
  </behavior>
  <action>
    Add tests to test_dynamic_blueprint.py using `import webapp.main as main`, `import base64`, `import json`, `import yaml`, and `from jinja2 import Environment`. For the render-validity test, construct the same Environment delimiters deploy_stream uses and render the joinIpPorts line with the split_endpoints output, then yaml.safe_load the result and assert the list round-trips.
  </action>
  <verify>
    <automated>PYTHONPATH=mcp-server:app-store-gui pytest app-store-gui/tests/test_dynamic_blueprint.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=mcp-server:app-store-gui pytest app-store-gui/tests/test_dynamic_blueprint.py -v` exits 0
    - A named test base64-decodes the quay auth and asserts equality to exactly `b"alice:s3cr3t"` AND asserts no trailing `b"\n"`
    - A named test renders `joinIpPorts: [[ join_ip_ports_list ]]` and yaml.safe_loads it to a real list
  </acceptance_criteria>
  <done>Tests prove byte-exact quay encoding, both endpoint forms, and valid-YAML render of the join_ip_ports_list flow-sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /deploy-stream | posted quay/weka creds + join_ip_ports cross here |
| GUI → operator helm --registry-config | derived quay_dockerconfigjson crosses here for the OCI pull |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering | build_quay_dockerconfigjson encoding | mitigate | byte-exact `base64.b64encode(...).decode("ascii")` (never encodebytes) — a wrong/newline-padded auth would silently break the OCI pull; the unit test base64-decodes and asserts exactly `b"user:pass"` with no trailing bytes (Task 1, Task 3) |
| T-29-05 | Tampering | split_endpoints / join_ip_ports_list render | mitigate | json.dumps list-string renders an explicit double-quoted YAML flow-sequence verified to parse, so malformed joinIpPorts cannot corrupt the WekaClient CR YAML (Task 1, Task 3) |
| T-29-06 | Information Disclosure | derived quay_dockerconfigjson in user_vars | transfer | the derived secret now lives in user_vars and would leak via the annotation/SSE without redaction — mitigation is owned by plan 29-03 (D-09/D-10); this plan only creates the value |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py` exits 0
- `PYTHONPATH=mcp-server:app-store-gui pytest app-store-gui/tests/ -v` exits 0
- `grep -c 'encodebytes\|encodestring' app-store-gui/webapp/main.py` returns 0
</verification>

<success_criteria>
SC2 satisfied: server derives quay_dockerconfigjson (byte-exact) and both endpoint forms, merged before render, all proven by unit tests.
</success_criteria>

<output>
Create `.planning/phases/29-backend-wiring-secret-safety/29-02-SUMMARY.md` when done.
</output>
