---
phase: 27-install-blueprint-authoring
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - operator_module/tests/test_install_blueprint.py
autonomous: true
requirements: [INST-01, INST-02, INST-03, INST-05, INST-08, INST-09]
user_setup: []

must_haves:
  truths:
    - "A pytest renders app-store-install.yaml through the same [[ var ]] Jinja2 path the GUI uses and the output parses as valid multi-doc YAML"
    - "The rendered components feed the operator's real resolve_dependencies and produce the exact topo order from D-01 / ROADMAP SC1"
    - "The quay dockerconfigjson round-trips with no trailing-newline corruption"
    - "Exactly one StorageClass carries is-default-class true"
  artifacts:
    - path: "operator_module/tests/test_install_blueprint.py"
      provides: "Cluster-free verification of the install blueprint contract"
      contains: "resolve_dependencies"
      min_lines: 60
  key_links:
    - from: "test_install_blueprint.py"
      to: "operator_module.main.resolve_dependencies"
      via: "import + call on rendered components"
      pattern: "resolve_dependencies"
    - from: "test_install_blueprint.py"
      to: "jinja2 Environment([[ ]])"
      via: "render with sample variables"
      pattern: "variable_start_string"
---

<objective>
Author a cluster-free pytest that proves the Phase 27 success criteria against `cluster_init/app-store-install.yaml`: it renders through the GUI's exact `[[ var ]]` Jinja2 delimiters, parses as multi-doc YAML, feeds the components into the operator's REAL `resolve_dependencies` and asserts the D-01 topo order, round-trips the quay dockerconfigjson encoding, and asserts exactly one default StorageClass.

Purpose: Make the blueprint contract machine-verifiable so Phases 28-31 build on a proven foundation. No live cluster needed.
Output: One new test file. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-install-blueprint-authoring/27-CONTEXT.md
@cluster_init/app-store-install.yaml
@CLAUDE.md

<interfaces>
<!-- Exact functions/config to reuse. Do NOT reimplement topo-sort. -->

operator_module/main.py:824 resolve_dependencies(components: List[Dict]) -> List[Dict]
  Kahn's topo-sort. Reads comp['name'] and comp.get('dependsOn', []). Returns components in deploy order.
  Raises ValueError on unknown dep or cycle. Import via `from main import resolve_dependencies` (conftest.py adds operator_module to sys.path).

GUI render delimiters (app-store-gui/webapp/main.py:2918):
  Environment(variable_start_string="[[", variable_end_string="]]"); template.render(**user_vars).
  Reproduce this exact construction in the test (jinja2 is already a dependency).

Test idiom (from CLAUDE.md "Operator tests"): PYTHONPATH=operator_module pytest operator_module/tests/ -v
  conftest.py at operator_module/tests/conftest.py inserts operator_module/ on sys.path so `from main import ...` works.

PROJECT_ROOT: the repo root. Resolve the blueprint path relative to the test file (parents[2] / "cluster_init" / "app-store-install.yaml").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render + topo-sort + secret-encoding + default-StorageClass assertions</name>
  <read_first>
    - operator_module/tests/conftest.py (sys.path setup so `from main import resolve_dependencies` works)
    - operator_module/tests/test_appstack.py (existing test style: imports, fixtures, assertion idiom)
    - operator_module/main.py lines 824-862 (resolve_dependencies signature and behavior)
    - app-store-gui/webapp/main.py lines 2916-2923 (exact Jinja2 Environment delimiters and render call to reproduce)
    - cluster_init/app-store-install.yaml (the artifact under test, produced by Plan 27-01)
  </read_first>
  <behavior>
    - Test render_parses: render the blueprint with a SAMPLE_VARS dict supplying ALL [[ ]] tokens (the 8 x-variables keys PLUS the Phase-29 server-injected tokens join_ip_ports_list as a YAML-array string e.g. '["10.0.0.1:14000"]' and endpoints_csv as '10.0.0.1:14000,10.0.0.2:14000', and quay_dockerconfigjson as a realistic base64 dockerconfigjson string). Assert yaml.safe_load_all(rendered) yields docs with no exception, including a WekaAppStore doc.
    - Test topo_order: extract spec.appStack.components from the rendered WekaAppStore, call the REAL resolve_dependencies, and assert ordering constraints from D-01: both quay-secret-* before weka-operator; weka-operator before weka-node-label-sa, weka-client-secret, csi-wekafs; weka-node-label-sa before -rbac before -job; csi-wekafs before csi-api-secret; weka-operator and weka-client-secret both before weka-client; csi-api-secret before storageclass-demote-job before storageclasses. Use index-of comparisons on the returned name list (avoid asserting one rigid total order, since parallel groups are interchangeable).
    - Test quay_roundtrip: build SAMPLE quay_dockerconfigjson the way Phase 29 will (json with auths['quay.io']['auth'] = base64(user:pass)), inject it, render, find the operator-ns quay Secret stringData['.dockerconfigjson'], json.loads it, base64-decode the auth, and assert it decodes to exactly 'user:pass' with NO trailing newline byte (INST-02).
    - Test single_default_sc: parse the storageclasses component's kubernetesManifest (multi-doc), assert exactly one StorageClass has annotation storageclass.kubernetes.io/is-default-class == "true" and it is storageclass-wekafs-dir-api (SC4, INST-08).
    - Test stringData_only: assert no Opaque/dockerconfigjson Secret in the rendered output uses a `data:` field (all use stringData) (INST-09).
  </behavior>
  <action>
Create operator_module/tests/test_install_blueprint.py. Import resolve_dependencies from main (relying on conftest sys.path). Build a module-level SAMPLE_VARS covering every [[ ]] token in the blueprint — the 8 x-variables keys and the two Phase-29 server-injected tokens (join_ip_ports_list, endpoints_csv) plus quay_dockerconfigjson. Reproduce the GUI render exactly with jinja2 Environment(variable_start_string="[[", variable_end_string="]]"). Resolve the blueprint path from the test file location. Implement the five tests described in <behavior>. For topo assertions, build a name->index map from the resolve_dependencies result and assert index(a) < index(b) for each required edge rather than a single hard-coded sequence. Match the existing test style in test_appstack.py (plain pytest functions, no class needed). Do not add new dependencies — jinja2, yaml, base64, json are already available.
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_install_blueprint.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_install_blueprint.py -v` passes all five tests
    - test file imports resolve_dependencies from main (real function, not reimplemented)
    - test reproduces Environment(variable_start_string="[[", variable_end_string="]]")
    - topo test asserts weka-operator index < weka-client index and storageclass-demote-job index < storageclasses index
    - quay roundtrip asserts decoded auth == 'user:pass' with no trailing newline
    - single_default_sc asserts exactly one is-default-class "true" StorageClass named storageclass-wekafs-dir-api
  </acceptance_criteria>
  <done>All five tests pass with no live cluster, using the operator's real resolve_dependencies and the GUI's exact render delimiters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test sample data -> assertions | The test injects synthetic credentials; none are real or committed beyond throwaway sample values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Information Disclosure | test_install_blueprint.py SAMPLE_VARS | accept | Sample credentials are obvious throwaway literals (e.g. user:pass) committed in a test; no production secret material is present. |
| T-27-07 | Tampering | reused resolve_dependencies | mitigate | Test calls the operator's real topo-sort rather than a copy, so the verified order matches runtime behavior exactly. |
</threat_model>

<verification>
Run `PYTHONPATH=operator_module pytest operator_module/tests/test_install_blueprint.py -v` — all five tests green proves SC1 (render + topo order), SC2 (quay roundtrip + stringData), and SC4 (single default StorageClass) without a cluster.
</verification>

<success_criteria>
- Blueprint renders via the GUI's [[ ]] path and parses as valid multi-doc YAML with sample vars
- Real resolve_dependencies produces the D-01 topo order (verified via edge index assertions)
- quay dockerconfigjson round-trips to exactly user:pass with no trailing newline
- Exactly one default StorageClass; all wizard secrets use stringData
</success_criteria>

<output>
Create `.planning/phases/27-install-blueprint-authoring/27-02-SUMMARY.md` when done.
</output>
