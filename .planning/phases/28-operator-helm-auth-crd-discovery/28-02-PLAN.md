---
phase: 28-operator-helm-auth-crd-discovery
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - operator_module/tests/test_operator_helm_auth.py
autonomous: true
requirements: [OPA-01, OPA-02]
must_haves:
  truths:
    - "A unit test proves a failed helm show crds (CalledProcessError) does NOT populate _chart_crds_cache and the next call re-invokes subprocess"
    - "A unit test proves a successful helm show crds caches its result keyed by (chart_ref, version, registry_config_path)"
    - "A unit test proves --registry-config <path> appears in the helm argv for an oci://quay.io/... chart when quay_dockerconfigjson is in stack_vars"
    - "A unit test proves --registry-config does NOT appear when quay_dockerconfigjson is absent or the repo is non-OCI"
    - "A unit test proves the quay docker auth string never appears in any captured subprocess argv"
    - "A unit test proves the temp registry-config file is removed after handle_appstack_deployment returns (including the install-failure path)"
  artifacts:
    - path: "operator_module/tests/test_operator_helm_auth.py"
      provides: "OPA-01/OPA-02 unit tests: cache-failure non-memoization, cache-on-success, registry-config argv presence/absence, credential-not-in-argv, temp-file cleanup"
      contains: "_chart_crds_cache"
  key_links:
    - from: "test_operator_helm_auth.py"
      to: "main.discover_chart_crds / main.handle_appstack_deployment"
      via: "patch('main.subprocess.check_output') / patch('main.subprocess.run') capturing argv"
      pattern: "patch\\(\"main\\.subprocess"
---

<objective>
Add a cluster-free pytest module that locks the Phase 28 behaviors implemented in 28-01: the success-only CRD cache, the registry-config argv threading, the credential-never-in-argv guarantee, and the temp-file cleanup. All tests mock subprocess and require no live cluster.

Purpose: Make the OPA-01 / OPA-02 invariants regression-proof and provide the automated evidence for Success Criteria 2 and 3.
Output: `operator_module/tests/test_operator_helm_auth.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-operator-helm-auth-crd-discovery/28-CONTEXT.md
@.planning/phases/28-operator-helm-auth-crd-discovery/28-01-SUMMARY.md

<interfaces>
<!-- Test conventions to mirror. Read the named files; do not invent fixtures. -->

Existing mocking pattern (operator_module/tests/test_appstack.py, test_helm_non_wiring.py):
- `from main import handle_appstack_deployment, discover_chart_crds, should_skip_crds_for_component` (conftest.py adds operator_module/ to sys.path).
- `with patch("main.subprocess.run", side_effect=mock_run), patch("main._load_kube_config_once", return_value=False), patch("main.HelmOperator"): ...` is the established harness for handle_appstack_deployment.
- For discover_chart_crds: patch `main.subprocess.check_output` (that is the call it makes).
- handle_appstack_deployment receives `body`, `spec`, `name`, `namespace`, and `**kwargs` (e.g. `status={}`, optional `patch`).

Targets under test (from 28-01):
- `main._chart_crds_cache` — module-level dict; clear it (`main._chart_crds_cache.clear()`) at the start of cache tests to isolate.
- `main.discover_chart_crds(chart_ref, version=None, registry_config_path=None)`.
- `main.handle_appstack_deployment(...)` — writes temp registry-config when an OCI component has `quay_dockerconfigjson` in `appStack.variables`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Test the success-only CRD cache (OPA-02)</name>
  <files>operator_module/tests/test_operator_helm_auth.py</files>
  <read_first>
    - operator_module/main.py — current discover_chart_crds and _chart_crds_cache (post 28-01).
    - operator_module/tests/test_appstack.py — header docstring style, imports (`from __future__ import annotations`, `from unittest.mock import patch`), `from main import ...` form.
    - operator_module/tests/conftest.py — sys.path setup (no fixture needed).
    - .planning/phases/28-operator-helm-auth-crd-discovery/28-CONTEXT.md — D-06, D-07.
  </read_first>
  <action>
    Create the test module with the established header docstring + imports. Add `test_failed_helm_show_crds_is_not_cached`: clear `main._chart_crds_cache`; patch `main.subprocess.check_output` with `side_effect=subprocess.CalledProcessError(1, 'helm')`; call `discover_chart_crds("oci://quay.io/weka.io/helm/weka-operator", "v1.13.0")` and assert it returns `set()` AND the cache key `("oci://quay.io/weka.io/helm/weka-operator", "v1.13.0", None)` is NOT in `main._chart_crds_cache`; then assert a second call re-invokes check_output (assert `check_output.call_count == 2`). Add `test_successful_helm_show_crds_is_cached`: clear the cache; patch `check_output` to return a one-document CRD yaml string (kind `CustomResourceDefinition`, metadata.name `wekaclients.weka.weka.io`); call `discover_chart_crds(...)` and assert the returned set contains that name and the `(chart_ref, version, None)` key is present in `_chart_crds_cache`; call again and assert `check_output.call_count == 1` (cache hit, no re-invoke). Add `test_registry_config_path_in_cache_key`: clear the cache; assert that calling with `registry_config_path="/tmp/x.json"` produces a different cache key than `registry_config_path=None` (two distinct keys after two successful calls). Use the same module-level `subprocess` import alias style as test_appstack.py.
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_operator_helm_auth.py -k "cache or registry_config_path" -v</automated>
  </verify>
  <done>
    - Test asserts a failed `helm show crds` does NOT populate `_chart_crds_cache` and the next call re-invokes subprocess.
    - Test asserts a successful result is cached and a second identical call does not re-invoke subprocess.
    - Test asserts `registry_config_path` participates in the cache key.
    - The named tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test registry-config argv threading, credential secrecy, and temp-file cleanup (OPA-01)</name>
  <files>operator_module/tests/test_operator_helm_auth.py</files>
  <read_first>
    - operator_module/main.py — current handle_appstack_deployment helmChart block (post 28-01): the OCI guard, temp-file write, and try/finally cleanup.
    - operator_module/tests/test_appstack.py — the `mock_run` / `patch("main.subprocess.run", side_effect=...)` + `patch("main.HelmOperator")` harness used for handle_appstack_deployment.
    - .planning/phases/28-operator-helm-auth-crd-discovery/28-CONTEXT.md — D-01, D-02, D-04, D-05; security_threat_model requirement (password never in argv/logs).
  </read_first>
  <action>
    Add `test_registry_config_flag_present_for_oci_quay_chart`: build an AppStack spec with one helmChart component whose `repository` is `oci://quay.io/weka.io/helm` and `name` `weka-operator`, and `appStack.variables` containing `quay_dockerconfigjson` set to a sentinel JSON string (e.g. `{"auths":{"quay.io":{"auth":"U0VOVElORUw="}}}`, where the inner auth is a recognizable sentinel). Patch `main.subprocess.run`/`main.subprocess.check_output` with a side_effect that records every argv into a captured list and returns success (rc 0, empty crds output). Patch `main._load_kube_config_once` to False and `main.wait_for_component_ready` to True. Do NOT patch `main.HelmOperator` here (the real `install_or_upgrade` must build the argv); instead patch the subprocess layer so the real methods run against the mock. Invoke `handle_appstack_deployment(...)`. Assert that across all captured helm argv there exists at least one argv containing both `--registry-config` and a path immediately following it, AND that the file written at that path (capture it during the side_effect, or assert the flag/value pairing) corresponded to the sentinel content. Assert the sentinel auth string (`U0VOVElORUw=`) and the full `quay_dockerconfigjson` value appear in NONE of the captured argv elements (Success Criterion 2 / T-28-01).

    Add `test_no_registry_config_flag_without_quay_credential`: same spec but with `appStack.variables` having NO `quay_dockerconfigjson`; assert `--registry-config` appears in NO captured argv (D-05 backward-compat).

    Add `test_temp_registry_config_file_removed_after_return`: capture the registry-config path during the subprocess side_effect (read the argv following `--registry-config`); after `handle_appstack_deployment` returns, assert `os.path.exists(captured_path)` is False (D-04 cleanup). Add a variant where the install side_effect raises / returns failure and assert the file is STILL removed (try/finally exception-safe cleanup, T-28-03).
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_operator_helm_auth.py -v</automated>
  </verify>
  <done>
    - Test asserts `--registry-config <path>` appears in the helm argv for an `oci://quay.io/...` chart when `quay_dockerconfigjson` is in stack_vars.
    - Test asserts `--registry-config` does NOT appear when `quay_dockerconfigjson` is absent.
    - Test asserts the quay docker auth sentinel never appears in any captured subprocess argv.
    - Test asserts the temp registry-config file is removed after return, including on the install-failure path.
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_operator_helm_auth.py -v` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → operator subprocess layer | Tests mock `main.subprocess.run`/`check_output` to capture the exact argv the operator would hand to helm — the observation point for the credential-secrecy and registry-config-threading guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | helm subprocess argv | mitigate | Task 2 captures every helm argv and asserts the quay auth sentinel appears in none of them — automated evidence for Success Criterion 2. |
| T-28-03 | Information Disclosure (residual credential) | temp registry-config file lifecycle | mitigate | Task 2 asserts `os.path.exists(captured_path)` is False after return, including the install-failure path — verifies the `try/finally` cleanup (D-04). |
| T-28-05 | Denial of Service (stale negative cache) | discover_chart_crds empty-failure memoization | mitigate | Task 1 asserts a failed `helm show crds` is not cached and the next call re-invokes subprocess — automated evidence for Success Criterion 3. |
| T-28-02 | Information Disclosure | operator logs | accept | Log-content secrecy is enforced by reviewer inspection in 28-01 (no log statement prints the credential); a unit test cannot fully assert absence across all log sinks. The argv-secrecy test (T-28-01) covers the primary leak vector. |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No installs; test uses stdlib `unittest.mock`/`subprocess`/`os` and the existing pytest dependency. |
</threat_model>

<verification>
- `PYTHONPATH=operator_module pytest operator_module/tests/test_operator_helm_auth.py -v` exits 0.
- `PYTHONPATH=operator_module pytest operator_module/tests/ -v` exits 0 (full suite green — new file does not regress existing tests).
</verification>

<success_criteria>
- Cache failure non-memoization, cache-on-success, and registry_config_path cache-key participation all proven.
- registry-config argv presence (OCI+credential) and absence (no credential / non-OCI) both proven.
- Credential never in argv proven via sentinel.
- Temp-file cleanup proven on both success and failure paths.
</success_criteria>

<output>
Create `.planning/phases/28-operator-helm-auth-crd-discovery/28-02-SUMMARY.md` when done.
</output>
