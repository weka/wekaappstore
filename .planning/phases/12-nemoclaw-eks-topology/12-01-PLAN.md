---
phase: 12-nemoclaw-eks-topology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/agent-sandbox/openclaw-sandbox.yaml
  - k8s/agent-sandbox/openclaw-secrets.yaml
  - scripts/install-agent-sandbox.sh
  - scripts/validate-topology.sh
autonomous: true
requirements: [NCLAW-01, NCLAW-03]

must_haves:
  truths:
    - "Sandbox CR manifest exists with correct OpenClaw image, GPU resources, and security context"
    - "Operator install script installs agent-sandbox v0.2.1 and waits for controller Ready"
    - "Smoke test script validates pod Running, GPU allocated, gateway responsive, and GPU node healthy"
  artifacts:
    - path: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      provides: "Sandbox CR manifest for OpenClaw deployment"
      contains: "agents.x-k8s.io/v1alpha1"
    - path: "k8s/agent-sandbox/openclaw-secrets.yaml"
      provides: "Secret templates for OpenClaw gateway token and NVIDIA API key"
      contains: "kind: Secret"
    - path: "scripts/install-agent-sandbox.sh"
      provides: "Operator installation and verification script"
      contains: "agent-sandbox"
    - path: "scripts/validate-topology.sh"
      provides: "End-to-end topology smoke test"
      contains: "Topology Validation"
  key_links:
    - from: "scripts/validate-topology.sh"
      to: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      via: "validates the pod created by the Sandbox CR"
      pattern: "openclaw-sandbox"
    - from: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      to: "k8s/agent-sandbox/openclaw-secrets.yaml"
      via: "secretKeyRef in env vars"
      pattern: "secretKeyRef"
---

<objective>
Write all Kubernetes manifests and shell scripts needed to deploy OpenClaw via the agent-sandbox CRD on EKS and validate the topology.

Purpose: Produce versioned, reusable artifacts before touching the live cluster. Plan 02 will apply these to EKS and document the resulting topology.
Output: Sandbox CR YAML, Secret templates, operator install script, topology smoke test script.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-nemoclaw-eks-topology/12-CONTEXT.md
@.planning/phases/12-nemoclaw-eks-topology/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sandbox CR manifest and Secret templates</name>
  <files>k8s/agent-sandbox/openclaw-sandbox.yaml, k8s/agent-sandbox/openclaw-secrets.yaml</files>
  <action>
Create directory `k8s/agent-sandbox/`.

**openclaw-sandbox.yaml** -- Sandbox CR for OpenClaw deployment:
- apiVersion: `agents.x-k8s.io/v1alpha1`, kind: `Sandbox`
- metadata.name: `openclaw-sandbox` (no hardcoded namespace -- applied via `-n` flag)
- podTemplate.spec based on the researched example from 12-RESEARCH.md Pattern 1:
  - nodeSelector: `nvidia.com/gpu: "true"`
  - tolerations: `nvidia.com/gpu` Exists/NoSchedule
  - Container `openclaw`:
    - image: `ghcr.io/openclaw/openclaw:latest`
    - command: `["node", "dist/index.js", "gateway", "--bind=lan", "--port", "18789", "--allow-unconfigured", "--verbose"]`
    - ports: 18789 (gateway), 18790 (secondary)
    - resources.limits and requests: `nvidia.com/gpu: "1"`
    - env: `OPENCLAW_GATEWAY_TOKEN` from Secret `openclaw-token` key `token`, `NVIDIA_API_KEY` from Secret `nvidia-api-key` key `key`
    - securityContext: runAsNonRoot true, runAsUser 1000, allowPrivilegeEscalation false, drop ALL capabilities
    - volumeMounts: `/home/node/.openclaw` (emptyDir config), `/home/node/.openclaw/workspace` (PVC)
  - pod-level securityContext: fsGroup 1000
  - volumes: `openclaw-config` emptyDir
- volumeClaimTemplates: `openclaw-workspace` RWO 2Gi

**openclaw-secrets.yaml** -- Template Secret manifests (values replaced at apply time):
- Secret `openclaw-token` with placeholder `token: <base64-encoded-gateway-token>`
- Secret `nvidia-api-key` with placeholder `key: <base64-encoded-nvidia-api-key>`
- Include a comment at top: "# Replace placeholder values before applying. Use: kubectl create secret generic openclaw-token --from-literal=token=YOUR_TOKEN -n NAMESPACE"

Add a header comment in openclaw-sandbox.yaml referencing the source: `kubernetes-sigs/agent-sandbox/examples/openclaw-sandbox/`.
  </action>
  <verify>
    <automated>test -f k8s/agent-sandbox/openclaw-sandbox.yaml && grep -q "agents.x-k8s.io/v1alpha1" k8s/agent-sandbox/openclaw-sandbox.yaml && grep -q "ghcr.io/openclaw/openclaw" k8s/agent-sandbox/openclaw-sandbox.yaml && grep -q "nvidia.com/gpu" k8s/agent-sandbox/openclaw-sandbox.yaml && test -f k8s/agent-sandbox/openclaw-secrets.yaml && grep -q "kind: Secret" k8s/agent-sandbox/openclaw-secrets.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sandbox CR YAML has correct API version, OpenClaw image, GPU resources, security context, volume mounts, and Secret references. Secret templates exist with placeholder values and usage instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Create operator install script and topology smoke test script</name>
  <files>scripts/install-agent-sandbox.sh, scripts/validate-topology.sh</files>
  <action>
Create directory `scripts/`.

**scripts/install-agent-sandbox.sh** -- Installs agent-sandbox operator v0.2.1:
- `#!/usr/bin/env bash` with `set -euo pipefail`
- Set `AGENT_SANDBOX_VERSION="v0.2.1"` at top
- Step 1: `kubectl apply -f` the manifest.yaml and extensions.yaml from the GitHub releases URL
- Step 2: `kubectl wait --for=condition=Ready pods --all -n agent-sandbox-system --timeout=120s`
- Step 3: Verify CRD exists: `kubectl get crd sandboxes.agents.x-k8s.io` (fail if not found)
- Step 4: Print success message with controller pod status
- Make executable: `chmod +x`

**scripts/validate-topology.sh** -- Validates Phase 12 topology (based on 12-RESEARCH.md smoke test):
- `#!/usr/bin/env bash` with `set -euo pipefail`
- Accept namespace as `$1` with default from env var `NAMESPACE` or fallback to `default`
- `SANDBOX_NAME="openclaw-sandbox"`
- 4 validation steps:
  1. Pod Running: `kubectl wait --for=condition=Ready` on pod with label `sandbox.agents.x-k8s.io/name=$SANDBOX_NAME` (300s timeout)
  2. GPU allocated: Get pod, check `resources.limits.nvidia.com/gpu` equals "1"
  3. OpenClaw gateway: `kubectl exec` into pod and `curl -sf --max-time 10 http://localhost:18789/healthz` -- WARN on failure (not FAIL, since healthz path is unconfirmed per open question #1 in research)
  4. GPU node: Get the pod's nodeName, verify `nvidia.com/gpu` allocatable >= 1
- Print summary: pod name, node name, GPU count
- Exit 0 on pass, exit 1 on any FAIL
- IMPORTANT: Do NOT test localhost:8080 -- that is the MCP sidecar which does not exist until Phase 13 (Pitfall 6 from research)
- Make executable: `chmod +x`
  </action>
  <verify>
    <automated>test -x scripts/install-agent-sandbox.sh && grep -q "v0.2.1" scripts/install-agent-sandbox.sh && test -x scripts/validate-topology.sh && grep -q "validate" scripts/validate-topology.sh && grep -q "18789" scripts/validate-topology.sh && ! grep -q "8080" scripts/validate-topology.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Install script targets agent-sandbox v0.2.1 with wait and CRD verification. Smoke test validates 4 checks (pod Ready, GPU limit, gateway, GPU node) without testing port 8080. Both scripts are executable.</done>
</task>

</tasks>

<verification>
- All 4 files exist in correct directories
- openclaw-sandbox.yaml is a valid Sandbox CR with GPU resources and OpenClaw image
- Scripts are executable and have correct shebang lines
- Smoke test does NOT reference port 8080 (Phase 13 concern)
- Existing 103 MCP tests still pass: `cd mcp-server && pytest tests/ -x -q`
</verification>

<success_criteria>
- k8s/agent-sandbox/ directory contains openclaw-sandbox.yaml and openclaw-secrets.yaml
- scripts/ directory contains install-agent-sandbox.sh and validate-topology.sh
- Manifests reference correct images, API versions, GPU specs per 12-RESEARCH.md
- Scripts are ready to execute against a live EKS cluster in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/12-nemoclaw-eks-topology/12-01-SUMMARY.md`
</output>
