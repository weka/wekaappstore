---
phase: 12-nemoclaw-eks-topology
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - .planning/TOPOLOGY.md
  - .planning/PROJECT.md
autonomous: false
requirements: [NCLAW-01, NCLAW-03]

user_setup:
  - service: openclaw-secrets
    why: "OpenClaw gateway token and NVIDIA API key needed as Kubernetes Secrets"
    env_vars:
      - name: OPENCLAW_GATEWAY_TOKEN
        source: "Generate a random token string for gateway authentication (e.g., openssl rand -hex 32)"
      - name: NVIDIA_API_KEY
        source: "NVIDIA NGC portal -> API Keys (already configured per CONTEXT.md)"
    dashboard_config:
      - task: "Create Kubernetes Secrets before applying Sandbox CR"
        location: "kubectl create secret generic openclaw-token --from-literal=token=<TOKEN> -n <NAMESPACE> && kubectl create secret generic nvidia-api-key --from-literal=key=<KEY> -n <NAMESPACE>"

must_haves:
  truths:
    - "NemoClaw/OpenClaw pod is Running in EKS via agent-sandbox Sandbox CRD"
    - "GPU is allocated to the pod (nvidia.com/gpu: 1)"
    - "Loopback networking works within the pod (containers share network namespace)"
    - "Topology is documented for Phase 13 manifest authoring"
    - "Agent-sandbox CRD approach documented as Key Decision in PROJECT.md"
  artifacts:
    - path: ".planning/TOPOLOGY.md"
      provides: "Structured topology reference for Phase 13"
      contains: "openclaw-sandbox"
      min_lines: 40
    - path: ".planning/PROJECT.md"
      provides: "Key Decision entry for agent-sandbox CRD topology approach"
      contains: "agent-sandbox"
  key_links:
    - from: ".planning/TOPOLOGY.md"
      to: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      via: "documents the deployed state of the Sandbox CR"
      pattern: "agents.x-k8s.io"
    - from: "scripts/validate-topology.sh"
      to: "EKS cluster"
      via: "kubectl commands against live cluster"
      pattern: "kubectl"
---

<objective>
Deploy OpenClaw to EKS via agent-sandbox CRD, validate the topology with the smoke test, and write TOPOLOGY.md as the structured reference for Phase 13.

Purpose: Prove the topology works on the live cluster and capture the exact deployed state so Phase 13 can author manifests without guessing.
Output: Running OpenClaw pod on EKS, validated topology, TOPOLOGY.md reference document, PROJECT.md Key Decision entry.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-nemoclaw-eks-topology/12-CONTEXT.md
@.planning/phases/12-nemoclaw-eks-topology/12-RESEARCH.md
@.planning/phases/12-nemoclaw-eks-topology/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 artifacts -->
Sandbox CR at k8s/agent-sandbox/openclaw-sandbox.yaml:
  - apiVersion: agents.x-k8s.io/v1alpha1
  - kind: Sandbox
  - name: openclaw-sandbox
  - image: ghcr.io/openclaw/openclaw:latest
  - ports: 18789, 18790
  - GPU: nvidia.com/gpu: 1

Scripts from Plan 01:
  - scripts/install-agent-sandbox.sh: installs operator v0.2.1, waits for Ready
  - scripts/validate-topology.sh [NAMESPACE]: runs 5 validation checks (including loopback 8080 WARN probe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install operator, create secrets, deploy Sandbox CR, and run smoke test</name>
  <files>k8s/agent-sandbox/openclaw-sandbox.yaml</files>
  <action>
Determine the target namespace by checking where existing WEKA App Store components are deployed:
```
kubectl get deployments --all-namespaces | grep -i weka
```
Use that namespace for all commands below. If no WEKA components found, ask the user via checkpoint.

**Step 1: Verify GPU Operator health (pre-flight)**
```
kubectl get nodes -o custom-columns=NAME:.metadata.name,GPU:.status.allocatable."nvidia\.com/gpu"
kubectl get pods -n gpu-operator -l app=nvidia-device-plugin-daemonset
```
If GPU allocatable is 0 or device plugin pods are not Running, STOP and report to user.

**Step 2: Install agent-sandbox operator**
Run `bash scripts/install-agent-sandbox.sh` (from Plan 01).
Verify: `kubectl get crd sandboxes.agents.x-k8s.io` returns the CRD.

**Step 3: Create Kubernetes Secrets**
Check if secrets already exist:
```
kubectl get secret openclaw-token -n <NAMESPACE> 2>/dev/null
kubectl get secret nvidia-api-key -n <NAMESPACE> 2>/dev/null
```
If secrets do not exist, this task cannot proceed autonomously -- the user must create them (see user_setup). Print instructions and pause.

**Step 4: Apply Sandbox CR**
```
kubectl apply -f k8s/agent-sandbox/openclaw-sandbox.yaml -n <NAMESPACE>
```
Wait for pod to be scheduled: `kubectl get pods -n <NAMESPACE> -l sandbox.agents.x-k8s.io/name=openclaw-sandbox -w` (30s timeout for initial output).

**Step 5: Run smoke test**
```
bash scripts/validate-topology.sh <NAMESPACE>
```
Capture full output. The smoke test now includes a loopback 8080 path probe (WARN-only) -- note the result for TOPOLOGY.md. If any FAIL, inspect pod events and logs:
```
kubectl describe pod <pod-name> -n <NAMESPACE>
kubectl logs <pod-name> -n <NAMESPACE> -c openclaw --tail=50
```
Report findings.

**Step 6: Record actual deployed state**
Capture these values for TOPOLOGY.md (Task 3 needs them):
- Actual pod name: `kubectl get pods -n <NAMESPACE> -l sandbox.agents.x-k8s.io/name=openclaw-sandbox -o jsonpath='{.items[0].metadata.name}'`
- Node name: `kubectl get pod <pod> -n <NAMESPACE> -o jsonpath='{.spec.nodeName}'`
- GPU allocatable on node
- Container image digest (not just tag): `kubectl get pod <pod> -n <NAMESPACE> -o jsonpath='{.status.containerStatuses[0].imageID}'`
- Actual ports listening (from pod logs)
- Namespace used
- Loopback 8080 probe result (from smoke test output)
  </action>
  <verify>
    <automated>bash scripts/validate-topology.sh $(kubectl get sandbox openclaw-sandbox -o jsonpath='{.metadata.namespace}' 2>/dev/null || echo "default")</automated>
  </verify>
  <done>Agent-sandbox operator installed, OpenClaw Sandbox CR applied, smoke test passes all checks (pod Ready, GPU allocated, gateway responsive, GPU node healthy). Loopback 8080 path probe result captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify EKS deployment</name>
  <action>
User verifies that OpenClaw is deployed correctly to EKS via agent-sandbox Sandbox CRD with GPU allocation and gateway running.

Verification steps:
1. Run: `kubectl get sandbox openclaw-sandbox -n NAMESPACE` -- should show the Sandbox resource
2. Run: `kubectl get pods -n NAMESPACE -l sandbox.agents.x-k8s.io/name=openclaw-sandbox` -- should show Running/Ready
3. Run: `bash scripts/validate-topology.sh NAMESPACE` -- should show all 4 core checks PASS plus loopback 8080 probe result
4. Verify GPU is actually allocated (not just requested): `kubectl describe node GPU_NODE | grep -A5 "Allocated resources"`
5. Verify this is the correct namespace (same as existing WEKA App Store components)
  </action>
  <verify>User confirms deployment is correct</verify>
  <done>User has approved the EKS deployment. Type "approved" to proceed to TOPOLOGY.md generation, or describe any issues.</done>
</task>

<task type="auto">
  <name>Task 3: Write TOPOLOGY.md and update PROJECT.md Key Decision</name>
  <files>.planning/TOPOLOGY.md, .planning/PROJECT.md</files>
  <action>
Create `.planning/TOPOLOGY.md` as a structured reference document that Phase 13 will consume for manifest authoring.

**Structure:**
```markdown
# EKS Topology Reference

**Generated:** <date>
**Cluster:** <from kubectl cluster-info>
**Namespace:** <actual namespace used>
**Status:** Validated via scripts/validate-topology.sh

## Sandbox Resource

| Field | Value |
|-------|-------|
| API Version | agents.x-k8s.io/v1alpha1 |
| Kind | Sandbox |
| Name | openclaw-sandbox |
| Namespace | <actual> |

## OpenClaw Container

| Field | Value |
|-------|-------|
| Image | ghcr.io/openclaw/openclaw:latest |
| Image Digest | <actual from imageID> |
| Ports | 18789 (gateway), 18790 (secondary) |
| GPU | nvidia.com/gpu: 1 (limits and requests) |
| Security Context | runAsNonRoot, uid 1000, drop ALL |
| Volume Mounts | /home/node/.openclaw (emptyDir), /home/node/.openclaw/workspace (PVC 2Gi) |

## GPU Node

| Field | Value |
|-------|-------|
| Node Name | <actual> |
| GPU Type | L40s |
| GPU Count | 1 allocatable |
| Node Selector | nvidia.com/gpu: "true" |
| Tolerations | nvidia.com/gpu Exists NoSchedule |

## Networking

| Field | Value |
|-------|-------|
| Pod Network | Shared namespace (all containers see localhost) |
| NetworkPolicies | None enforced (standard EKS VPC CNI) |
| Agent-Sandbox Egress | No restrictions (CRD passes through standard pod networking) |
| Loopback Access | Confirmed working (NCLAW-03) |
| Loopback :8080 Path | <result from smoke test probe -- expected: "Connection refused" = path open> |
| Gateway Bind | --bind=lan (binds to eth0, reachable via localhost from same pod) |

## Phase 13 Integration Points

- MCP sidecar goes in: spec.podTemplate.spec.containers[] (second container)
- MCP sidecar reaches gateway at: localhost:18789
- Gateway reaches MCP sidecar at: localhost:8080 (Phase 11 port)
- openclaw.json generation: init container writes to shared emptyDir volume
- Blueprint volume: add to spec.podTemplate.spec.volumes[] and mount in sidecar

## Secrets

| Secret Name | Key | Purpose |
|-------------|-----|---------|
| openclaw-token | token | Gateway authentication |
| nvidia-api-key | key | NVIDIA cloud inference API |

## Key Decision

Topology uses agent-sandbox CRD (not plain Deployment) per user decision. This means Phase 13 modifies the Sandbox CR spec, not a Deployment spec.
```

Populate all `<actual>` values from the live cluster state captured in Task 1. Run `kubectl` commands as needed to fill any gaps.

**Update `.planning/PROJECT.md`:**
Add a new row to the Key Decisions table:
| Deploy NemoClaw/OpenClaw via agent-sandbox CRD on EKS | Experimental CRD provides sandbox isolation, GPU scheduling, and volume management out of the box; avoids hand-rolling Deployment + RBAC + PV scaffolding | Validated in Phase 12 -- pod Running with GPU on EKS |

**Update `.planning/STATE.md`:**
- Add to Decisions: "[Phase 12]: OpenClaw deployed via agent-sandbox Sandbox CRD in <namespace> namespace; topology validated"
- Update the blocker "NemoClaw EKS topology must be validated" to note it is resolved
  </action>
  <verify>
    <automated>test -f .planning/TOPOLOGY.md && grep -q "agents.x-k8s.io" .planning/TOPOLOGY.md && grep -q "Phase 13" .planning/TOPOLOGY.md && grep -q "localhost" .planning/TOPOLOGY.md && grep -q "agent-sandbox" .planning/PROJECT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TOPOLOGY.md exists with all fields populated from live cluster state (including loopback 8080 probe result). Contains: Sandbox resource details, container spec, GPU node info, networking confirmation (NCLAW-03), and Phase 13 integration points. PROJECT.md updated with Key Decision for agent-sandbox CRD approach. STATE.md updated with topology decision.</done>
</task>

</tasks>

<verification>
- Sandbox CR is deployed and pod is Running in EKS
- GPU is allocated (nvidia.com/gpu: 1)
- Smoke test (`scripts/validate-topology.sh`) passes all 4 core checks plus loopback probe
- TOPOLOGY.md exists with live cluster values (not placeholders)
- TOPOLOGY.md documents loopback access works (NCLAW-03 evidence) including 8080 path probe result
- TOPOLOGY.md has Phase 13 integration points section
- PROJECT.md contains Key Decision entry for agent-sandbox CRD approach
- Existing 103 MCP tests still pass: `cd mcp-server && pytest tests/ -x -q`
</verification>

<success_criteria>
- OpenClaw pod Running on EKS GPU node via Sandbox CRD (NCLAW-01)
- Loopback networking confirmed working within the pod (NCLAW-03)
- TOPOLOGY.md is the single reference doc Phase 13 needs for manifest authoring
- PROJECT.md Key Decision table includes agent-sandbox CRD topology entry
- User has verified the deployment via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/12-nemoclaw-eks-topology/12-02-SUMMARY.md`
</output>
