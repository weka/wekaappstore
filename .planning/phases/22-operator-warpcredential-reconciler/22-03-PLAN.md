---
phase: 22-operator-warpcredential-reconciler
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - operator_module/tests/test_warp_credential.py
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-08, OPS-09, API-08]
requirements_addressed: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-08, OPS-09, API-08]
tags: [operator, tests, kopf, kr8s, secrets, caplog]

must_haves:
  truths:
    - "PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v exits 0 without network access"
    - "_derive_ngc_payloads('k') is asserted to base64-encode 'k' into NGC_API_KEY and to base64-encode the JSON whose auths.nvcr.io.auth equals base64.b64encode(b'$oauthtoken:k').decode() (D-12, OPS-04)"
    - "_derive_hf_payload('t') is asserted to return exactly one key HF_API_KEY (OPS-05)"
    - "_derive_weka_payload('u','t','e') is asserted to return exactly the three keys WEKA_API_USERNAME, WEKA_API_TOKEN, WEKA_API_ENDPOINT (OPS-06)"
    - "_apply_secret_idempotent test asserts: on .create() raising kr8s.ServerError(409), .patch is called exactly once with {'data': secret.raw['data'], 'type': secret.raw['type']} (OPS-09, D-02)"
    - "_apply_secret_idempotent test asserts: on .create() raising kr8s.ServerError(503), kopf.TemporaryError(delay=30) is raised (D-10)"
    - "_apply_secret_idempotent test asserts: on .create() raising kr8s.ServerError(403), kopf.PermanentError is raised (D-10)"
    - "reconcile_warpcredential test asserts: missing source Secret -> kopf.TemporaryError with delay=30 AND patch.status['conditions'][0]['reason'] == 'KeyMissing' is set BEFORE the raise (OPS-02, D-07, S-4)"
    - "reconcile_warpcredential test asserts: whitespace-only key -> kopf.PermanentError AND patch.status['conditions'][0]['reason'] == 'EmptyKey' (OPS-03, D-09)"
    - "reconcile_warpcredential test asserts: spec.type='unknown' -> kopf.PermanentError AND patch.status['conditions'][0]['reason'] == 'UnknownType' (OPS-01, D-08)"
    - "reconcile_warpcredential idempotency test asserts: second reconcile after derived Secret 'restoration' calls _apply_secret_idempotent again; on 409 path patch is invoked — verified via mock-call inspection (OPS-09)"
    - "delete_warpcredential test asserts: calling the handler emits exactly one logger.warning and makes zero calls to any kr8s.objects.Secret method (OPS-08, D-05)"
    - "caplog test asserts: across ALL captured log records at level DEBUG and above, no record.getMessage() and no str(record.args) contains the raw test key fixture value (API-08, T-22-01)"
    - "caplog test asserts: when reconcile raises kopf.PermanentError for empty key, the exception message references the key NAME (e.g. 'NGC_API_KEY') but NOT the key VALUE (T-22-04)"
    - "All Phase 22 tests live in a single new file operator_module/tests/test_warp_credential.py; pure-helper tests import and call the _derive_* helpers directly with no kr8s mocking; handler-level tests use unittest.mock.patch on kr8s.objects.Secret only (D-16)"
    - "Test scope covers: all three _derive_* payload shapes, the NGC base64 padding case, the KeyMissing TemporaryError path, the EmptyKey PermanentError path, the UnknownType PermanentError path, the idempotency restore path, the delete warning-only path, and the caplog assertion that no log record at DEBUG-or-above contains a raw key fixture value (D-17, API-08)"
  artifacts:
    - path: "operator_module/tests/test_warp_credential.py"
      provides: "Unit tests for all Phase 22 helpers and the reconcile + delete handlers"
      min_lines: 200
    - path: "operator_module/tests/test_warp_credential.py"
      provides: "caplog-based assertion of API-08 (no key in logs at any level)"
      contains: "def test_no_key_in_logs_anywhere"
  key_links:
    - from: "test_warp_credential.py"
      to: "operator_module/tests/test_appstack.py:62-80 (helpers _make_kr8s_secret, _make_kr8s_server_error)"
      via: "verbatim copy of helper bodies into the new test file (Patterns S-6)"
      pattern: "def _make_kr8s_secret"
    - from: "test_warp_credential.py"
      to: "operator_module/tests/test_render.py (pure-helper test pattern)"
      via: "import-inside-test (`from main import ...`) and `with pytest.raises(...) as exc_info` shape"
      pattern: "with pytest.raises\\("
    - from: "caplog assertions"
      to: "API-08 (no token values in operator logs at any level)"
      via: "caplog.at_level(logging.DEBUG) capturing every record across the reconcile"
      pattern: "caplog\\.at_level\\(logging\\.DEBUG\\)"
---

<objective>
Create `operator_module/tests/test_warp_credential.py` covering: (a) pure-helper correctness for all three `_derive_*` helpers including the NGC base64 padding case; (b) `_apply_secret_idempotent` create/409→patch + 5xx → TemporaryError + 4xx → PermanentError paths; (c) `reconcile_warpcredential` handler paths for OPS-01/02/03/07/09 with kr8s mocked; (d) `delete_warpcredential` warning-only behavior (OPS-08); (e) API-08 caplog-based no-key-leak assertion across every reconcile path at log level DEBUG and above.

Purpose: This is the only plan that VALIDATES the seven ROADMAP success criteria for Phase 22. No live cluster — all kr8s calls are mocked via `unittest.mock.patch`. Pytest exits 0 = phase done. The caplog assertion (the first of its kind in this project) is the load-bearing test for API-08.

Output: A single new file `operator_module/tests/test_warp_credential.py` (~250 LOC) following the structure established by `operator_module/tests/test_appstack.py` and `operator_module/tests/test_render.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md
@.planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md
@.planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md
@.planning/phases/22-operator-warpcredential-reconciler/22-01-SUMMARY.md
@.planning/phases/22-operator-warpcredential-reconciler/22-02-SUMMARY.md
@CLAUDE.md
@operator_module/tests/test_appstack.py
@operator_module/tests/test_render.py
@operator_module/tests/conftest.py
@operator_module/main.py

<interfaces>
<!-- Identifiers exported by operator_module/main.py (added in Plans 01 + 02) that this test
     file imports via the `from main import <name>` pattern inside test functions. -->

# From Plan 01:
_b64                            # for re-encoding test inputs to compare expected values
_now_iso                        # not directly tested but called via patch.status['lastSyncTime']
_build_condition                # asserted via the status patch inspection
_derive_ngc_payloads            # tested directly
_derive_hf_payload              # tested directly
_derive_weka_payload            # tested directly
_apply_secret_idempotent        # tested with mocked kr8s Secret (.create / .patch / .raw)
_read_source_secret             # tested indirectly via reconcile_warpcredential

# From Plan 02:
reconcile_warpcredential        # tested with mocked kr8s.objects.Secret.get + constructor
delete_warpcredential           # tested with kr8s methods all mocked to fail-on-call

# Existing in operator_module/main.py (Plan 01 / Plan 02):
_VALID_WARPCRED_TYPES
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 3.1: Create test file scaffolding, mock helpers, and pure derivation tests</name>
  <files>operator_module/tests/test_warp_credential.py</files>
  <read_first>
    - operator_module/tests/test_appstack.py:1-90 (module docstring, sys.path setup, `_make_kr8s_secret`, `_make_kr8s_server_error` — copy verbatim into the new test file, do NOT import-share)
    - operator_module/tests/test_render.py:1-50 (pure-helper test shape: `from main import <name>` inside the test function, `pytest.raises(...) as exc_info`, decision-citing one-line docstrings)
    - operator_module/tests/conftest.py (sys.path setup — note the test file should ALSO do its own defense-in-depth sys.path block per test_appstack.py:23-26)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Section 6 (full derivation test bodies — verbatim sources)
    - .planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md §Excerpt E and §Excerpt F (test file header + mock helper copy targets)
    - .planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md §Pattern S-6 (mock import-pattern INSIDE the test function)
    - .planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md §D-16 §D-17 (test structure decisions)
    - .planning/phases/22-operator-warpcredential-reconciler/22-01-SUMMARY.md (helper signatures for accurate `from main import ...` lines)
    - operator_module/main.py (helpers from Plan 01 — for cross-reference, but tests should NEVER inspect implementation; they assert on observable behavior)
  </read_first>
  <action>
Create `operator_module/tests/test_warp_credential.py` with the following structure (top-to-bottom):

1. Module docstring (verbatim from PATTERNS §Excerpt E, adapted): cites OPS-01..OPS-09 + API-08, lists the test categories (helpers, idempotency, handler paths, delete, caplog).

2. Imports (mirrors test_appstack.py:13-21):
   - `from __future__ import annotations`
   - `import base64`
   - `import json`
   - `import logging`
   - `import sys`
   - `from pathlib import Path`
   - `from unittest.mock import MagicMock, patch`
   - `import pytest`

3. sys.path defense-in-depth block — copy verbatim from `operator_module/tests/test_appstack.py:23-26` (the three-line block that assigns `OPERATOR_MODULE_ROOT = Path(__file__).resolve().parents[1]` and inserts it into `sys.path` if not present). Place immediately after the imports.

4. Helper functions (file-local — do NOT import from test_appstack.py per the project's per-file-copy convention):
   - `_make_kr8s_secret(data_dict)` — copy verbatim from test_appstack.py:62-69
   - `_make_kr8s_server_error(status_code, message='server error')` — copy verbatim from test_appstack.py:72-80
   - `_make_patch_obj()` — new helper that returns a MagicMock with `.status = {}` (kopf patch stand-in). Docstring cites RESEARCH §Section 6.
   - `_make_secret_class_mock()` — new helper that returns a callable factory. Each call (i.e. each `kr8s.objects.Secret({...})` invocation in the handler under test) records the raw dict and returns a MagicMock supporting `.create()`, `.patch()`, and `.raw` (set to the recorded dict). The factory exposes `.instances` (list of returned mocks) so tests can assert on derived-Secret metadata, type, and data. Docstring cites RESEARCH §Section 6 and Pattern S-5 (the test will assert no `ownerReferences` on any recorded raw dict).

5. Pure-derivation tests (4 functions — verbatim test bodies from RESEARCH §Section 6.6):
   - `test_ngc_apikey_data_is_b64_encoded` — calls `_derive_ngc_payloads('my-secret-key')`, asserts `apikey == {'NGC_API_KEY': base64.b64encode(b'my-secret-key').decode('ascii')}`. (OPS-04, D-12)
   - `test_ngc_docker_auth_is_oauthtoken_b64` — calls `_derive_ngc_payloads('my-secret-key')`, base64-decodes the `.dockerconfigjson` value, json-loads it, asserts: `auths.nvcr.io.username == '$oauthtoken'`, `auths.nvcr.io.password == 'my-secret-key'`, `auths.nvcr.io.auth == base64.b64encode(b'$oauthtoken:my-secret-key').decode('ascii')`. (D-12)
   - `test_hf_payload_has_only_hf_api_key` — calls `_derive_hf_payload('hf-token-xyz')`, asserts `set(data.keys()) == {'HF_API_KEY'}` and `base64.b64decode(data['HF_API_KEY']).decode() == 'hf-token-xyz'`. (OPS-05)
   - `test_weka_payload_three_keys` — calls `_derive_weka_payload('admin', 'tok-abc', 'https://weka-cluster:14000')`, asserts `set(data.keys()) == {'WEKA_API_USERNAME', 'WEKA_API_TOKEN', 'WEKA_API_ENDPOINT'}` and decodes each value to confirm the originals round-trip. (OPS-06)

6. Each test function:
   - Uses the import-inside-test pattern: `from main import _derive_ngc_payloads` (etc.) INSIDE the function body, NOT at module top. (S-6)
   - Has a one-line docstring citing the relevant D-* or OPS-* IDs (S-3).
   - Uses single quotes for dict literals (matches the helper code under test).

DO NOT create a conftest.py — the existing operator_module/tests/conftest.py already does the sys.path work. The defense-in-depth block at the top of test_warp_credential.py is per-test-file convention (matches test_appstack.py:23-26).
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py::test_ngc_apikey_data_is_b64_encoded operator_module/tests/test_warp_credential.py::test_ngc_docker_auth_is_oauthtoken_b64 operator_module/tests/test_warp_credential.py::test_hf_payload_has_only_hf_api_key operator_module/tests/test_warp_credential.py::test_weka_payload_three_keys -v</automated>
  </verify>
  <acceptance_criteria>
    - File `operator_module/tests/test_warp_credential.py` exists
    - `grep -c "from __future__ import annotations" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "def _make_kr8s_secret" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "def _make_kr8s_server_error" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "def _make_patch_obj" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "def _make_secret_class_mock" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_ngc_apikey_data_is_b64_encoded" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_ngc_docker_auth_is_oauthtoken_b64" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_hf_payload_has_only_hf_api_key" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_weka_payload_three_keys" operator_module/tests/test_warp_credential.py` returns 1
    - `python -m py_compile operator_module/tests/test_warp_credential.py` exits 0
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -k 'ngc or hf or weka_payload' -v` exits 0 (all four derivation tests pass)
  </acceptance_criteria>
  <done>
    The test file scaffolding is in place, four pure-derivation tests pass, and all helper-mock builders are present. Test imports follow the import-inside-test convention so sys.path setup runs first.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: Add idempotency, handler-path, and delete-handler tests</name>
  <files>operator_module/tests/test_warp_credential.py</files>
  <read_first>
    - operator_module/tests/test_warp_credential.py (file from Task 3.1 — append new test functions to this file)
    - operator_module/tests/test_appstack.py:353-380 (error-dispatch assertion pattern — verbatim shape for `with patch(...) ... pytest.raises(kopf.X) as exc_info: ... assert exc_info.value.delay == 30 / assert <token> in str(exc_info.value)`)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Section 6.4-6.6 (handler-level tests including idempotency + error paths)
    - .planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md §Excerpt G (error-dispatch assertion shape)
    - .planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md §D-02 D-05 D-07 D-08 D-09 D-15
    - operator_module/main.py (the freshly-written `reconcile_warpcredential` and `delete_warpcredential` from Plans 01 + 02 — so tests target the actual handler signature)
  </read_first>
  <action>
Append the following tests to `operator_module/tests/test_warp_credential.py`. All use the `from main import <name>` import-inside-test pattern. All target identifiers added by Plans 01 + 02.

**Idempotency tests for `_apply_secret_idempotent`** (D-02, OPS-09):

  - `test_apply_secret_idempotent_creates_on_first_call` — Mock kr8s Secret with `.create = MagicMock()` (success). Assert `.create` called once, `.patch` NOT called.
  - `test_apply_secret_idempotent_patches_on_409` — Mock with `.create.side_effect = _make_kr8s_server_error(409)`, `.patch = MagicMock()`, `.raw = {'data': {'k': 'v'}, 'type': 'Opaque'}`. Call helper. Assert `.create.assert_called_once()` AND `.patch.assert_called_once_with({'data': {'k': 'v'}, 'type': 'Opaque'})` (D-02 exact patch shape).
  - `test_apply_secret_idempotent_500_raises_temporary` — Mock with `.create.side_effect = _make_kr8s_server_error(503)`. `with pytest.raises(kopf.TemporaryError) as exc_info: ...`. Assert `getattr(exc_info.value, 'delay', None) == 30` (D-10 message template).
  - `test_apply_secret_idempotent_403_raises_permanent` — Mock with `.create.side_effect = _make_kr8s_server_error(403)`. `with pytest.raises(kopf.PermanentError): ...` (D-10, 4xx non-409 path).
  - `test_apply_secret_idempotent_timeout_raises_temporary` — Mock with `.create.side_effect = kr8s.APITimeoutError('timeout')`. `with pytest.raises(kopf.TemporaryError) as exc_info: ...`. Assert delay=30 (D-10).

**Handler-path tests for `reconcile_warpcredential`** (use `_make_patch_obj()` and `_make_secret_class_mock()`):

  - `test_reconcile_unknown_type_raises_permanent_with_status` (OPS-01, D-08) — spec={'type': 'invalid-type', 'displayName': 'X', 'secretRef': {'name': 'src', 'key': 'k'}}. Assert `kopf.PermanentError` raised AND `patch_obj.status['conditions'][0]['reason'] == 'UnknownType'`.
  - `test_reconcile_missing_secret_raises_temporary_with_status` (OPS-02, D-07) — mock `main.kr8s.objects.Secret.get` to raise `kr8s.NotFoundError('not found')`. spec=valid huggingface. Assert `kopf.TemporaryError` raised, `exc_info.value.delay == 30`, AND `patch_obj.status['conditions'][0]['reason'] == 'KeyMissing'`.
  - `test_reconcile_empty_key_raises_permanent_with_status` (OPS-03, D-09) — mock `kr8s.objects.Secret.get` to return `_make_kr8s_secret({'k': '   '})` (whitespace). spec=valid huggingface with secretRef.key='k'. Assert `kopf.PermanentError` raised AND `patch_obj.status['conditions'][0]['reason'] == 'EmptyKey'`. Assert the exception message contains the key NAME ('k') but does NOT contain the value '   ' as the value (T-22-04).
  - `test_reconcile_ngc_success_creates_two_derived_secrets` (OPS-04, OPS-07) — mock `kr8s.objects.Secret.get` to return `_make_kr8s_secret({'NGC_API_KEY': 'my-key'})`. Use `_make_secret_class_mock()` to intercept derived-Secret construction. spec={'type': 'nvidia-ngc', 'displayName': 'NGC', 'secretRef': {'name': 'src', 'key': 'NGC_API_KEY'}}, name='ngc-test'. Call handler. Assert: factory `.instances` has length 2; instance[0].raw['metadata']['name'] == 'warp-ngc-test-apikey' AND .raw['type'] == 'Opaque'; instance[1].raw['metadata']['name'] == 'warp-ngc-test-docker' AND .raw['type'] == 'kubernetes.io/dockerconfigjson'; neither instance.raw['metadata'] contains key 'ownerReferences' (T-22-05). Assert `patch_obj.status['conditions']` has two entries with types `KeyReady` and `DockerSecretReady` both with status 'True'. Assert `patch_obj.status['derivedSecrets']` contains two dicts with `name`+`type` fields. Assert `patch_obj.status['lastSyncTime']` is set.
  - `test_reconcile_hf_success_creates_one_token_secret` (OPS-05, OPS-07) — analogous, single derived Secret `warp-hf-test-token` (Opaque, single key `HF_API_KEY`).
  - `test_reconcile_weka_success_three_keys_and_endpoint_status` (OPS-06, OPS-07) — source Secret has `_make_kr8s_secret({'WEKA_API_USERNAME': 'admin', 'WEKA_API_TOKEN': 'tok', 'WEKA_API_ENDPOINT': 'https://weka:14000'})`. spec={'type': 'weka-storage', 'displayName': 'WEKA Prod', 'secretRef': {'name': 'src', 'key': 'WEKA_API_TOKEN'}, 'endpoint': 'https://weka:14000'}. Assert derived Secret has `'data'` keys exactly `{'WEKA_API_USERNAME', 'WEKA_API_TOKEN', 'WEKA_API_ENDPOINT'}` AND `patch_obj.status['wekaEndpoint'] == 'https://weka:14000'`.
  - `test_reconcile_weka_missing_username_raises_permanent` (OPS-06 + D-09 weka-storage validation) — source Secret only has `{'WEKA_API_TOKEN': 'tok', 'WEKA_API_ENDPOINT': 'e'}` (missing username). Assert `kopf.PermanentError` AND `patch_obj.status['conditions'][0]['reason'] == 'EmptyKey'` AND exception message contains `'WEKA_API_USERNAME'`.
  - `test_reconcile_idempotent_restore_on_resume` (OPS-09, D-02) — Simulate the "deleted derived Secret restored" scenario: source Secret returns normally; the `_make_secret_class_mock()` factory's `.create.side_effect` for the first instance is `_make_kr8s_server_error(409)` (Secret exists), and `.patch` is a MagicMock. After handler completes: assert `instance.create.assert_called_once()`; assert `instance.patch.assert_called_once_with({'data': <expected derived data>, 'type': 'Opaque'})` (proves restoration path goes through patch — D-02 + OPS-09).

**Delete handler test** (OPS-08, D-05):

  - `test_delete_warpcredential_logs_warning_and_does_nothing` — Create a MagicMock logger. Patch `main.kr8s.objects.Secret` (and `.get` / `.create` / `.patch`) to all raise `AssertionError` if called. Call `delete_warpcredential(name='hf-test', namespace='weka-app-store', logger=mock_logger)`. Assert: `mock_logger.warning.assert_called_once()`; the call args reference both `name` and `namespace`; the message contains the string `'OPS-08'` (or equivalent — verify against Plan 01 Task 1.3 message template); no `kr8s.*` method was called.

**Conventions**:
  - Patch target paths must be `main.kr8s.objects.Secret.get` and `main.kr8s.objects.Secret` (the constructor) — matching how `operator_module/main.py` references kr8s (i.e. `kr8s.objects.Secret.get(...)`).
  - Every test imports `kopf` and `kr8s` INSIDE the test function body (S-6 / test_appstack.py:361-362 convention).
  - For tests that call the handler, build the `spec` dict with single quotes for keys.
  - All assertions on the exception message MUST be on `str(exc_info.value)` (matches test_appstack.py:372 shape).
  - Tests that exercise the handler MUST pass `logger=logging.getLogger('test')` to satisfy the handler signature.
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v --deselect operator_module/tests/test_warp_credential.py::test_no_key_in_logs_anywhere --deselect operator_module/tests/test_warp_credential.py::test_no_key_in_exception_message_on_empty_key</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_apply_secret_idempotent_creates_on_first_call" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_apply_secret_idempotent_patches_on_409" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_apply_secret_idempotent_500_raises_temporary" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_apply_secret_idempotent_403_raises_permanent" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_apply_secret_idempotent_timeout_raises_temporary" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_unknown_type_raises_permanent_with_status" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_missing_secret_raises_temporary_with_status" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_empty_key_raises_permanent_with_status" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_ngc_success_creates_two_derived_secrets" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_hf_success_creates_one_token_secret" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_weka_success_three_keys_and_endpoint_status" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_weka_missing_username_raises_permanent" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_reconcile_idempotent_restore_on_resume" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_delete_warpcredential_logs_warning_and_does_nothing" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "'KeyMissing'" operator_module/tests/test_warp_credential.py` is at least 1
    - `grep -c "'EmptyKey'" operator_module/tests/test_warp_credential.py` is at least 2 (huggingface empty + weka missing username)
    - `grep -c "'UnknownType'" operator_module/tests/test_warp_credential.py` is at least 1
    - `grep -c "ownerReferences" operator_module/tests/test_warp_credential.py` is at least 1 (T-22-05 assertion that derived Secret raw dicts do NOT contain this key)
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v --deselect operator_module/tests/test_warp_credential.py::test_no_key_in_logs_anywhere --deselect operator_module/tests/test_warp_credential.py::test_no_key_in_exception_message_on_empty_key` exits 0 (all non-caplog tests pass)
  </acceptance_criteria>
  <done>
    Idempotency, handler-path, and delete-handler tests are present and pass. All `reconcile_warpcredential` failure branches are exercised. Derived-Secret raw dict assertions verify the no-ownerReferences invariant (T-22-05). OPS-09 idempotency test demonstrates the create→409→patch path with the exact patch payload shape from D-02.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.3: Add API-08 caplog-based log-safety tests</name>
  <files>operator_module/tests/test_warp_credential.py</files>
  <read_first>
    - operator_module/tests/test_warp_credential.py (file from Task 3.1 + 3.2 — append two final tests)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Section 7 (full caplog assertion patterns — verbatim test bodies)
    - .planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md §Excerpt I (caplog convention `caplog.at_level(logging.DEBUG)` for log-safety tests)
    - .planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md §D-03 §D-17 (no key in logs — locked)
    - .planning/REQUIREMENTS.md §API-08 (the requirement under test)
    - operator_module/main.py (the actual `reconcile_warpcredential` body from Plan 02 — confirm no `logger.*` or exception message includes the `key` variable)
  </read_first>
  <action>
Append two final tests to `operator_module/tests/test_warp_credential.py`. These are the load-bearing tests for API-08 (T-22-01, T-22-04).

1. `test_no_key_in_logs_anywhere(caplog)` (API-08, T-22-01, D-03):
   - Define `test_key = 'super-secret-test-key-value-do-not-leak-42'` (long, unique, ASCII; unlikely to appear by accident in any log message).
   - Source Secret = `_make_kr8s_secret({'NGC_API_KEY': test_key})`.
   - patch_obj = `_make_patch_obj()`.
   - Wrap the reconcile call in `with caplog.at_level(logging.DEBUG):` (capture EVERYTHING, including DEBUG records — API-08 says "at any level").
   - Patch `main.kr8s.objects.Secret.get` to return the source secret; patch `main.kr8s.objects.Secret` (constructor) with the `_make_secret_class_mock()` factory.
   - Invoke `reconcile_warpcredential(body={}, spec={'type': 'nvidia-ngc', 'displayName': 'NGC Test', 'secretRef': {'name': 'src', 'key': 'NGC_API_KEY'}}, name='ngc-test', namespace='weka-app-store', patch=patch_obj, logger=logging.getLogger('test'))`.
   - After the call returns, iterate `for record in caplog.records: msg = record.getMessage(); assert test_key not in msg, f'Key leaked in log: {record.levelname} {msg}'; assert test_key not in str(record.args or ''), f'Key leaked in args: {record.args}'`.
   - Also assert `len(caplog.records) >= 1` (the handler emits at least the success log line, so we have evidence the capture actually ran).

2. `test_no_key_in_exception_message_on_empty_key(caplog)` (T-22-04, D-09):
   - Source Secret = `_make_kr8s_secret({'NGC_API_KEY': '   '})` (whitespace value).
   - Invoke `reconcile_warpcredential` with `spec={'type': 'nvidia-ngc', 'displayName': 'NGC Test', 'secretRef': {'name': 'src', 'key': 'NGC_API_KEY'}}` inside `with pytest.raises(kopf.PermanentError) as exc_info:` and `with caplog.at_level(logging.DEBUG)`.
   - Assert `'NGC_API_KEY' in str(exc_info.value)` — key NAME is OK to reference (T-22-04).
   - Assert the exception message does NOT contain the literal whitespace value as the value (acceptable: `'NGC_API_KEY'` appears, but the message must not contain `:    ` or `=    ` patterns suggesting the empty value is being displayed). A pragmatic check: `assert "'   '" not in str(exc_info.value)` (no quoted whitespace).
   - Additionally iterate caplog records and assert no record message contains `'   '` as a quoted value pattern (defensive — even though the test value is whitespace, the principle is that the value never echoes back).

DO NOT add a `_secret_class_mock` cleanup hook — the test scope is per-function; mocks expire automatically.

Add a one-line module-level comment immediately above `test_no_key_in_logs_anywhere`: `# Load-bearing assertion for API-08: this is the only enforcement that no key value leaks into operator logs at any level. DO NOT WEAKEN.`
  </action>
  <verify>
    <automated>PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py::test_no_key_in_logs_anywhere operator_module/tests/test_warp_credential.py::test_no_key_in_exception_message_on_empty_key -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_no_key_in_logs_anywhere" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "^def test_no_key_in_exception_message_on_empty_key" operator_module/tests/test_warp_credential.py` returns 1
    - `grep -c "caplog.at_level(logging.DEBUG)" operator_module/tests/test_warp_credential.py` is at least 2 (both caplog tests use the DEBUG level)
    - `grep -c "super-secret-test-key-value-do-not-leak-42" operator_module/tests/test_warp_credential.py` is at least 1 (the unique test fixture key value)
    - `grep -c "Load-bearing assertion for API-08" operator_module/tests/test_warp_credential.py` returns 1
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py::test_no_key_in_logs_anywhere -v` exits 0
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py::test_no_key_in_exception_message_on_empty_key -v` exits 0
    - `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v` exits 0 (ENTIRE test file passes — this is the gate the phase ships behind)
  </acceptance_criteria>
  <done>
    Both caplog tests are in place and the entire `test_warp_credential.py` test suite exits 0 with `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v`. The unique test-key value never appears in any captured log record at any log level, satisfying API-08 / T-22-01. The empty-key exception message references the key NAME but not the key VALUE, satisfying T-22-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture key → operator code under test | The test seeds a unique sentinel key value into a mocked source Secret; reaches the handler via patched kr8s; the test then asserts the sentinel does NOT escape into any log record or exception message |
| Test mocks → real kr8s/network | `unittest.mock.patch` boundaries prevent any real K8s API calls; pytest runs hermetically |

## STRIDE Threat Register (Plan-03 validates Plan 01 + 02 mitigations)

| Threat ID | Category | Validated By | Disposition | Severity | Mitigation Plan |
|-----------|----------|--------------|-------------|----------|-----------------|
| T-22-01 | Information Disclosure | `test_no_key_in_logs_anywhere` | mitigate | high | caplog.at_level(logging.DEBUG) captures every log record across the full reconcile (including the success log line); test asserts the sentinel key value is absent from every record's `getMessage()` AND `str(record.args)`. This is the only project-wide enforcement of "no key in logs at any level"; comment marks it as load-bearing. |
| T-22-02 | Tampering | `test_reconcile_empty_key_raises_permanent_with_status`, `test_reconcile_weka_missing_username_raises_permanent` | mitigate | high | Tests assert kopf.PermanentError is raised AND patch.status reason is 'EmptyKey' BEFORE the raise. Empty/missing values are rejected before any derivation occurs. |
| T-22-04 | Information Disclosure | `test_no_key_in_exception_message_on_empty_key` | mitigate | high | Asserts the empty-key PermanentError message contains the key NAME ('NGC_API_KEY') but NOT the quoted empty value. Generalizes to: every error path uses key NAMES only. |
| T-22-05 | Tampering | `test_reconcile_ngc_success_creates_two_derived_secrets` | mitigate | high | Test asserts the recorded raw dict for every derived Secret does NOT contain the key `'ownerReferences'` — so kubectl-delete-CR will not trigger K8s GC of derived Secrets. OPS-08 is structurally enforced. |
| T-22-06 | Information Disclosure | `test_reconcile_*_success_*` | mitigate | high | Tests inspect patch_obj.status and verify only `conditions`, `derivedSecrets`, `lastSyncTime`, `wekaEndpoint` are written, and `derivedSecrets` items contain only `name`+`type`. No status field carries credential bytes. |
| T-22-07 | Tampering | `test_reconcile_idempotent_restore_on_resume` | mitigate | low | Test exercises the create→409→patch path with the exact patch shape from D-02. Confirms the operator converges to current spec on every reconcile (no stale-data path). |
</threat_model>

<verification>
- `PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v` exits 0 (ENTIRE test file passes — the phase-shipping gate)
- All 7 ROADMAP Phase 22 success criteria covered by at least one test in this file:
  - SC#1 nvidia-ngc creates two derived Secrets → `test_reconcile_ngc_success_creates_two_derived_secrets`
  - SC#2 huggingface creates warp-{name}-token → `test_reconcile_hf_success_creates_one_token_secret`
  - SC#3 weka-storage creates 3-key Secret + status.wekaEndpoint → `test_reconcile_weka_success_three_keys_and_endpoint_status`
  - SC#4 idempotency (manual delete → reconcile restores) → `test_reconcile_idempotent_restore_on_resume` + `test_apply_secret_idempotent_patches_on_409`
  - SC#5 missing Secret → TemporaryError + KeyMissing status → `test_reconcile_missing_secret_raises_temporary_with_status`
  - SC#6 delete CR leaves derived Secrets + logs warning → `test_delete_warpcredential_logs_warning_and_does_nothing`
  - SC#7 no key in logs + pytest passes without network → `test_no_key_in_logs_anywhere` + the absence of any real-network call in any test
</verification>

<success_criteria>
- Single new file `operator_module/tests/test_warp_credential.py` exists
- Full test suite (`PYTHONPATH=operator_module pytest operator_module/tests/test_warp_credential.py -v`) exits 0 without network access
- All 7 ROADMAP success criteria for Phase 22 are covered by at least one test (mapped above)
- API-08 caplog assertion is present and uses `caplog.at_level(logging.DEBUG)` to capture EVERY level
- No test mocks an internal helper to bypass log/state assertions — every test exercises the real helper bodies added by Plans 01 and 02
- Tests do NOT modify `operator_module/main.py` — only the new test file is written
</success_criteria>

<output>
After completion, create `.planning/phases/22-operator-warpcredential-reconciler/22-03-SUMMARY.md` listing the test count, the ROADMAP success criteria → test name mapping, and the final pytest exit summary (test count + pass count). This summary is the artifact gsd-verify-phase will consume.
</output>
