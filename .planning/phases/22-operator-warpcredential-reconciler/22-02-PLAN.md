---
phase: 22-operator-warpcredential-reconciler
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - operator_module/main.py
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-09, API-08]
requirements_addressed: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-09, API-08]
tags: [operator, kopf, kr8s, reconciler, status]

must_haves:
  truths:
    - "A WarpCredential CR with spec.type='nvidia-ngc' triggers the operator to call _apply_secret_idempotent twice — once for warp-{name}-apikey (type Opaque, single key NGC_API_KEY) and once for warp-{name}-docker (type kubernetes.io/dockerconfigjson, single key .dockerconfigjson). Both derived Secret raw dicts have no metadata.ownerReferences. (OPS-04, T-22-05)"
    - "A WarpCredential CR with spec.type='huggingface' triggers a single _apply_secret_idempotent for warp-{name}-token (Opaque, single key HF_API_KEY). (OPS-05)"
    - "A WarpCredential CR with spec.type='weka-storage' triggers a single _apply_secret_idempotent for warp-{name}-token (Opaque, three keys WEKA_API_USERNAME, WEKA_API_TOKEN, WEKA_API_ENDPOINT). status.wekaEndpoint is patched to spec.endpoint. (OPS-06, RESEARCH §5)"
    - "When spec.secretRef references a missing Secret, the handler patches status.conditions[KeyReady].status='False' with reason='KeyMissing' BEFORE raising kopf.TemporaryError(delay=30). (OPS-02, D-07, D-15, S-4)"
    - "When the resolved key value is empty or whitespace-only, the handler patches status.conditions[KeyReady].status='False' with reason='EmptyKey' BEFORE raising kopf.PermanentError. (OPS-03, D-09, D-15)"
    - "When spec.type is not in _VALID_WARPCRED_TYPES, the handler patches status with reason='UnknownType' BEFORE raising kopf.PermanentError. (OPS-01, D-08)"
    - "On success, status.conditions contains KeyReady=True (reason=KeyPresent) and — for nvidia-ngc only — also DockerSecretReady=True (reason=DockerSecretPresent). status.derivedSecrets lists each derived Secret as {name, type}. status.lastSyncTime is set to _now_iso(). (OPS-07, D-14, CRD lines 330-377)"
    - "Manually deleting a derived Secret and triggering a reconcile (e.g. operator restart firing @kopf.on.resume) restores it via the create-or-patch idempotency path. (OPS-09, D-02, D-04)"
    - "No call site in reconcile_warpcredential logs, prints, includes in an exception message, or otherwise emits the decoded source-Secret value at any log level. Log records carry name/namespace/type/derived-secret-name only. (API-08, D-03, T-22-01, T-22-04)"
    - "All three reconcile decorators target the canonical CRD coordinates group='warp.io', version='v1alpha1', plural='warpcredentials' — verbatim, matching the schema locked by Phase 21 (D-06)"
    - "All kr8s secret writes performed by this handler go exclusively through Plan 01's _apply_secret_idempotent — no kubectl subprocess, no Helm SDK — consistent with the existing kr8s.objects.Secret.get reads in operator_module/main.py (D-01)"
  artifacts:
    - path: "operator_module/main.py"
      provides: "reconcile_warpcredential stacked-decorator handler dispatching on spec.type"
      contains: "@kopf.on.create('warp.io', 'v1alpha1', 'warpcredentials')"
    - path: "operator_module/main.py"
      provides: "reconcile handler registered for update with field='spec' (Pitfall 3)"
      contains: "@kopf.on.update('warp.io', 'v1alpha1', 'warpcredentials', field='spec')"
    - path: "operator_module/main.py"
      provides: "reconcile handler registered for resume (OPS-09 post-restart idempotency)"
      contains: "@kopf.on.resume('warp.io', 'v1alpha1', 'warpcredentials')"
  key_links:
    - from: "reconcile_warpcredential"
      to: "Plan 01 helpers (_derive_*, _apply_secret_idempotent, _read_source_secret, _build_condition, _now_iso)"
      via: "direct in-module function calls"
      pattern: "_derive_(ngc_payloads|hf_payload|weka_payload)\\("
    - from: "reconcile_warpcredential status writes"
      to: "CRD status subresource at weka-app-store-operator-chart/templates/crd.yaml:326-377"
      via: "kopf patch.status['conditions'|'derivedSecrets'|'lastSyncTime'|'wekaEndpoint']"
      pattern: "patch\\.status\\["
    - from: "reconcile_warpcredential update path"
      to: "Pitfall 3 (prevent status-write re-entry)"
      via: "field='spec' on @kopf.on.update"
      pattern: "field='spec'"
    - from: "reconcile_warpcredential resume path"
      to: "OPS-09 idempotency after operator restart"
      via: "@kopf.on.resume re-fires reconcile for every existing CR on pod startup"
      pattern: "@kopf\\.on\\.resume\\('warp\\.io'"
---

<objective>
Add the `reconcile_warpcredential` kopf handler to `operator_module/main.py`, stacked on three decorators (`on.create` / `on.update` with `field='spec'` / `on.resume`). The handler reads the source Secret via Plan 01's `_read_source_secret`, dispatches on `spec.type` to the matching `_derive_*` helper, applies each derived Secret via Plan 01's `_apply_secret_idempotent` (which gives OPS-09 idempotency for free), and writes status via kopf's `patch.status`. All failure paths patch status BEFORE raising.

Purpose: Wire the helpers from Plan 01 into a single kopf-watched reconcile function that satisfies OPS-01..OPS-07 and OPS-09. Honors D-04 (stacked decorators), D-07/D-08/D-09/D-10 (error classification), D-14/D-15 (status writes), D-03/API-08 (no key in logs).

Output: ~100 lines of new code added to `operator_module/main.py` immediately after `delete_warpcredential` (added in Plan 01) — the WarpCredential reconcile handler is the final group of handlers in the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md
@.planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md
@.planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md
@.planning/phases/22-operator-warpcredential-reconciler/22-01-SUMMARY.md
@CLAUDE.md
@operator_module/main.py
@weka-app-store-operator-chart/templates/crd.yaml

<interfaces>
<!-- The reconcile_warpcredential handler consumes the following identifiers added by Plan 01.
     All are module-level in operator_module/main.py — no import needed; they are in the same file. -->

# From Plan 01 (already in main.py):
_VALID_WARPCRED_TYPES: set[str]                                      # {'nvidia-ngc', 'huggingface', 'weka-storage'}
def _b64(s: str) -> str
def _now_iso() -> str
def _build_condition(type_, status, reason, message) -> dict
def _derive_ngc_payloads(key: str) -> tuple[dict, dict]              # (apikey_data, docker_data)
def _derive_hf_payload(key: str) -> dict                              # {'HF_API_KEY': ...}
def _derive_weka_payload(username, token, endpoint) -> dict           # 3 keys
def _read_source_secret(name, namespace, *, ctx) -> dict[str, bytes]  # 4-branch dispatch matrix
def _apply_secret_idempotent(secret_obj, *, ctx) -> None              # create-or-patch on 409
def delete_warpcredential(name, namespace, logger, **_) -> None       # already registered

# New in this plan:
@kopf.on.create('warp.io', 'v1alpha1', 'warpcredentials')
@kopf.on.update('warp.io', 'v1alpha1', 'warpcredentials', field='spec')
@kopf.on.resume('warp.io', 'v1alpha1', 'warpcredentials')
def reconcile_warpcredential(body, spec, name, namespace, patch, logger, **kwargs): ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: Implement reconcile_warpcredential with stacked decorators and dispatch logic</name>
  <files>operator_module/main.py</files>
  <read_first>
    - operator_module/main.py — entire file, but in particular: lines 1-30 (imports), 285-308 (_render_or_raise + the new helpers added by Plan 01), 439-481 (Phase 18 dispatch matrix), 720-728 (status patch shape), 951-975 (existing WekaAppStore create handler), 1159 (existing update decorator with `field='spec'`), 1175 (existing delete decorator), and the freshly-added `delete_warpcredential` function near the bottom of the file (added by Plan 01)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Example D (full reconcile_warpcredential skeleton — use as the structural template, but DO NOT paste verbatim; cite RESEARCH for each block)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Section 2 (exact patch.status shape — required fields, enum values, derivedSecrets schema)
    - .planning/phases/22-operator-warpcredential-reconciler/22-RESEARCH.md §Section 5 (weka-storage source-Secret structure: three hard-coded keys; spec.secretRef.key is read for schema symmetry only)
    - .planning/phases/22-operator-warpcredential-reconciler/22-PATTERNS.md §Excerpt C (decorator + signature + status-patch pattern) and §Pattern S-4 (status-patch-before-raise — REQUIRED for every failure branch)
    - .planning/phases/22-operator-warpcredential-reconciler/22-CONTEXT.md §D-04 D-06 D-07 D-08 D-09 D-10 D-14 D-15 (locked decisions)
    - weka-app-store-operator-chart/templates/crd.yaml:279-382 (LOCKED schema — secretRef.required = [name, key]; status.conditions items.required = [type, status]; status enum values are "True"/"False"/"Unknown")
    - .planning/phases/22-operator-warpcredential-reconciler/22-01-SUMMARY.md (helpers and constants added in Plan 01 — confirms callable names and signatures)
  </read_first>
  <action>
Append `reconcile_warpcredential` to `operator_module/main.py` immediately AFTER `delete_warpcredential` (the function added in Plan 01). This becomes the new bottom of the module.

Decorator stack (ORDER MATTERS — kopf applies decorators bottom-up, but all three register handlers on the same callable):

  - `@kopf.on.create('warp.io', 'v1alpha1', 'warpcredentials')`
  - `@kopf.on.update('warp.io', 'v1alpha1', 'warpcredentials', field='spec')`
  - `@kopf.on.resume('warp.io', 'v1alpha1', 'warpcredentials')`

All three positional args use single quotes. `field='spec'` on update is REQUIRED — prevents the operator's own status writes from re-firing the handler (Pitfall 3, mirrors main.py:1159 convention). Include a one-line comment above the update decorator citing Pitfall 3 + D-04 so future maintainers do not remove it.

Function signature: `def reconcile_warpcredential(body, spec, name, namespace, patch, logger, **kwargs):` — accept `logger` (per-handler) and capture remaining kwargs with `**kwargs`.

Docstring: one paragraph citing D-04 (stacked decorators), D-07/D-08/D-09/D-10 (error classification), D-11..D-13 (derivation helpers), D-14/D-15 (status writes), OPS-01..OPS-09 (requirements covered), API-08/D-03 (no key in logs).

Body (in order — each step uses Plan 01 helpers; raise sites MUST patch status first per S-4):

1. `cred_type = spec.get('type')` ; `display_name = spec.get('displayName', name)` ; `ctx = f"WarpCredential {namespace}/{name}({display_name})"`. The `ctx` is the only piece of caller-controlled string that flows into error messages; it carries metadata, NOT key material.

2. OPS-01 (D-08) — Unknown type belt-and-suspenders:
   - If `cred_type not in _VALID_WARPCRED_TYPES`:
     - `patch.status['conditions'] = [_build_condition('KeyReady', 'False', 'UnknownType', f'spec.type {cred_type!r} not recognized')]`
     - `raise kopf.PermanentError(f'{ctx}: unknown spec.type {cred_type!r}')`

3. Extract `secret_ref = spec.get('secretRef', {})` ; `src_name = secret_ref.get('name')` ; `src_key = secret_ref.get('key')`. If either is falsy, patch status with reason `'InvalidSpec'` and raise `kopf.PermanentError(f'{ctx}: spec.secretRef.name and .key required')`. (CRD admission should catch this; belt-and-suspenders.)

4. OPS-02 (D-07) — Read source Secret using Plan 01's `_read_source_secret(src_name, namespace, ctx=ctx)`. Wrap in `try` to add the status patch BEFORE the kopf.TemporaryError escapes:
   - Catch `kopf.TemporaryError` AS `e`; set `patch.status['conditions'] = [_build_condition('KeyReady', 'False', 'KeyMissing', f'Source Secret {namespace}/{src_name} not found (retrying)')]`; `raise` (re-raise unchanged so kopf still treats as retryable).
   - Catch `kopf.PermanentError` AS `e`; set `patch.status['conditions'] = [_build_condition('KeyReady', 'False', 'KeyReadError', f'API error fetching Secret {namespace}/{src_name}')]`; `raise`.

5. After successful read, `src_data: dict[str, bytes] = ...`. For nvidia-ngc and huggingface: verify `src_key in src_data`; if not, patch status with reason `'KeyMissing'` and message `f'Source Secret {namespace}/{src_name} has no key {src_key!r}'`, then `raise kopf.PermanentError(...)`. Decode `key = src_data[src_key].decode('utf-8')`. OPS-03 (D-09): if `not key.strip()`, patch status with reason `'EmptyKey'` (message: `f'Source Secret {namespace}/{src_name}[{src_key}] is empty'`) and raise `kopf.PermanentError(f'{ctx}: source Secret key {src_key!r} is empty')`. Exception message references key NAME only — NEVER value (T-22-04).

6. For weka-storage (RESEARCH §5 — three hard-coded keys, spec.secretRef.key is read for schema symmetry only):
   - Extract `username_bytes = src_data.get('WEKA_API_USERNAME')`, `token_bytes = src_data.get('WEKA_API_TOKEN')`, `endpoint_bytes = src_data.get('WEKA_API_ENDPOINT')`.
   - For each that is None or whose `.decode('utf-8').strip()` is empty: patch status with reason `'EmptyKey'` and message naming the missing key (e.g. `f'weka-storage source Secret {namespace}/{src_name} missing key WEKA_API_USERNAME'`), then `raise kopf.PermanentError`. Do this BEFORE attempting derivation.
   - `username = username_bytes.decode('utf-8')`, similarly for token. `endpoint_from_src = endpoint_bytes.decode('utf-8')`. `endpoint = spec.get('endpoint') or endpoint_from_src` (defense-in-depth — RESEARCH §5 step 3).

7. Dispatch on `cred_type`. Each branch builds the derived kr8s Secret objects with the raw dict shape from RESEARCH §Example D. Critical: the `metadata` dict MUST contain ONLY `name` and `namespace` — NO `ownerReferences` (T-22-05, Pattern S-5).
   - `nvidia-ngc`: `apikey_data, docker_data = _derive_ngc_payloads(key)`. Build `apikey_secret = kr8s.objects.Secret({'apiVersion': 'v1', 'kind': 'Secret', 'metadata': {'name': f'warp-{name}-apikey', 'namespace': namespace}, 'type': 'Opaque', 'data': apikey_data})`. Build `docker_secret` similarly with `'name': f'warp-{name}-docker'` and `'type': 'kubernetes.io/dockerconfigjson'`. Call `_apply_secret_idempotent(apikey_secret, ctx=f'{ctx}: apikey')` then `_apply_secret_idempotent(docker_secret, ctx=f'{ctx}: docker')`. Build `derived_secrets_list = [{'name': f'warp-{name}-apikey', 'type': 'Opaque'}, {'name': f'warp-{name}-docker', 'type': 'kubernetes.io/dockerconfigjson'}]`.
   - `huggingface`: `hf_data = _derive_hf_payload(key)`; build `hf_secret` with `'name': f'warp-{name}-token'`, `'type': 'Opaque'`, `'data': hf_data`. Call `_apply_secret_idempotent(hf_secret, ctx=f'{ctx}: token')`. `derived_secrets_list = [{'name': f'warp-{name}-token', 'type': 'Opaque'}]`.
   - `weka-storage`: `weka_data = _derive_weka_payload(username, token, endpoint)`; build `weka_secret` similarly to huggingface. `_apply_secret_idempotent(weka_secret, ctx=f'{ctx}: token')`. Same `derived_secrets_list` shape.

8. OPS-07 — Success status write (D-14):
   - `conditions = [_build_condition('KeyReady', 'True', 'KeyPresent', 'Derived secrets reconciled')]`
   - If `cred_type == 'nvidia-ngc'`: `conditions.append(_build_condition('DockerSecretReady', 'True', 'DockerSecretPresent', 'NGC docker Secret reconciled'))`
   - `patch.status['conditions'] = conditions`
   - `patch.status['derivedSecrets'] = derived_secrets_list`
   - `patch.status['lastSyncTime'] = _now_iso()`
   - If `cred_type == 'weka-storage'`: `patch.status['wekaEndpoint'] = spec.get('endpoint', '')`

9. Final log line: `logger.info(f'{ctx}: reconciled {len(derived_secrets_list)} derived Secret(s)')` — name+namespace+count only; no key values, no derived payload contents.

CONVENTIONS (locked by Plan 01 and Patterns §Conventions):
  - Single quotes for dict literals (status writes) and decorator positional args.
  - Double quotes inside the four-branch dispatch matrix f-strings (Plan 01 added this in `_read_source_secret`; this handler does NOT need its own dispatch matrix — it consumes the helper's exceptions).
  - `from e` NOT needed when re-raising a kopf.* error you caught (step 4) — `raise` alone preserves the original exception.
  - NEVER `print(...)`. NEVER include `key`, `token`, `username`, `endpoint` parameter VALUES in logger calls. NEVER pass `body` / `spec` to `logger.*` (no `extra={'body': body}` — RESEARCH Anti-Patterns).
  - The `metadata` dict for every derived Secret has ONLY `name` and `namespace`. NO `ownerReferences`, NO `labels`, NO `annotations`. (Pattern S-5; if labels/annotations are needed in a future phase, add them deliberately.)
  - Use the `logger` kwarg, NOT `logging.info(...)` — this lets caplog tests (Plan 03) capture handler-tagged records cleanly.
  </action>
  <verify>
    <automated>grep -nE "@kopf\.on\.(create|update|resume)\('warp\.io', 'v1alpha1', 'warpcredentials'" operator_module/main.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@kopf.on.create('warp.io', 'v1alpha1', 'warpcredentials')" operator_module/main.py` returns 1
    - `grep -c "@kopf.on.update('warp.io', 'v1alpha1', 'warpcredentials', field='spec')" operator_module/main.py` returns 1
    - `grep -c "@kopf.on.resume('warp.io', 'v1alpha1', 'warpcredentials')" operator_module/main.py` returns 1
    - `grep -c "^def reconcile_warpcredential" operator_module/main.py` returns 1
    - `python -m py_compile operator_module/main.py` exits 0
    - `grep -c "_derive_ngc_payloads\|_derive_hf_payload\|_derive_weka_payload" operator_module/main.py | head -1` is at least 4 (3 helper definitions from Plan 01 + at least one call in the handler per type — total >= 6 in practice)
    - `grep -c "_apply_secret_idempotent" operator_module/main.py` is at least 4 (definition + multiple calls — one for each derived Secret across the dispatch branches)
    - `grep -c "'KeyMissing'" operator_module/main.py` is at least 2 (both the missing-secret and missing-key-in-secret branches use this reason; required for OPS-02 + D-07)
    - `grep -c "'EmptyKey'" operator_module/main.py` is at least 1 (OPS-03 / D-09)
    - `grep -c "'UnknownType'" operator_module/main.py` is at least 1 (OPS-01 / D-08)
    - `grep -c "'KeyPresent'" operator_module/main.py` is at least 1 (D-14 success reason)
    - `grep -c "'DockerSecretReady'" operator_module/main.py` is at least 1 (OPS-07 — second condition for nvidia-ngc)
    - `grep -c "patch.status\['conditions'\]" operator_module/main.py` increases by at least 5 from pre-plan (5+ failure branches all write status before raising — S-4)
    - `grep -c "patch.status\['derivedSecrets'\]" operator_module/main.py` is at least 1 (OPS-07)
    - `grep -c "patch.status\['lastSyncTime'\]" operator_module/main.py` is at least 1 (OPS-07)
    - `grep -c "patch.status\['wekaEndpoint'\]" operator_module/main.py` is at least 1 (OPS-06)
    - `grep -nE "ownerReferences" operator_module/main.py | wc -l` returns 0 (T-22-05, Pattern S-5 — no GC trigger on derived Secrets)
    - `grep -c "warp-{name}-apikey" operator_module/main.py` is at least 1 (OPS-04 derived-Secret naming)
    - `grep -c "warp-{name}-docker" operator_module/main.py` is at least 1 (OPS-04)
    - `grep -c "warp-{name}-token" operator_module/main.py` is at least 1 (OPS-05/OPS-06)
    - `grep -c "kubernetes.io/dockerconfigjson" operator_module/main.py` is at least 1 (OPS-04 NGC docker Secret type)
    - `grep -nE "logger\.(info|warning|error|debug)" operator_module/main.py | wc -l` increases by at least 1 from Plan 01 baseline (success log line at end of handler)
  </acceptance_criteria>
  <done>
    `reconcile_warpcredential` is registered with three stacked decorators (create / update field='spec' / resume), dispatches correctly on spec.type to the three Plan 01 helpers, writes status before every raise, never references `ownerReferences` on derived Secrets, never logs key values, and py_compile succeeds. The handler is the final group of decorators in `operator_module/main.py`. Plan 03 tests are now unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CR `spec` → operator | Untrusted (admin-supplied) input; CRD admission gates enum/required, handler does belt-and-suspenders validation |
| Source Secret `.data` → operator memory | Cleartext credential material crosses into process memory; must never escape via logs or status |
| Operator → K8s API server (write) | Operator writes derived Secrets via kr8s; RBAC (Phase 21) restricts to App Store namespace |
| Operator → kopf patch.status | Status fields are public-readable to anyone with CR get/list — must contain only metadata, never key values |

## STRIDE Threat Register (Plan-02 subset of T-22-01..T-22-08)

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-22-01 | Information Disclosure | `reconcile_warpcredential` log calls | mitigate | high | All `logger.*` invocations reference only `ctx` (CR name+namespace+displayName), `cred_type`, and counts. The variables `key`, `username`, `token`, `endpoint` are never passed to a logger. Caplog test in Plan 03 (`test_no_key_in_logs_anywhere`) enforces end-to-end. D-03, API-08. |
| T-22-02 | Tampering | OPS-03 empty-key validation | mitigate | high | Step 5 (nvidia-ngc/huggingface) and step 6 (weka-storage) both check `not value.strip()` BEFORE invoking any `_derive_*` helper. PermanentError raised; status patched with reason `EmptyKey` first (S-4). |
| T-22-03 | Elevation of Privilege | Cross-namespace Secret access | mitigate | high | Handler ALWAYS passes `namespace=namespace` (the CR's metadata.namespace) to `_read_source_secret` and to every derived-Secret `metadata.namespace`. No code path constructs a different namespace string. Phase 21 RBAC scopes the operator's ServiceAccount to the App Store namespace only — the K8s API server enforces this regardless of operator code (defense in depth). |
| T-22-04 | Information Disclosure | Exception messages | mitigate | high | Every `kopf.*Error` message references CR name+namespace, source-Secret name+key NAME, or status-code. The `{key!r}` / `{value!r}` patterns reference KEY NAMES only (e.g., `'NGC_API_KEY'`), never key values. Code review gate: any `f'...{key}...'` outside `ctx` construction is a defect. |
| T-22-05 | Tampering | Derived Secret garbage collection | mitigate | high | Every `kr8s.objects.Secret({...})` call site has `metadata` with ONLY `name` + `namespace`. NO `ownerReferences`. Grep gate in acceptance criteria enforces zero occurrences of `ownerReferences` in the file. OPS-08 thereby holds: kubectl delete WarpCredential leaves derived Secrets intact. |
| T-22-06 | Information Disclosure | Status field contents | mitigate | high | `patch.status['conditions']` writes use only the four CRD-permitted fields (type / status / reason / message / lastTransitionTime); message strings reference only metadata (name, namespace, key NAME). `patch.status['derivedSecrets']` items have only `name` + `type`. `patch.status['wekaEndpoint']` mirrors `spec.endpoint` (already-public CR data). No status field carries credential bytes. |
| T-22-07 | Tampering | Stale spec replay | mitigate | low | Every reconcile (create/update/resume) re-reads `spec.secretRef` from the live CR. `_apply_secret_idempotent` always patches with the CURRENT derived payload — no caching, no history. The create-or-409→patch idempotency guarantees convergence to spec on every reconcile. |
| T-22-08 | Tampering | Non-UTF-8 source Secret bytes | accept | low | Source Secret `.data[key]` decoded via `bytes.decode('utf-8')`. UnicodeDecodeError is uncaught and surfaces as a kopf-tracked error (logged at kopf level; retried per kopf default). Acceptable: a malformed admin Secret is fail-loud, not silent-broken. (Future enhancement: wrap in try/except for a nicer kopf.PermanentError with reason 'InvalidEncoding'; out of scope for Phase 22.) |
</threat_model>

<verification>
- `python -m py_compile operator_module/main.py` exits 0
- All grep gates in `<acceptance_criteria>` pass
- `grep -nE "ownerReferences" operator_module/main.py | wc -l` returns 0 (re-checked at phase end)
- `grep -nE "logging\.(info|warning|error|debug)\(.*key" operator_module/main.py | wc -l` returns 0 (sanity: no module-level logging of any variable named `key`)
- Three @kopf.on.* decorators all use single quotes for positional args (`'warp.io', 'v1alpha1', 'warpcredentials'`)
</verification>

<success_criteria>
- `reconcile_warpcredential` is registered with three stacked decorators (create / update with `field='spec'` / resume)
- Type dispatch reaches all three branches with the correct helper calls and derived-Secret metadata
- Every failure path patches `status.conditions[KeyReady].status='False'` with the correct reason (`UnknownType`, `InvalidSpec`, `KeyMissing`, `EmptyKey`, `KeyReadError`) BEFORE raising
- Success path writes `conditions` (including `DockerSecretReady` for nvidia-ngc), `derivedSecrets`, `lastSyncTime`, and `wekaEndpoint` (weka-storage only)
- No derived Secret has `metadata.ownerReferences`
- No logger or exception message includes any decoded source-Secret value
- The handler delegates ALL kr8s I/O to Plan 01 helpers (`_read_source_secret`, `_apply_secret_idempotent`) — does not call `kr8s.objects.Secret.get` or `.create()` directly
</success_criteria>

<output>
After completion, create `.planning/phases/22-operator-warpcredential-reconciler/22-02-SUMMARY.md` listing:
  - The exact line range occupied by `reconcile_warpcredential`
  - The three decorator definitions verbatim
  - Each failure branch with its `reason` string (for Plan 03 to assert against)
  - Confirmation that no `ownerReferences` were introduced
</output>
