---
phase: 24-settings-gui-overhaul
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - app-store-gui/webapp/templates/settings.html
autonomous: true
requirements: [GUI-02, GUI-03, GUI-04, GUI-05, GUI-06, GUI-07, GUI-08, GUI-09]
tags: [settings-gui, credentials, traffic-light, polling, forms, xss-defense]

must_haves:
  truths:
    - "Clicking [+ Add] on any sub-section expands a single inline form below that sub-section's list; opening a form on any other sub-section first closes the currently-open form (single-open-form invariant, cross-type) (GUI-03)"
    - "NGC and HuggingFace add forms render exactly two inputs: a text Name and a password Key; both have required attribute; Save is disabled until both are non-empty (GUI-04)"
    - "WEKA add form renders four inputs: text Name, text Username, password API Token, url Endpoint; Save is disabled until all four are non-empty AND endpoint.checkValidity() === true (GUI-05)"
    - "Successful POST /api/credentials returns the credential name in data.item.name and triggers immediate row insertion in amber state with polling started (GUI-07)"
    - "Polling calls GET /api/credentials every 2000 ms; transitions to green on ready=true, red on error present, red with timeout copy after 30000 ms elapsed (GUI-07)"
    - "Polling pauses when document.visibilityState !== 'visible' (or document.hidden === true) and resumes on visibilitychange (UI-SPEC line 267)"
    - "All polling intervals are cleared on beforeunload (UI-SPEC line 346)"
    - "Green-state row shows ONLY the display name + Ready badge + Delete button — no input fields are present in the DOM of a green row (GUI-06)"
    - "Amber-state row has NO Delete button (UI-SPEC line 156 — prevents race with operator reconcile)"
    - "Red-state row shows display name + truncated error reason (text-red-400 text-xs, full reason in title attr) + Delete button (GUI-08)"
    - "Delete button triggers a browser-native confirm() dialog with the locked copy from UI-SPEC line 295; on confirm, sends DELETE /api/credentials/<name>; on 200/204, the <li> fades out (150 ms opacity) and is removed (GUI-09)"
    - "ALL innerHTML interpolation of API-returned strings (displayName, error message, credential name) MUST use the esc() helper at settings.html:323 (XSS mitigation from commit c7ca314)"
    - "POST and DELETE are sent as multipart form-data (POST) and bare DELETE (DELETE), matching the Phase 23 Form(...) signatures already in main.py:805-921"
    - "If POST /api/credentials returns 400/409/500, the Save button's parent form displays the error inline (text-xs text-red-400 mt-2) and stays open; the row is NOT inserted in amber state"
  artifacts:
    - path: "app-store-gui/webapp/templates/settings.html"
      provides: "Inline JS for Credential Management: form open/close, validation, submit, polling state machine, delete with confirm; server-side initial row hydration"
      contains: "startCredentialPoll"
  key_links:
    - from: "[+ Add] button click handler"
      to: "closeAllAddForms() + openAddForm(type)"
      via: "document.querySelector('[data-add-form-container=\"…\"]')"
      pattern: "closeAllAddForms"
    - from: "Save button submit handler"
      to: "POST /api/credentials"
      via: "fetch with FormData body"
      pattern: "/api/credentials"
    - from: "startCredentialPoll(name)"
      to: "GET /api/credentials (no type filter — returns all items including not-ready)"
      via: "setInterval(2000)"
      pattern: "setInterval.*POLL_MS"
    - from: "Delete button click handler"
      to: "DELETE /api/credentials/<name>"
      via: "del() helper at settings.html:254"
      pattern: "del\\(`/api/credentials/"
---

<objective>
Add the Credential Management section's full client-side behaviour: inline add forms, single-open-form invariant, traffic-light state machine (green/amber/red), 2-second polling with 30-second timeout, browser-confirm delete, and inline form-level error display. Server-rendered rows from Plan 01 are hydrated on page load; new credentials submitted via the inline form appear in amber state and poll to green/red.

Purpose: This is the GUI-02 through GUI-09 implementation — the heart of the feature. Without it, users cannot add, monitor, or delete credentials.

Output:
- A single new block of inline JS inside the existing `<script>` element at settings.html:245 — appended below the existing blueprint code (no new `<script>` tag, per UI-SPEC Performance Contract).
- A small CSS addition (or none, if Tailwind classes suffice) for the 150 ms opacity fade-out animation on row delete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-settings-gui-overhaul/24-UI-SPEC.md
@.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md
@.planning/phases/24-settings-gui-overhaul/24-01-SUMMARY.md
@CLAUDE.md
@app-store-gui/webapp/templates/settings.html
@app-store-gui/webapp/main.py

<interfaces>
<!-- Existing JS helpers already in scope inside the settings.html <script> block — DO NOT redeclare. -->

From `app-store-gui/webapp/templates/settings.html` (the existing <script> at line 245):
- `async function post(url, data)` — line 246. Returns parsed JSON. Used for POST submissions; pass FormData.
- `async function get(url)` — line 250. Returns parsed JSON.
- `async function del(url)` — line 254. Returns parsed JSON.
- `const esc = s => (s || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')` — line 323. **MANDATORY** for every innerHTML interpolation of server-returned data. This is the XSS mitigation from commit c7ca314 (WR-04).

DOM contract from Plan 01 (Plan 01 SUMMARY documents these):
- `<button data-add-form="<type>">` — the [+ Add] button. `<type>` ∈ {`nvidia-ngc`, `huggingface`, `weka-storage`}.
- `<ul data-credential-list="<type>">` — the credential list container.
- `<li data-cred-name="<name>" data-cred-ready="true|false" data-cred-error="<msg or empty>">` — server-rendered placeholder rows; this plan hydrates them with full traffic-light DOM on page load.
- `<div hidden data-add-form-container="<type>">` — the slot into which the inline form is injected on [+ Add] click; emptied on Cancel/Save success.

Backend API contracts (Phase 23, shipped — main.py:759-980):
- `GET /api/credentials?namespace=<ns>` → `{ok: true, items: [credItem, ...]}` where `credItem` has the shape returned by `_build_credential_response_item` (see Plan 01 interfaces block).
- `POST /api/credentials` (multipart form-data, Form(...) signatures from main.py:805-813):
  - `display_name: str` (Form key: `display_name`)
  - `cred_type: str` (Form key: `type` — note the alias)
  - `namespace: str` (Form key: `namespace`, default `"default"`)
  - `key: str` — the secret key value
  - `username: str | None` — weka-storage only
  - `endpoint: str | None` — weka-storage only
  - Response on success: `{ok: true, item: {name, displayName, type, ...}}`.
  - Response on error: `{ok: false, error: "..."}` with HTTP 400/409/500.
- `DELETE /api/credentials/<name>` → `{ok: true}` on success.

Detected namespace (already in Jinja2 context from Plan 01): `{{ detected_namespace }}` — emit into JS as a module-level constant for the POST body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hydrate server-rendered rows on page load and wire the [+ Add] / closeAllAddForms / inline-form scaffolding</name>
  <files>app-store-gui/webapp/templates/settings.html</files>
  <read_first>
    - `app-store-gui/webapp/templates/settings.html:245-399` — the existing `<script>` block; new code appends below the blueprint-uninstall logic (after the `// -------- Debug toggles --------` block).
    - `app-store-gui/webapp/templates/settings.html:323` — the `esc()` helper (MUST be referenced verbatim; do NOT redeclare in a nested scope).
    - `app-store-gui/webapp/templates/settings.html:246-257` — `post`, `get`, `del` helpers (re-used; do NOT redeclare).
    - `app-store-gui/webapp/templates/settings.html:336-356` — Blueprint delete handler with confirm() + try/catch — the analog for `wireDeleteButton(name, displayName)` here.
    - `.planning/phases/24-settings-gui-overhaul/24-UI-SPEC.md` §Component 2 (CredentialTypeSubsection), §Component 3 (CredentialRow — three states, exact DOM), §Component 4 (InlineAddForm), and the Copywriting Contract (lines 274-300) — copy values are LOCKED.
    - `.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md` §Pattern 3 (.status-dot extended for green/amber/red), §Pattern 4 (browser confirm), §Pattern 7 (fetch helpers), §Pattern 9 (form submit + result feedback), §Pattern 10 (button + input class palette).
    - `.planning/phases/24-settings-gui-overhaul/24-01-SUMMARY.md` — Plan 01's data-* attribute contract (load this after Plan 01 ships).
  </read_first>
  <action>
Append a new block of inline JS to the existing `<script>` element in `settings.html` (do NOT add a new `<script>` tag). The block is structured as commented region marker `// -------- Credential Management --------` (matches the existing markers at line 305).

Implement, in this order, the following module-level constants and helpers (all `const`-declared at the top of the new block):

1. **Constants** (sourced from Plan 01's Jinja2 context):
   - `const DETECTED_NAMESPACE = {{ (detected_namespace or 'default') | tojson }};` — Jinja2 emits a quoted JSON string. Used for the POST `namespace` field.
   - `const POLL_MS = 2000;` (UI-SPEC §Component 6)
   - `const POLL_TIMEOUT_MS = 30000;` (UI-SPEC §Component 6, GUI-07)
   - `const CREDENTIAL_TYPES = ['nvidia-ngc', 'huggingface', 'weka-storage'];` (the LOCKED render order from Plan 01)

2. **Type metadata helper**:
   ```
   const TYPE_META = {
     'nvidia-ngc':  { label: 'NGC',        fields: ['name', 'key'] },
     'huggingface': { label: 'HuggingFace', fields: ['name', 'key'] },
     'weka-storage': { label: 'WEKA',       fields: ['name', 'username', 'key', 'endpoint'] },
   };
   ```
   The `fields` array drives the InlineAddForm renderer; `name` always maps to `display_name` POST field, `key` to `key`, `username` to `username`, `endpoint` to `endpoint`.

3. **`renderCredentialRow(li, cred)`** — replaces the inner HTML of an existing `<li data-cred-name>` element with the full state-row DOM, choosing green/amber/red based on `cred.ready` and `cred.error`. EVERY string interpolation MUST go through `esc()`. The function NEVER calls `new Function` / `eval` / template-literal-with-unescaped-data; it composes the final innerHTML once and assigns it (Pattern 5 from PATTERNS.md).

   State decision tree:
   - `cred.ready === true` → green DOM (UI-SPEC §Component 3 green block, lines 147-155). Includes Ready badge + Delete button. NO input fields anywhere in the DOM (GUI-06 assertion).
   - `cred.ready === false && cred.error` → red DOM. Dot `bg-red-500`. Error span: `<span class="text-red-400 text-xs ml-2" title="${esc(cred.error)}">${esc(truncate(cred.error, 120))}</span>`. Delete button present.
   - `cred.ready === false && !cred.error` → amber DOM. Dot `bg-amber-500 animate-pulse`. Badge: `Verifying…` (use the U+2026 character, NOT three dots). NO Delete button.

   Add `aria-live="polite"` on the badge span (UI-SPEC line 158). Set `aria-label="Credential ${esc(cred.displayName)}"` on the `<li>` element so the dot's `aria-hidden="true"` does not lose semantic context.

   `truncate(s, n)` is a tiny inline helper: `s.length > n ? s.slice(0, n - 1) + '…' : s`.

4. **`wireDeleteButton(btn, name, displayName)`** — copy the Blueprint delete handler shape at lines 336-356, switching the confirm copy to the LOCKED string from UI-SPEC line 295:
   ```
   if (!confirm(`Delete credential "${displayName}"? Derived secrets in the cluster will remain — you can delete them manually with kubectl if needed.`)) return;
   ```
   On success (`data && data.ok`), apply a CSS fade-out (set `li.style.transition = 'opacity 150ms'; li.style.opacity = '0';` then `setTimeout(() => li.remove(), 150)`) — UI-SPEC line 372 motion contract. Then check the parent `<ul>` for `:empty` and, if no other rows exist, append the `(none stored)` empty-state `<li>`. On failure, append a `<div class="text-xs text-red-400 mt-2">` inside the sub-section card (matching UI-SPEC line 316).

5. **`hydrateInitialRows()`** — on DOMContentLoaded, walks each `<ul data-credential-list>`, calls `GET /api/credentials?namespace=${encodeURIComponent(DETECTED_NAMESPACE)}` once, and for each returned item with a matching `data-cred-name` placeholder li, calls `renderCredentialRow(li, item)`. For items NOT yet placeholdered (race condition where K8s changed between server-render and JS-load), append a new `<li data-cred-name="…">` and render it. For placeholder `<li>` elements that have NO matching backend item (race in the other direction — was just deleted), remove the placeholder.

   Note: Plan 01's server render already auto-escapes via Jinja2 default, so server-rendered placeholder text is already safe. This hydration step replaces it with the full state DOM via `esc()`-guarded JS — same XSS posture, just upgraded UI.

6. **`closeAllAddForms()`** — queries every `[data-add-form-container]`, sets `hidden` attribute back, and empties `innerHTML`. Cross-type single-open invariant (UI-SPEC line 177): clicking [+ Add] on type B while type A's form is open MUST collapse A's form first. No "unsaved changes" warning — this is the locked decision in UI-SPEC line 177 because the user has not submitted yet.

7. **`openAddForm(type)`** — calls `closeAllAddForms()` first, then renders the appropriate form HTML into the type's `[data-add-form-container]`, then `removeAttribute('hidden')` on that container, then focuses the first input.

   Form HTML structure (UI-SPEC §Component 4, line 175 inputs):
   - For `nvidia-ngc` and `huggingface`: two inputs (Name = text, Key = password), both `required`.
   - For `weka-storage`: four inputs (Name = text, Username = text, API Token = password, Endpoint = url with `required pattern` constraint), all `required`.
   - Both form types include a `<button type="submit">Save</button>` (`.btn-purple` + `px-3 py-1.5 rounded-md text-sm font-medium`, initially `disabled` with `opacity-50 cursor-not-allowed`) and a `<button type="button" data-cancel>Cancel</button>` (outline secondary per PATTERNS Pattern 10).
   - Wrap in `<form class="mt-4 p-4 rounded-md bg-white/5 border border-white/10 space-y-3" data-type="${type}">`.
   - Each input has `<label for="…">` per UI-SPEC line 334; ids are namespaced `${type}-name`, `${type}-key`, `${type}-username`, `${type}-endpoint` to keep them unique across forms.
   - Add a `<div class="text-xs text-red-400 mt-2" data-form-error hidden></div>` for inline error display.

   After injecting innerHTML, attach event listeners:
   - `input` event on every required input → calls `updateSaveState(form)` (Task 2 detail).
   - `click` on `[data-cancel]` → calls `closeAllAddForms()`.
   - `keydown` on the form → if `event.key === 'Escape'`, call `closeAllAddForms()` (UI-SPEC line 335 keyboard contract).
   - `submit` event on form → calls `submitAddForm(form, type)` (Task 2).

8. **Wire-up on DOMContentLoaded**:
   - Query every `[data-add-form]` button and attach click → `openAddForm(btn.getAttribute('data-add-form'))`.
   - Query every `<li data-cred-name>` and call `renderCredentialRow(li, …)` once initial rows are fetched via `hydrateInitialRows()`.

Do NOT introduce any new external library, framework, or new CDN script tag. Do NOT use template literals with unescaped server data — every interpolation runs through `esc()`. Do NOT touch the existing blueprint or auth-status JS.

Code style: match the existing block — `const`/`let`, arrow functions for handlers, `async/await` for fetch, no semicolons missing, 2-space indent.
  </action>
  <acceptance_criteria>
    - `python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())"` exits 0.
    - Exactly ONE `<script>` block in the file: `grep -c '<script>' app-store-gui/webapp/templates/settings.html` returns the same count as before this plan (Task 1 appends to the existing block; no new tag).
    - The `esc()` helper is referenced (not redeclared) — `grep -c 'const esc = s =>' app-store-gui/webapp/templates/settings.html` equals exactly 1 (still the original declaration at line 323).
    - The new JS uses `esc()` for every interpolation of `cred.displayName`, `cred.error`, `cred.name`. Grep gate: every line containing `\`${cred\.(displayName|error|name)}\`` inside a template-literal innerHTML assignment must also pipe through `esc()`. Verifier: `grep -nE '\$\{cred\.(displayName|error|name)\}' app-store-gui/webapp/templates/settings.html | grep -v esc | wc -l` returns 0 (every match is also wrapped in esc()).
    - `grep -c 'closeAllAddForms' app-store-gui/webapp/templates/settings.html` ≥ 3 (declaration + call inside openAddForm + call inside Cancel handler).
    - `grep -c 'renderCredentialRow' app-store-gui/webapp/templates/settings.html` ≥ 2 (declaration + at least one call from hydrateInitialRows).
    - `grep -c 'Verifying' app-store-gui/webapp/templates/settings.html` ≥ 1 with the U+2026 character: `grep -c $'Verifying…' app-store-gui/webapp/templates/settings.html` ≥ 1 (the ellipsis character, not three dots).
    - The delete confirm string is exact: `grep -c 'Derived secrets in the cluster will remain' app-store-gui/webapp/templates/settings.html` equals 1.
    - No `eval(` or `new Function(` introduced: `grep -cE '\beval\(|new Function\(' app-store-gui/webapp/templates/settings.html` equals 0.
    - Manual browser check: `[+ Add]` on NGC sub-section opens an inline form with two inputs (Name, Key) below the NGC list. Clicking `[+ Add]` on WEKA closes the NGC form (no warning) and opens the four-input WEKA form below the WEKA list. Pressing Esc inside the form closes it. Pressing Cancel closes it.
    - Manual browser check: a pre-existing `ready: true` credential in the cluster renders on page load with: filled green dot + display name + green "Ready" pill + outline red Delete button. NO `<input>` element appears inside that `<li>`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())" && grep -c closeAllAddForms app-store-gui/webapp/templates/settings.html | awk '{exit ($1>=3)?0:1}' && grep -c renderCredentialRow app-store-gui/webapp/templates/settings.html | awk '{exit ($1>=2)?0:1}' && python -c "import re,sys; src=open('app-store-gui/webapp/templates/settings.html').read(); lines=[l for l in src.splitlines() if re.search(r'\\\$\\{cred\\.(displayName|error|name)\\}', l) and 'esc(' not in l]; sys.exit(0 if not lines else (print('\n'.join(lines)) or 1))"</automated>
  </verify>
  <done>The Credential Management section renders rows in correct colour state from server-rendered data on page load. [+ Add] opens an inline form below the matching sub-section. Cross-type single-open invariant is enforced. Esc and Cancel close the form. Every API string is `esc()`-wrapped in JS. No new `<script>` tag introduced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Form validation + submit, polling state machine, visibility / beforeunload lifecycle</name>
  <files>app-store-gui/webapp/templates/settings.html</files>
  <read_first>
    - `app-store-gui/webapp/templates/settings.html:301-303` — existing setInterval pattern for auth-status (analog).
    - `app-store-gui/webapp/templates/settings.html:336-356` — submit/result-feedback shape (analog for Save).
    - `.planning/phases/24-settings-gui-overhaul/24-UI-SPEC.md` lines 244-268 (Polling state machine) and lines 308-322 (per-requirement Interaction Contract).
    - `.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md` §Pattern 6 (per-row polling Map + visibility hook + beforeunload — copy verbatim, adapt the inner `pollCredentialOnce`).
    - Task 1 deliverables in this same file (TYPE_META, renderCredentialRow, hydrateInitialRows, openAddForm, closeAllAddForms) — Task 2 builds on top.
  </read_first>
  <action>
Add the validation, submit, and polling logic to the same Credential Management JS block created in Task 1. New helpers and listeners, in order:

1. **`updateSaveState(form)`** — called on every input event in any inline form. Iterates required inputs; sets Save button `disabled` if any required input is empty. For the WEKA Storage form, additionally checks `endpointInput.checkValidity() === true` (HTML5 Constraint Validation API; no JS validation library — UI-SPEC line 312). When disabled, the Save button carries the LOCKED tooltip from UI-SPEC line 290 only for the WEKA form's endpoint-invalid sub-case: set `title="Endpoint must be a valid URL, e.g. https://weka-cluster:14000"` when the endpoint is non-empty but invalid; clear `title` otherwise. Toggle the `opacity-50 cursor-not-allowed` classes via `classList.toggle('opacity-50', isDisabled); classList.toggle('cursor-not-allowed', isDisabled);`.

2. **`submitAddForm(form, type)`** — called on form's `submit` event. Implementation:
   - `event.preventDefault()`.
   - Re-check validity (`updateSaveState` then `form.querySelector('button[type=submit]').disabled` → bail if true). Defence in depth: even though the disabled button cannot fire submit via click, `Enter` in an input still submits the form.
   - Build a `FormData(form)` and explicitly add the static fields: `formData.append('type', type)`, `formData.append('namespace', DETECTED_NAMESPACE)`, `formData.append('display_name', form.querySelector(\`#${type}-name\`).value)`. The `key` field in the FormData already maps to the password input (input `name="key"`).
   - Set Save button to `disabled` and change its `textContent` to `'Saving…'` (U+2026 — UI-SPEC line 298).
   - Hide the inline `[data-form-error]` div.
   - `try { const data = await post('/api/credentials', formData); }` ... See the Blueprint pattern at line 336-356 for the try/catch shape.
   - On success (`data && data.ok && data.item && data.item.name`):
     - Call `closeAllAddForms()`.
     - Find or create the `<li data-cred-name="${data.item.name}">` placeholder inside the matching `<ul data-credential-list="${type}">`. If an empty-state `(none stored)` `<li data-empty-state>` is present, remove it first.
     - Call `renderCredentialRow(li, data.item)` (which renders amber state because the just-created CR has `ready: false` and no error yet).
     - Call `startCredentialPoll(data.item.name)`.
   - On failure (response has `ok: false`, or non-200, or thrown error):
     - Show the `[data-form-error]` div with the error text (escaped via `esc()` even though it goes into `textContent` not `innerHTML` — defensive consistency; UI-SPEC line 281).
     - Re-enable the Save button, restore label to `'Save'`.
     - Form stays open for user to correct and retry.

3. **Polling state machine** (UI-SPEC §Component 6 + PATTERNS §Pattern 6 — implement verbatim from the pattern, with these specifics):
   - Module-level `const pollIntervals = new Map();` and `const pollStartedAt = new Map();`.
   - `function startCredentialPoll(name)` — guards against double-start; records start time; starts `setInterval(() => pollCredentialOnce(name), POLL_MS)`.
   - `function stopCredentialPoll(name)` — clearInterval, delete from both Maps.
   - `async function pollCredentialOnce(name)`:
     - Compute `elapsed = Date.now() - pollStartedAt.get(name)`.
     - If `elapsed >= POLL_TIMEOUT_MS` → render the row in red state with the LOCKED timeout copy: `"Verification timed out. The operator may still be reconciling — refresh the page to recheck."` (UI-SPEC line 279). `stopCredentialPoll(name)`. Return.
     - `try { const data = await get(\`/api/credentials?namespace=${encodeURIComponent(DETECTED_NAMESPACE)}\`); } catch (e) { return; /* keep amber, retry next tick — UI-SPEC line 261 */ }`.
     - Locate the matching item by `name` in `data.items`. If not found → row was deleted externally; remove the `<li>` and `stopCredentialPoll(name)`.
     - If `item.ready === true` → call `renderCredentialRow(li, item)` (green); `stopCredentialPoll(name)`.
     - Else if `item.error` is non-empty → render red with that error; `stopCredentialPoll(name)`.
     - Else stay amber, no DOM change.

4. **Page Visibility lifecycle** (UI-SPEC line 267):
   ```
   document.addEventListener('visibilitychange', () => {
     if (document.hidden) {
       pollIntervals.forEach(clearInterval);
     } else {
       Array.from(pollIntervals.keys()).forEach(name => {
         const id = setInterval(() => pollCredentialOnce(name), POLL_MS);
         pollIntervals.set(name, id);
       });
     }
   });
   ```
   Implementation note: on resume, the elapsed counter is NOT reset. If a tab was backgrounded for 25 seconds, returning will give the next tick only 5 seconds before timeout, which is the locked behaviour (it preserves the 30-second total budget — a user who backgrounds a tab is not entitled to a fresh budget).

5. **beforeunload cleanup** (UI-SPEC line 346):
   ```
   window.addEventListener('beforeunload', () => { pollIntervals.forEach(clearInterval); });
   ```

6. **Wire-up on DOMContentLoaded** (extending Task 1's wire-up):
   - After `hydrateInitialRows()`, walk every `<li data-cred-name>` whose `data-cred-ready="false"` AND no `data-cred-error` and call `startCredentialPoll(name)` — handles the case where the user reloads the page during an in-flight verification.

Do NOT poll on a global interval over all credentials — UI-SPEC Performance Contract explicitly says per-row only. Do NOT re-fetch on every single tick if no rows are amber (the Map will be empty and no intervals will be running, naturally).

Do NOT add a global "refresh credentials" button — this is out of scope; row deletion and add-form success are the only mutation paths.
  </action>
  <acceptance_criteria>
    - `python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())"` exits 0.
    - `grep -c startCredentialPoll app-store-gui/webapp/templates/settings.html` ≥ 3 (declaration + call after Save success + call from DOMContentLoaded for amber rows).
    - `grep -c stopCredentialPoll app-store-gui/webapp/templates/settings.html` ≥ 3 (declaration + green-transition call + red-transition call + timeout call).
    - `grep -c POLL_MS app-store-gui/webapp/templates/settings.html` ≥ 2 (declaration + setInterval call).
    - `grep -c POLL_TIMEOUT_MS app-store-gui/webapp/templates/settings.html` ≥ 2 (declaration + elapsed check).
    - `grep -c "Verification timed out" app-store-gui/webapp/templates/settings.html` equals 1 (locked timeout copy from UI-SPEC line 279).
    - `grep -c "visibilitychange" app-store-gui/webapp/templates/settings.html` equals 1 (single listener registration).
    - `grep -c "beforeunload" app-store-gui/webapp/templates/settings.html` equals 1.
    - `grep -nE "setInterval\\(refreshAuthStatus|setInterval.*POLL_MS" app-store-gui/webapp/templates/settings.html` returns exactly two distinct lines (the existing auth-status global poll at line 303 and the new per-row poll).
    - The submit handler uses `FormData(form)` and `post('/api/credentials', formData)`: `grep -c "FormData(form)" app-store-gui/webapp/templates/settings.html` ≥ 1 and `grep -c "post('/api/credentials'" app-store-gui/webapp/templates/settings.html` ≥ 1.
    - Manual browser check, end-to-end: with operator running, adding a new NGC credential displays amber row immediately, polls every 2 s, and transitions to green within the operator's reconcile window. Stopping the operator and adding a credential leaves the row amber for 30 s then transitions to red with the timeout copy. Backgrounding the tab during amber pauses the network requests (verified in DevTools Network panel) and resumes on visibility.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())" && for tok in startCredentialPoll stopCredentialPoll POLL_MS POLL_TIMEOUT_MS visibilitychange beforeunload "Verification timed out"; do n=$(grep -c "$tok" app-store-gui/webapp/templates/settings.html); echo "$tok=$n"; done</automated>
  </verify>
  <done>Saving a new credential transitions the row through amber → (green | red | timeout-red) entirely client-side without a page reload. Polling pauses on hidden tabs and resumes on visibility, with the elapsed budget preserved. All intervals are cleared on `beforeunload`. Form-level errors render inline and the form stays open for retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ API (`/api/credentials*`) | Strings returned from the API are interpolated into the DOM. Even though the backend whitelists fields via `_build_credential_response_item`, the `displayName` and `error` strings are user-controlled (an administrator can set any displayName when creating a credential) and the `error` message originates from a kopf-managed K8s status condition — both must be HTML-escaped at the DOM-write boundary. |
| Browser → API submit | The Add form submits credentials (API keys / tokens) via POST. These values must not leak into the URL, the DOM, browser logs, or any GET query string. |
| Browser → API delete | Browser-native confirm() before any destructive call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | XSS via DOM injection (`I=Information disclosure` via stolen session) | `renderCredentialRow` and the `[data-form-error]` populator | mitigate | EVERY interpolation of `cred.displayName`, `cred.error`, `cred.name`, and the API error message goes through the `esc()` helper at line 323 (WR-04 fix, commit c7ca314). Acceptance criterion runs a static grep checking no `${cred.…}` template-literal interpolation lacks an `esc()` call. |
| T-24-02-02 | XSS via the operator-injected `cred.error` field | `renderCredentialRow` red-state branch | mitigate | `cred.error` originates from a kopf status condition message. An attacker who can write to a WarpCredential's status (only the operator's service account can in production) could craft an XSS payload. `esc()` neutralises it; the `title` attribute also goes through `esc()` so it is safe even when read by the browser's tooltip. |
| T-24-02-03 | Credential exposure via DOM | Inline add form `<input type="password">` | mitigate | The Key / API Token inputs are `type="password"` (UI-SPEC §Component 4). They are NEVER written back to the DOM after submit — only the API response's safe item shape (displayName, ready, etc.) is rendered. The form is wiped on Save success via `closeAllAddForms()`. |
| T-24-02-04 | Credential exposure via URL | submit path | mitigate | Submit uses `FormData` + POST. No credential value ever appears in a URL or in `GET /api/credentials?…`. The poll's GET query string contains only `namespace`. |
| T-24-02-05 | CSRF on POST/DELETE | submit + delete | accept | The Settings page is gated by `ClusterInitMiddleware` and runs on a same-origin context inside the operator's pod. No external origins can target /api/credentials* because the FastAPI app does not advertise CORS. If a future requirement opens CORS, a per-route CSRF token will be needed. |
| T-24-02-06 | DoS via runaway polling | `pollIntervals` Map | mitigate | Per-row only; capped at the number of amber rows; auto-cleared on tab background (visibilitychange) and tab close (beforeunload). 30-second timeout caps any single row's poll lifetime. |
| T-24-02-07 | Race: delete pressed during amber | `wireDeleteButton` × `startCredentialPoll` | mitigate | UI-SPEC line 156: amber row has NO Delete button. The race is structurally prevented by the DOM contract. |
| T-24-02-08 | Open-redirect via `endpoint` field | `<input type="url">` validation | accept | URL validation here is UX-only — `checkValidity()` ensures form input format. The endpoint is consumed by the operator (Phase 22) and by the WEKA proxy (Phase 23), both of which apply their own SSRF guards (`_validate_weka_endpoint` at main.py:1031). No new attack surface is introduced by this plan. |
</threat_model>

<verification>
- `python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())"` exits 0.
- All grep gates in the two `<acceptance_criteria>` blocks pass.
- Manual: open `/settings`, add an NGC credential, observe amber → green transition without page reload (operator must be running). Add a second credential with the operator stopped; observe 30-second timeout → red with the locked copy. Delete a green credential, observe confirm() dialog with the locked copy, observe row fade-out and `(none stored)` reappearing if it was the last row.
- Manual: open the WEKA Storage form, type an invalid endpoint URL (`weka-cluster:14000` without scheme), observe Save remains disabled and hovering Save shows the locked tooltip "Endpoint must be a valid URL, e.g. https://weka-cluster:14000".
- Manual: open the NGC form, type a name, click [+ Add] on HuggingFace → observe NGC form silently collapses and HF form opens.
- Manual: with an amber row mid-poll, switch to another tab for 10 seconds, return → observe poll resumes with elapsed-budget preserved (no full reset).
- XSS injection test: create a WarpCredential CR manually with `spec.displayName: <img src=x onerror=alert(1)>`. Reload the page. Verify the displayName renders as literal text in both the server-rendered placeholder and the JS-hydrated row; no alert fires.
</verification>

<success_criteria>
1. All GUI-02..GUI-09 acceptance behaviour is browser-observable.
2. No new `<script>` tag added; the existing inline `<script>` block at line 245 is the single source of all settings.html JS.
3. No new external library or CDN script tag.
4. Static grep gates for `esc()` coverage on every cred.* interpolation pass with zero unprotected lines.
5. Single-open-form invariant works across types.
6. Polling lifecycle (start/stop/pause/resume/clear) is correct and leak-free.
</success_criteria>

<output>
After completion, create `.planning/phases/24-settings-gui-overhaul/24-02-SUMMARY.md` documenting:
- New JS module-level constants and helpers exported (TYPE_META, pollIntervals, startCredentialPoll, etc.) — for Plan 03 to be aware of and not collide with.
- The exact DOM shape of green / amber / red rows (so Plan 03 cross-references when an overview-related credential row's state changes).
- The visibility / beforeunload contract (Plan 03 must NOT add additional visibilitychange listeners; if Plan 03 needs to pause its own polling, it can hook into the same listener — although per UI-SPEC, the WEKA Overview is fetched only on initial render and on Refresh, so no polling is needed there).
</output>
</content>
</invoke>