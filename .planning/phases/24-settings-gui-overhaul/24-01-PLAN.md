---
phase: 24-settings-gui-overhaul
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/templates/settings.html
autonomous: true
requirements: [GUI-01, GUI-02, GUI-10]
tags: [settings-gui, jinja2, credentials, page-order]

must_haves:
  truths:
    - "GET /settings response HTML contains the Credential Management section as the FIRST <section> inside <main>, above Kubernetes Auth Status (GUI-01)"
    - "The page section order in /settings HTML is: Credential Management → WEKA Storage Overview → Kubernetes Auth Status → Cluster Status → Blueprint Uninstall → Debug (GUI-01)"
    - "settings_page() Jinja2 context contains a credentials_by_type dict with keys nvidia-ngc, huggingface, weka-storage (each a list, possibly empty)"
    - "settings_page() Jinja2 context contains weka_storage_credentials (list of ready weka-storage credentials only)"
    - "When the Kubernetes API is unreachable, settings_page() still returns HTTP 200 — credentials_by_type and weka_storage_credentials default to empty lists (graceful degradation; SDK-05 pattern)"
    - "Credential Management section renders three sub-section cards titled exactly 'NVIDIA NGC API Keys', 'HuggingFace Tokens', 'WEKA Storage API Tokens' in that order (GUI-02 ordering)"
    - "WEKA Storage Overview section is the second top-level <section> and renders the 'No WEKA Storage credential configured. Add one above.' hint when weka_storage_credentials is empty (GUI-10)"
    - "All Jinja2 interpolation of credential displayName and error strings into HTML uses the standard {{ … }} auto-escape (Jinja2 default); inline JS interpolation will be added in Plan 02 using the existing esc() helper at settings.html:323 (XSS mitigation, commit c7ca314)"
  artifacts:
    - path: "app-store-gui/webapp/main.py"
      provides: "Extended settings_page() that fetches WarpCredential CRs and injects credentials_by_type + weka_storage_credentials into the Jinja2 context"
      contains: "credentials_by_type"
    - path: "app-store-gui/webapp/templates/settings.html"
      provides: "Restructured page with Credential Management and WEKA Storage Overview section shells at the top of <main>"
      contains: "Credential Management"
  key_links:
    - from: "app-store-gui/webapp/main.py settings_page()"
      to: "client.CustomObjectsApi list_namespaced_custom_object (group=warp.io, plural=warpcredentials)"
      via: "asyncio.to_thread"
      pattern: "list_namespaced_custom_object.*warpcredentials"
    - from: "app-store-gui/webapp/main.py settings_page()"
      to: "_build_credential_response_item helper at main.py:717"
      via: "list comprehension over resp.items"
      pattern: "_build_credential_response_item"
    - from: "app-store-gui/webapp/templates/settings.html new sections"
      to: "credentials_by_type Jinja2 variable"
      via: "{% for cred in credentials_by_type['nvidia-ngc'] %} loops"
      pattern: "credentials_by_type\\["
---

<objective>
Restructure the Settings page page-order and inject the credential context. This plan establishes the locked page order (Credential Management first), creates the section shells, and extends the /settings route to fetch WarpCredential CRs and pass them to the template. It does NOT implement form interactions, polling, delete, or the WEKA Overview data fetch — those are Plans 02 and 03.

Purpose: Without this plan, Plans 02 and 03 have nowhere to attach. The shells + server-rendered initial-state are the foundation; everything else is JS that hydrates these shells.

Output:
- `app-store-gui/webapp/main.py` /settings route: extended with credentials_by_type + weka_storage_credentials context injection, with graceful-degradation try/except.
- `app-store-gui/webapp/templates/settings.html`: page order changed; Credential Management section (3 sub-section shells with empty (none stored) state + [+ Add] button placeholder) and WEKA Storage Overview section (with no-credential hint OR placeholder div for Plan 03) inserted at top of `<main>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-settings-gui-overhaul/24-UI-SPEC.md
@.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-CONTEXT.md
@.planning/phases/23-backend-credentials-api-and-weka-overview-proxy/23-02-SUMMARY.md
@CLAUDE.md
@app-store-gui/webapp/main.py
@app-store-gui/webapp/templates/settings.html

<interfaces>
<!-- Server-side helpers and constants already in main.py — DO NOT redeclare. -->

From `app-store-gui/webapp/main.py`:
- `def load_kube_config() -> None` — line 233. Call before any K8s client use.
- `client.CustomObjectsApi()` — already imported; used at lines 70, 95, 577, 773.
- `def _build_credential_response_item(cr: Dict[str, Any]) -> Dict[str, Any]` — line 717. Canonical response shape; reuse to keep server-rendered and JS-rendered rows identical.
- `_CREDENTIAL_TYPE_KEYS: Dict[str, Dict[str, Any]]` — line 656. Keys: `nvidia-ngc`, `huggingface`, `weka-storage`.
- Existing `settings_page()` route — lines 521-537. The function this plan extends.
- `asyncio.to_thread(sync_callable, ...)` — pattern used at lines 523-524.
- Imports already in scope: `asyncio`, `client`, `ApiException` (from `kubernetes.client.rest`), `Dict`, `Any` (typing), `Request`, `HTMLResponse`, `templates`.

Response item shape returned by `_build_credential_response_item`:
- `name: str` (metadata.name — used as DNS-1123 slug; DELETE/poll uses this)
- `namespace: str`
- `type: "nvidia-ngc" | "huggingface" | "weka-storage"`
- `displayName: str` (the user-facing label; render via Jinja2 auto-escape, never via raw innerHTML)
- `ready: bool` (true → green state; false + no error → amber; false + error → red)
- `lastSyncTime: str | None` (ISO timestamp)
- `error: str` (present only when not-ready AND KeyReady condition has a message)
- `dockerSecretReady: bool` (nvidia-ngc only)
- `endpoint: str | None` (weka-storage only — from status.wekaEndpoint, NEVER from raw Secret)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend /settings route with credentials_by_type and weka_storage_credentials context</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - `app-store-gui/webapp/main.py:521-537` — existing settings_page() that this task extends
    - `app-store-gui/webapp/main.py:233-258` — load_kube_config() helper (call before any K8s use)
    - `app-store-gui/webapp/main.py:656-671` — `_CREDENTIAL_TYPE_KEYS` constant (defines the three valid keys)
    - `app-store-gui/webapp/main.py:717-756` — `_build_credential_response_item()` helper (canonical response shape; reuse to keep server and JS rows identical)
    - `app-store-gui/webapp/main.py:759-802` — `list_credentials()` handler — copy its `asyncio.to_thread` + `_list` inner-function pattern verbatim
    - `app-store-gui/webapp/main.py:621-654` — secrets list handler showing the `ApiException` / `Exception` envelope pattern (for reference only — settings_page must NOT raise; it must degrade gracefully)
    - `.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md` — §Pattern 12 (the exact code block to replicate) and §Pattern 13 (no-raise rule for /settings)
  </read_first>
  <action>
Modify the existing `settings_page(request: Request)` async function at `main.py:521-537` (do NOT add a new route).

Steps, in order, inside the function body (before the existing `return templates.TemplateResponse(...)` call):

1. After the existing `auth = await asyncio.to_thread(get_auth_status)` and `status = await asyncio.to_thread(get_cluster_status)` lines, compute the effective namespace:
   `ns = detected_ns or "default"` (use the existing `detected_ns` local; it is already defined three lines above).

2. Define an inner `async def _fetch_credentials() -> list[dict]:` that:
   - Calls `load_kube_config()` inside a `try` block.
   - Builds a synchronous inner function `def _list(): return client.CustomObjectsApi().list_namespaced_custom_object(group="warp.io", version="v1alpha1", plural="warpcredentials", namespace=ns)`.
   - Calls `resp = await asyncio.to_thread(_list)`.
   - Returns `[_build_credential_response_item(cr) for cr in (resp or {}).get("items", []) or []]`.
   - Wraps the K8s call in `except ApiException: return []` and `except Exception: return []`. This is the SDK-05 graceful-degradation pattern — the /settings page must always render, even when the K8s API is unreachable.

3. Call `cred_items = await _fetch_credentials()`.

4. Build the typed dict (preserving the locked three keys from `_CREDENTIAL_TYPE_KEYS`):
   ```
   credentials_by_type: dict[str, list[dict]] = {"nvidia-ngc": [], "huggingface": [], "weka-storage": []}
   for it in cred_items:
       t = it.get("type")
       if t in credentials_by_type:
           credentials_by_type[t].append(it)
   ```
   Use a single-quote vs double-quote convention matching the surrounding GUI code style (this file uses double quotes; CLAUDE.md §"GUI uses double quotes"). Do NOT introduce `setdefault` (the three keys are locked; an unknown type should be discarded silently, not silently registered).

5. Derive `weka_storage_credentials = [c for c in credentials_by_type["weka-storage"] if c.get("ready")]`. This is the list used by the GUI-10 visibility test and by the GUI-11 dropdown (multiple vs single label).

6. Add both new keys to the dict passed to `templates.TemplateResponse(...)`:
   - `"credentials_by_type": credentials_by_type,`
   - `"weka_storage_credentials": weka_storage_credentials,`

Do NOT remove or rename any of the existing context keys (`auth`, `status`, `detected_namespace`, `logo_b64`).

Do NOT add a new top-level helper function — keep `_fetch_credentials` nested inside `settings_page` so its `ns` closure is unambiguous and the helper is not callable from elsewhere. The existing module already nests sync helpers inside async handlers (see `list_credentials` at line 759 for the precedent).

Do NOT add `print(...)` anywhere; CLAUDE.md MCP rule is global hygiene for this codebase (the GUI's `logger.info` pattern at line 1008 is the correct outlet for any future debug needs, but no logging is required for this task).
  </action>
  <acceptance_criteria>
    - `python -m py_compile app-store-gui/webapp/main.py` exits 0.
    - `grep -n "credentials_by_type" app-store-gui/webapp/main.py` returns at least 4 lines (initial dict, the for-loop body, the response-dict key, and the final return — assertion: count ≥ 4).
    - `grep -n "weka_storage_credentials" app-store-gui/webapp/main.py` returns at least 2 lines.
    - `grep -n "settings_page" app-store-gui/webapp/main.py` returns exactly 1 line (no duplicate route definition).
    - `grep -nP "credentials_by_type\\.setdefault" app-store-gui/webapp/main.py` returns 0 lines (the three keys are locked).
    - `grep -nP "^\\s*print\\(" app-store-gui/webapp/main.py | grep -v '^#'` returns no new lines compared to the pre-change diff for this function.
    - Static manual trace: passing a stub CR list containing one each of `nvidia-ngc` / `huggingface` / `weka-storage` results in three keys each with exactly one entry and `weka_storage_credentials` length equal to (1 if that entry is `ready`, else 0).
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile app-store-gui/webapp/main.py && grep -c "credentials_by_type" app-store-gui/webapp/main.py | awk '{exit ($1>=4)?0:1}' && grep -c "weka_storage_credentials" app-store-gui/webapp/main.py | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>The /settings route still returns HTTP 200, the response is a TemplateResponse with the existing four context keys preserved, two new keys (`credentials_by_type`, `weka_storage_credentials`) are present in the context dict, and a Kubernetes API outage degrades to empty lists rather than raising.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restructure settings.html section order — insert Credential Management and WEKA Storage Overview shells at top of main</name>
  <files>app-store-gui/webapp/templates/settings.html</files>
  <read_first>
    - `app-store-gui/webapp/templates/settings.html:49-243` — the full `<main>` block whose section order this task changes
    - `app-store-gui/webapp/templates/settings.html:24` — the `.card` CSS rule (re-used; do NOT redefine)
    - `app-store-gui/webapp/templates/settings.html:25-26` — `.btn-purple` CSS (re-used for the [+ Add] buttons)
    - `app-store-gui/webapp/templates/settings.html:28` — `.status-dot` CSS (re-used for credential traffic-light dots in Plan 02)
    - `app-store-gui/webapp/templates/settings.html:200` — the existing outline-secondary button pattern (re-used for Cancel/Refresh later)
    - `app-store-gui/webapp/templates/settings.html:323` — the `esc()` JS helper (XSS mitigation, commit c7ca314; called out in PATTERNS.md §"HTML escape on innerHTML"). This task does NOT add any new JS yet, but Plans 02/03 will need it.
    - `.planning/phases/24-settings-gui-overhaul/24-UI-SPEC.md` lines 126-141 (CredentialManagementSection + CredentialTypeSubsection) and lines 180-188 (overview panel state table) and lines 274-300 (Copywriting Contract — exact strings)
    - `.planning/phases/24-settings-gui-overhaul/24-PATTERNS.md` §Pattern 1 (card wrapper) and §Pattern 11 (Jinja2 conditional with empty fallback)
  </read_first>
  <action>
Restructure the existing `<main class="max-w-6xl mx-auto px-4 py-8 space-y-8">` block in `settings.html`. The new order, top-to-bottom, MUST be exactly:

1. Existing `<h2 class="text-xl font-semibold">Settings</h2>` (line 50) — unchanged.
2. **NEW:** Credential Management `<section>` (added by this task) — see structure below.
3. **NEW:** WEKA Storage Overview `<section>` (added by this task, as a shell — Plan 03 fills in the success-state body).
4. Existing Kubernetes Auth Status `<section>` (currently at line 53) — move below the two new sections; do not modify its contents.
5. Existing Cluster Status `<section>` (currently at line 98) — unchanged; remains in its current relative order, now third.
6. Existing Blueprint Uninstall `<section>` (currently at line 189) — unchanged; remains in its current relative order, now fourth.
7. Existing Debug `<section>` (currently at line 222) — unchanged; remains last.

This re-order is implemented by physical move within the file — do NOT introduce Jinja2 conditional rendering of existing sections. The HTML comment markers (`<!-- Section 3: Kubernetes Auth Status (moved here) -->` etc.) are stale post-Phase-23; update them to reflect the new order (e.g., `<!-- Section 1: Credential Management -->`, `<!-- Section 2: WEKA Storage Overview -->`, `<!-- Section 3: Kubernetes Auth Status -->`, ...). Numbering matches the new top-to-bottom order.

**Credential Management section structure** (UI-SPEC §Component 1+2, copywriting locked by UI-SPEC lines 290-293):

```
<section class="card rounded-lg p-5" id="credentials-section">
  <div class="mb-4">
    <h3 class="font-semibold text-xl">Credential Management</h3>
    <p class="muted text-sm">Stored credentials used by blueprints and by the WEKA Storage Overview below.</p>
  </div>
  <div class="space-y-6">
    {# Sub-section: NVIDIA NGC API Keys (GUI-02 fixed order) #}
    <div class="card rounded-lg p-5" id="ngc-credentials">
      <div class="flex items-center justify-between mb-4">
        <div>
          <h4 class="font-semibold">NVIDIA NGC API Keys</h4>
          <p class="muted text-sm">API keys used to pull NIM and NeMo images and download models from NGC.</p>
        </div>
        <button type="button" class="btn-purple px-3 py-1.5 rounded-md text-sm font-medium" data-add-form="nvidia-ngc">+ Add</button>
      </div>
      <ul role="list" class="text-sm" data-credential-list="nvidia-ngc">
        {% for cred in credentials_by_type['nvidia-ngc'] %}
          {# Plan 02 renders rows server-side; placeholder for now #}
          <li data-cred-name="{{ cred.name }}" data-cred-ready="{{ cred.ready|lower }}" class="py-2 border-b border-white/5 last:border-b-0">{{ cred.displayName }}</li>
        {% else %}
          <li class="muted text-sm py-2" data-empty-state="nvidia-ngc">(none stored)</li>
        {% endfor %}
      </ul>
      <div hidden data-add-form-container="nvidia-ngc"></div>
    </div>

    {# Sub-section: HuggingFace Tokens #}
    <div class="card rounded-lg p-5" id="hf-credentials">
      <!-- identical shape with type="huggingface", header "HuggingFace Tokens",
           helper "Tokens used to download gated models from HuggingFace Hub." -->
    </div>

    {# Sub-section: WEKA Storage API Tokens #}
    <div class="card rounded-lg p-5" id="weka-credentials">
      <!-- identical shape with type="weka-storage", header "WEKA Storage API Tokens",
           helper "API credentials used by blueprints that need WEKA storage and by the live overview below." -->
    </div>
  </div>
</section>
```

Required attributes on the wrappers (used by Plan 02 JS to query without re-traversing the DOM):
- Sub-section `<div>` ids: `ngc-credentials`, `hf-credentials`, `weka-credentials` (the last one is also the anchor target for the GUI-10 "Add one above" link).
- `<button data-add-form="<type>">` on each [+ Add] button.
- `<ul data-credential-list="<type>">` on each list.
- `<li data-cred-name="…" data-cred-ready="true|false" data-cred-error="…">` on each pre-rendered row (Plan 02 will use this attribute for delete-button wiring and to find existing rows when polling).
- `<div hidden data-add-form-container="<type>">` empty placeholder where Plan 02 injects the inline add form.

Server-side row rendering: For this plan, render only the displayName text inside each `<li>` plus the data attributes above. Plan 02 will replace the inner HTML with the full state-row template (dot + badge + Delete button). The `data-cred-name` and `data-cred-ready` attributes are sufficient for Plan 02 to detect "this row exists; promote/hydrate it" vs. "no row, create one in amber state."

Jinja2 auto-escape: `{{ cred.displayName }}` and `{{ cred.name }}` are auto-escaped by Jinja2 default — this is the server-side XSS mitigation. No raw `|safe` filter anywhere.

**WEKA Storage Overview section shell** (UI-SPEC §Component 5, lines 180-188; this task only renders the no-credential hint and a placeholder for the success-state body — Plan 03 fills in the rest):

```
<section class="card rounded-lg p-5" id="weka-overview-section">
  {% if weka_storage_credentials and weka_storage_credentials|length > 0 %}
    <div class="flex items-center justify-between mb-4">
      <div>
        <h3 class="font-semibold">WEKA Storage Overview</h3>
        <p class="muted text-sm">Live capacity, filesystem, and backend-node summary for the selected WEKA cluster.</p>
      </div>
      {# Plan 03 will inject the credential dropdown / static label, Refresh button, and Last updated timestamp here #}
      <div id="weka-overview-controls"></div>
    </div>
    {# Plan 03 fills these three state-machine containers (loading / error / success) #}
    <div id="weka-overview-loading" class="muted text-sm">Loading WEKA cluster…</div>
    <div id="weka-overview-error" hidden></div>
    <div id="weka-overview-success" hidden></div>
  {% else %}
    <h3 class="font-semibold mb-2">WEKA Storage Overview</h3>
    <p class="muted text-sm">
      No WEKA Storage credential configured.
      <a href="#weka-credentials" class="text-[var(--weka-purple)] underline hover:text-white">Add one above.</a>
    </p>
  {% endif %}
</section>
```

The link target `#weka-credentials` matches the sub-section id added above (single source of truth).

Copywriting MUST match UI-SPEC lines 274-300 character-for-character — including the ellipsis character `…` (U+2026, NOT three dots), the en-dash and em-dash usage, and "Add one above." (sentence-cased, period).

Do NOT add any new `<script>` blocks in this plan — the existing block at line 245 stays untouched. Plan 02 will add credential JS inside that same block (single inline `<script>` per Performance Contract). Do NOT add any new CSS rules — `.card`, `.btn-purple`, `.muted`, `.status-dot` already cover every visual primitive this section needs (the Tailwind CDN provides everything else).

The four existing imports of `React`/`MUI` CDN scripts at lines 14-19 stay — those are legacy from the previous secret-management UI and are still used by the home page; do not touch them in this phase.
  </action>
  <acceptance_criteria>
    - First `<section>` after `<h2>Settings</h2>` has `id="credentials-section"`. Grep gate: `grep -nB1 'id="credentials-section"' app-store-gui/webapp/templates/settings.html | grep -c 'Settings</h2>'` returns 1 (the `<h2>` appears in the line immediately preceding, modulo whitespace lines — manual inspection confirms order).
    - Second `<section>` has `id="weka-overview-section"`. Grep gate: `grep -nP '<section[^>]*id="weka-overview-section"' app-store-gui/webapp/templates/settings.html` returns exactly 1 line, with a line number greater than the `id="credentials-section"` line and less than the `Kubernetes Auth Status` line.
    - The three sub-section ids exist: `grep -cE 'id="(ngc|hf|weka)-credentials"' app-store-gui/webapp/templates/settings.html` equals 3.
    - The three `[+ Add]` buttons exist: `grep -cE 'data-add-form="(nvidia-ngc|huggingface|weka-storage)"' app-store-gui/webapp/templates/settings.html` equals 3.
    - Exactly three sub-section header strings are present, in order: `grep -nE 'NVIDIA NGC API Keys|HuggingFace Tokens|WEKA Storage API Tokens' app-store-gui/webapp/templates/settings.html` returns lines in ascending order, three lines total.
    - The no-credential hint string is present verbatim: `grep -c 'No WEKA Storage credential configured.' app-store-gui/webapp/templates/settings.html` equals 1.
    - The link anchor matches: `grep -c 'href="#weka-credentials"' app-store-gui/webapp/templates/settings.html` equals 1.
    - The empty-state copy is the exact UI-SPEC string: `grep -c '(none stored)' app-store-gui/webapp/templates/settings.html` equals 3 (one per sub-section).
    - The existing Kubernetes Auth Status, Cluster Status, Blueprint Uninstall, and Debug sections still exist (their headings still match): each of `grep -c 'Kubernetes Auth Status' app-store-gui/webapp/templates/settings.html`, `grep -c 'Cluster Status' app-store-gui/webapp/templates/settings.html`, `grep -c 'Blueprint Uninstall' app-store-gui/webapp/templates/settings.html`, `grep -c '>Debug<' app-store-gui/webapp/templates/settings.html` is ≥ 1.
    - The four heading line numbers (Credential Management, WEKA Storage Overview, Kubernetes Auth Status, Cluster Status) are strictly ascending — verified by piping through `awk '{print $1}' | sort -n -c` not erroring.
    - No `|safe` filter has been introduced: `grep -c '|safe' app-store-gui/webapp/templates/settings.html` equals 0 (or matches the pre-change baseline, if any pre-existed).
    - No new `<script>` block added: `grep -c '<script>' app-store-gui/webapp/templates/settings.html` is unchanged vs. the pre-change baseline.
    - Browser-observable (manual checkpoint not required for this plan, but documented for executor self-check): visiting `/settings` against a cluster with zero credentials renders Credential Management first with three "(none stored)" sub-sections, then the "No WEKA Storage credential configured. Add one above." hint section, then the existing sections.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())" && grep -cE 'id="(ngc|hf|weka)-credentials"' app-store-gui/webapp/templates/settings.html | awk '{exit ($1==3)?0:1}' && grep -cE 'data-add-form="(nvidia-ngc|huggingface|weka-storage)"' app-store-gui/webapp/templates/settings.html | awk '{exit ($1==3)?0:1}' && grep -c '(none stored)' app-store-gui/webapp/templates/settings.html | awk '{exit ($1==3)?0:1}'</automated>
  </verify>
  <done>Server-rendered /settings HTML places Credential Management first, then WEKA Storage Overview, then the legacy sections. With zero WarpCredentials in the cluster, the page renders three "(none stored)" empty states plus the no-WEKA hint. With pre-existing credentials, the rows render as `<li data-cred-name=…>` placeholders ready for Plan 02 to hydrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /settings | Browser receives Jinja2-rendered HTML. The credential displayName and error strings are interpolated server-side. |
| /settings → Kubernetes API | The handler calls list_namespaced_custom_object; an attacker who controls a WarpCredential's metadata.name or spec.displayName controls what is rendered into the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering / S=Spoofing | settings.html — server-rendered credential displayName | mitigate | Jinja2 auto-escape is on (Jinja2 default). NO `|safe` filter introduced. Acceptance criterion explicitly checks `grep -c '|safe'` is unchanged. |
| T-24-01-02 | Information disclosure | settings_page() context | mitigate | `_build_credential_response_item` (line 717) is the whitelist response shape — it never includes raw key values. This task reuses it verbatim; it cannot leak. |
| T-24-01-03 | Denial of service | settings_page() | mitigate | K8s API failure caught by `except ApiException` and `except Exception` → empty list. /settings always returns 200; an unreachable cluster cannot make the Settings page non-functional. |
| T-24-01-04 | XSS via DOM injection | Future Plan 02 / 03 JS | accept (in this plan) | This plan adds zero JS. Plans 02 and 03 declare the `esc()`-helper requirement in their own threat models. |
| T-24-01-05 | CSRF on /settings | settings_page() | accept | /settings is a `GET` route returning HTML only. No state-changing side effects. CSRF protection on the POST/DELETE credential routes is Phase 23's responsibility (already shipped). |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py` — file is valid Python.
- `python -c "import jinja2; jinja2.Environment().parse(open('app-store-gui/webapp/templates/settings.html').read())"` — template parses without syntax errors.
- Manual: `uvicorn app-store-gui.webapp.main:app` against a cluster with no WarpCredentials renders Credential Management first with three "(none stored)" rows; with one WarpCredential of each type, three populated `<li data-cred-name=…>` placeholders render in the correct sub-section.
- `grep -nE '(Credential Management|WEKA Storage Overview|Kubernetes Auth Status|Cluster Status|Blueprint Uninstall|>Debug<)' app-store-gui/webapp/templates/settings.html | awk -F: '{print $1}' | sort -n -c` — confirms line-order ascending.
</verification>

<success_criteria>
1. /settings route returns HTTP 200 in both K8s-reachable and K8s-unreachable scenarios.
2. With zero credentials in the cluster, the rendered HTML contains: one "Credential Management" heading, three sub-sections each containing "(none stored)", one WEKA Storage Overview section containing "No WEKA Storage credential configured", and the original four legacy sections after them — in that order.
3. With at least one WarpCredential CR of each of the three types, the rendered HTML places one `<li data-cred-name=…>` placeholder in each corresponding sub-section, and (if any are ready) the WEKA Storage Overview shell renders with empty `weka-overview-loading` / `error` / `success` containers ready for Plan 03 hydration.
4. The Jinja2 template parses without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/24-settings-gui-overhaul/24-01-SUMMARY.md` documenting: the new section order, the two new context keys with their shape, the data-* attribute contract used by Plans 02/03 (`data-add-form`, `data-credential-list`, `data-cred-name`, `data-cred-ready`, `data-add-form-container`), and the WEKA Overview placeholder container ids (`weka-overview-loading`, `weka-overview-error`, `weka-overview-success`, `weka-overview-controls`) that Plan 03 will fill.
</output>
</content>
</invoke>