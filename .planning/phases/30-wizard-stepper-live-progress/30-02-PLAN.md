---
phase: 30-wizard-stepper-live-progress
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - app-store-gui/webapp/templates/welcome.html
autonomous: true
requirements: [WIZ-01, WIZ-02, WIZ-03, WIZ-04, WIZ-05, WIZ-06, WIZ-07, WIZ-08]
must_haves:
  truths:
    - "The customer progresses through a 5-step MUI Stepper form (Node Prerequisites → Quay Credentials → WEKA Connection → WEKA Credentials → Review) with Next/Back navigation"
    - "Clicking Next with an empty required field or a format-invalid endpoint/version shows an inline error and does not advance the step"
    - "Step 1 shows the KubeletConfiguration + hugepage snippet as copy-paste text and the Next button is disabled until the 'I have applied node prerequisites' checkbox is checked"
    - "The Review step shows a masked summary (passwords rendered as bullets, quay_dockerconfigjson never shown) and a namespace selector"
    - "The old prerequisite hard-block (handleInitialize early-return on wekaOperatorInstalled/wekaCsiInstalled) is removed"
    - "A named handleInstall function and a showProgress/progressActive visibility flag exist as the handoff contract the progress-view plan (30-03) fills in"
  artifacts:
    - path: "app-store-gui/webapp/templates/welcome.html"
      provides: "WelcomeApp wizard with Stepper, 5 steps, client-side validation, buildVariables() returning the D-08 variables dict, and a named handleInstall entry point + showProgress/progressActive state (the 30-03 handoff contract)"
      contains: "Stepper"
  key_links:
    - from: "welcome.html WelcomeApp validation"
      to: "join_ip_ports / version-tag regexes"
      via: "Next/Submit click handler"
      pattern: "\\^\\[\\^:\\]\\+:"
    - from: "welcome.html Review Install button"
      to: "handleInstall + showProgress"
      via: "onClick after validation calls handleInstall and flips showProgress"
      pattern: "handleInstall"
---

<objective>
Replace the single-button prerequisite hard-block in `welcome.html` with a 5-step MUI Stepper wizard (Node Prerequisites → Quay Credentials → WEKA Connection → WEKA Credentials → Review) that collects all install variables with client-side inline validation, and a Review step that masks secrets and selects a namespace.

Purpose: The wizard form is the customer-facing entry point for installing the full WEKA storage stack — it gathers the exact x-variables the Phase 27 blueprint expects and validates them client-side (the backend does NOT enforce host:port or version-tag formats).
Output: A modified `welcome.html` whose `WelcomeApp` component renders the stepper + steps + validation, exposes the collected `variables` dict via `buildVariables()`, and provides a REQUIRED named `handleInstall` entry point + `showProgress`/`progressActive` visibility state that the live-progress plan (30-03) fills in with the SSE wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md
@.planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md

<interfaces>
<!-- Field → x-variable mapping (D-08). The buildVariables() helper this plan creates
     MUST return an object keyed exactly by these x-variable keys. quay_dockerconfigjson,
     join_ip_ports_list, endpoints_csv are derived SERVER-SIDE — do NOT include them. -->

x-variable keys submitted (from cluster_init/app-store-install.yaml x-variables):
  quay_username        (required)
  quay_password        (required, masked input type="password")
  operator_version     (required, default "v1.13.0")
  join_ip_ports        (required, comma-delimited "host:port,host:port" string)
  weka_image_version   (required)
  weka_endpoint_scheme (default "http", dropdown http|https)
  weka_org             (default "Root")
  weka_username        (required)
  weka_password        (required, masked input type="password")
  namespace            (default "default", ignored server-side but sent)

Validation regexes (D-04, client-side only — backend does not enforce these):
  each join_ip_ports entry:  ^[^:]+:\d+$
  version tags (operator_version, weka_image_version):  ^v?\d+\.\d+(\.\d+)?$

MUI components to add to the existing `const { ... } = MaterialUI;` destructure (welcome.html:40-45):
  Stepper, Step, StepLabel, TextField, Checkbox, FormControlLabel
  (all present in @mui/material@5.15.14 UMD bundle — no new <script> tag)

HANDOFF CONTRACT consumed by 30-03 (these are REQUIRED outputs of this plan):
  buildVariables()  → returns the D-08 variables dict
  handleInstall     → named function; the Review "Install" button calls it after validation. In this
                      plan it is a minimal entry point (calls buildVariables(), flips showProgress/progressActive);
                      30-03 replaces its body with the SSE stream.
  showProgress / progressActive  → boolean state that toggles the wizard view to the progress view.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wizard state (incl. handleInstall + progress flags), MUI imports, and remove the hard-block</name>
  <files>app-store-gui/webapp/templates/welcome.html</files>
  <read_first>
    - app-store-gui/webapp/templates/welcome.html (FULL — the file being modified; note the MaterialUI destructure at lines 40-45, the WelcomeApp useState block at lines 98-110, the handleInitialize hard-block at lines 251-259, and the namespace Select pattern at lines 358-373)
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-01, D-02, D-08, D-09)
    - .planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md (Pattern 2, Pattern 4, Pattern 5)
  </read_first>
  <action>
    Extend the `const { ... } = MaterialUI;` destructure (welcome.html:40-45) to also pull `Stepper, Step, StepLabel, TextField, Checkbox, FormControlLabel`. Do not add any new `<script>` CDN tags — these are in the existing `@mui/material@5.15.14` UMD bundle (D-01).

    Add `React.useState` hooks inside `WelcomeApp` for: `activeStep` (number, default 0); `nodePrereqAck` (bool, default false); and one form-state object `form` holding the D-08 fields with defaults — `quay_username:""`, `quay_password:""`, `operator_version:"v1.13.0"`, `join_ip_ports:""`, `weka_image_version:""`, `weka_endpoint_scheme:"http"`, `weka_org:"Root"`, `weka_username:""`, `weka_password:""`. Add `fieldErrors` state (object, default `{}`) for inline validation messages. Secret fields (`quay_password`, `weka_password`) MUST live only in this in-memory state — never write them to `localStorage` (D-02). `localStorage` stays restricted to `selectedNamespace` (reuse the existing `selectedNamespace` state/effect at lines 110/135-136/365-368).

    Add the REQUIRED progress-visibility state: reuse the existing `showProgress` state (welcome.html:108) and add a `progressActive` boolean state (default false). These are the visibility flag 30-03 consumes; keep them named exactly `showProgress` and `progressActive`.

    Remove the prerequisite hard-block early-return in `handleInitialize` (welcome.html:251-259) — the block that checks `wekaOperatorInstalled !== true || wekaCsiInstalled !== true` and sets `Failed` (WIZ-08). The wizard now installs the operator/CSI rather than requiring them present. Leave the `wekaOperatorInstalled`/`wekaCsiInstalled` status-card display intact for now (it is informational; 30-03 reworks the body); only the gating early-return is removed.

    Add a `buildVariables()` helper function returning a plain object keyed by the exact x-variable keys in the interfaces block above, reading from `form` and `selectedNamespace` (set `namespace: selectedNamespace`). Do NOT include `quay_dockerconfigjson`, `join_ip_ports_list`, or `endpoints_csv` — those are server-derived (Phase 29). Trim string values.

    Add a REQUIRED named `handleInstall` function (this is the 30-03 handoff point). In THIS plan it is a minimal entry point: call `buildVariables()`, set `showProgress=true` and `progressActive=true` (so the view flips to the progress region). 30-03 replaces its body with the SSE stream. `handleInstall` and `buildVariables()` are the contract 30-03 depends on — they must exist as named functions.
  </action>
  <verify>
    <automated>python -m py_compile app-store-gui/webapp/main.py operator_module/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `welcome.html` MaterialUI destructure contains `Stepper`, `Step`, `StepLabel`, `TextField`, `Checkbox`, `FormControlLabel`.
    - `welcome.html` contains a `buildVariables` function (grep: `buildVariables`) that references `operator_version`, `weka_endpoint_scheme`, and `weka_org` keys.
    - `welcome.html` contains a named `handleInstall` function (grep: `handleInstall`) and `progressActive` state (grep: `progressActive`).
    - `welcome.html` no longer contains the hard-block string `you need to install the WEKA Operator and WEKA CSI driver first` inside an early-return at the top of `handleInitialize` (the early `return;` guard on `wekaOperatorInstalled`/`wekaCsiInstalled` is gone).
    - No new `<script src=` CDN tag was added (grep -c of `<script src=` is unchanged from original = 7).
    - `quay_password` / `weka_password` never appear as an argument to `localStorage.setItem` (grep: `localStorage.setItem` lines reference only `selectedNamespace`).
    - welcome.html has no automated test harness — acceptance is source + behavioral assertions above; py_compile only confirms the touched repo's Python still compiles (no Python was changed).
  </acceptance_criteria>
  <done>MUI imports extended, wizard state hooks added (incl. progressActive), hard-block removed, buildVariables() + named handleInstall entry point present.</done>
</task>

<task type="auto">
  <name>Task 2: Render the 5-step Stepper with steps 1-4 (prereqs, quay, connection, credentials)</name>
  <files>app-store-gui/webapp/templates/welcome.html</files>
  <read_first>
    - app-store-gui/webapp/templates/welcome.html (the WelcomeApp render tree at lines 299-496; the StatusIndicator sub-component at lines 80-96 as the model for a new step sub-component; the namespace Select FormControl at lines 358-373; the dark theme at lines 47-78)
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-03, D-04, D-08; Specific Ideas — node prereq pre-block + checkbox)
    - .planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md (Pattern 2 — Jinja {% raw %} guarding rule; "No Analog Found" guidance for Stepper, node-prereq checkbox)
    - .planning/PRD-install-wizard-weka-storage-stack.md (Decision A1 — node prereqs are copy-paste + checkbox, never automated; the exact KubeletConfiguration content: cpuManagerPolicy static, strictCPUReservation true, hugepage config)
  </read_first>
  <action>
    Add a `Stepper` with five `Step`/`StepLabel` entries in order: "Node Prerequisites", "Quay Credentials", "WEKA Connection", "WEKA Credentials", "Review" (D-03). Drive it with `activeStep`. Place Next/Back `Button`s below the active step's body. Render this stepper region in place of (or replacing) the current prerequisites prose + single Initialize button in the left column (welcome.html:317-393); the right-column status `Paper` may remain. When `progressActive` is true the stepper region is hidden in favor of the progress view (30-03 renders that view; this plan only needs to gate the stepper on `!progressActive`).

    Implement step bodies 1-4 (step 5 Review and the validation gate are Task 3):
    - **Step 1 Node Prerequisites (WIZ-02):** a copy-paste `<pre>` block (or `Box component="pre"`) containing the required `KubeletConfiguration` snippet — `cpuManagerPolicy: static`, a `kubeReserved`/`reservedSystemCPUs` line reflecting `strict-cpu-reservation`, plus the hugepage config — and a controlled MUI `Checkbox` via `FormControlLabel` labeled "I have applied node prerequisites on all worker nodes" bound to `nodePrereqAck`. Disable the Next button on this step while `nodePrereqAck` is false. The App Store never writes node config (Decision A1) — this is display + acknowledgment only.
    - **Step 2 Quay Credentials (WIZ-03):** `TextField` for `quay_username`; `TextField type="password"` for `quay_password`; `TextField` for `operator_version` pre-filled with `v1.13.0`.
    - **Step 3 WEKA Connection (WIZ-04):** `TextField` for `join_ip_ports` (helper text: comma-delimited `host:port` list); `TextField` for `weka_image_version`; an `http`/`https` dropdown for `weka_endpoint_scheme` using the existing `FormControl`/`Select`/`MenuItem` pattern from lines 358-373, defaulting to `http`.
    - **Step 4 WEKA Credentials (WIZ-05):** `TextField` for `weka_org` pre-filled `Root`; `TextField` for `weka_username`; `TextField type="password"` for `weka_password`.

    All inputs are controlled, bound to the `form` state object (onChange updates `form[key]`). Each input renders its `fieldErrors[key]` (set in Task 3) via the MUI `error` + `helperText` props. Every new inline `sx={{...}}` / `style={{...}}` object literal MUST be wrapped `sx={% raw %}{{ ... }}{% endraw %}` (Jinja2 template rule — non-negotiable, see Pattern 2). Use `variant="outlined"` inputs and the existing dark theme to match the visual language.
  </action>
  <verify>
    <automated>grep -c 'raw %}' app-store-gui/webapp/templates/welcome.html</automated>
  </verify>
  <acceptance_criteria>
    - `welcome.html` contains a `Stepper` element with five distinct `StepLabel` texts: "Node Prerequisites", "Quay Credentials", "WEKA Connection", "WEKA Credentials", "Review".
    - `welcome.html` contains a `Checkbox` (via `FormControlLabel`) whose label includes "applied node prerequisites" and the step-1 Next button is `disabled` when `nodePrereqAck` is false (grep: `disabled={!nodePrereqAck` or equivalent guard).
    - `welcome.html` contains `TextField` inputs bound to `quay_username`, `quay_password` (type="password"), `operator_version`, `join_ip_ports`, `weka_image_version`, `weka_org`, `weka_username`, `weka_password` (type="password"), and a `Select`/`MenuItem` dropdown for `weka_endpoint_scheme` with `http` and `https` options.
    - Step 1 snippet block contains the literal strings `cpuManagerPolicy` and `static` and a hugepage reference.
    - Every new `sx={{` / `style={{` occurrence is wrapped in `{% raw %}` ... `{% endraw %}` — the established pattern is `sx={% raw %}{{`.
    - The stepper region renders only when `!progressActive` (grep: a guard referencing `progressActive`).
    - Behavioral: loading `/welcome` renders the Stepper at step 0; clicking Next on step 1 without checking the box does nothing (button disabled); checking the box enables Next.
    - No automated UI test exists; acceptance is the source + behavioral assertions above.
  </acceptance_criteria>
  <done>Stepper with 5 labeled steps renders (gated on !progressActive); step bodies 1-4 collect all D-08 fields as controlled inputs; step-1 Next gated by the prereq checkbox; all sx/style literals Jinja-guarded.</done>
</task>

<task type="auto">
  <name>Task 3: Review step + inline validation gate</name>
  <files>app-store-gui/webapp/templates/welcome.html</files>
  <read_first>
    - app-store-gui/webapp/templates/welcome.html (the step bodies and Next/Back buttons added in Task 2; the namespace Select FormControl at 358-373; the buildVariables + handleInstall functions from Task 1)
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-04 validation, D-06 mask, D-09 review/namespace, D-08 fields)
    - .planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md (Pattern 4 — localStorage.selectedNamespace; "No Analog Found" — masked review summary)
  </read_first>
  <action>
    Add a `validateStep(stepIndex)` function that returns a `fieldErrors`-shaped object for the fields on that step and is invoked on Next/Submit click (D-04 — validate on click, NOT on blur). It MUST block forward navigation when it returns any error (do not advance `activeStep`; set `fieldErrors`). Rules (client-side is the sole gate — backend does not check these):
    - Step 2: `quay_username` non-empty; `quay_password` non-empty; `operator_version` matches `^v?\d+\.\d+(\.\d+)?$`.
    - Step 3: `join_ip_ports` non-empty AND every comma-split entry (trimmed) matches `^[^:]+:\d+$`; `weka_image_version` matches `^v?\d+\.\d+(\.\d+)?$`.
    - Step 4: `weka_username` non-empty; `weka_password` non-empty. (`weka_org` defaults to `Root`, not required.)
    Each error message is specific (e.g. "Each endpoint must be host:port", "Use a version like v1.13.0 or 5.1.0.605", "Quay username is required") and surfaces inline via the `error`/`helperText` props wired in Task 2.

    Add **Step 5 Review (WIZ-06, D-09):** a read-only summary listing every submitted field with `quay_password` and `weka_password` rendered as the constant mask string `••••••••` (the actual secret value is NEVER echoed to the DOM beyond the masked password `TextField` on earlier steps). `quay_dockerconfigjson` is NOT shown at all. Include a namespace selector reusing the exact `FormControl`/`Select`/`MenuItem` block from welcome.html:358-373 (writes `selectedNamespace` to `localStorage` per Pattern 4). The Review step's primary button is labeled "Install" and on click runs `validateStep` for steps 2-4 (re-validate all) and, if clean, calls the named `handleInstall` entry point from Task 1 (which flips `showProgress`/`progressActive` — 30-03 fills in its SSE body).
  </action>
  <verify>
    <automated>grep -F '^[^:]+:\d+$' app-store-gui/webapp/templates/welcome.html</automated>
  </verify>
  <acceptance_criteria>
    - `welcome.html` contains the regex literal `^[^:]+:\d+$` (the join_ip_ports per-entry check) and the version-tag regex `^v?\d+\.\d+(\.\d+)?$`.
    - `welcome.html` contains a `validateStep` function invoked from the Next click handler that does NOT advance `activeStep` when errors exist.
    - The Review step renders the mask string `••••••••` for the two password fields and does NOT contain any reference to displaying `quay_dockerconfigjson`.
    - The Review step contains a namespace `Select` reusing the existing FormControl pattern and an Install button whose onClick validates then calls `handleInstall`.
    - Behavioral: on step 3, entering `join_ip_ports` = "nope" (no colon/port) and clicking Next shows an inline error and stays on step 3; entering "192.168.1.1:14000" advances. Entering `operator_version` = "abc" on step 2 blocks Next; "v1.13.0" passes.
    - Behavioral: on the Review step, the password rows show bullets, not the typed secret.
    - No automated UI test exists; acceptance is source + behavioral assertions.
  </acceptance_criteria>
  <done>validateStep blocks invalid forward navigation with specific inline errors; Review step shows masked summary + namespace selector + Install button that gathers buildVariables() via handleInstall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ↔ React in-memory state | Customer-entered secrets (quay_password, weka_password) held in component state |
| browser ↔ localStorage | Only selectedNamespace is persisted; secrets must NOT cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | quay_password / weka_password persisted to localStorage | mitigate | D-02: secrets live only in `form` React state; only `selectedNamespace` is written to localStorage. Task 1 acceptance asserts `localStorage.setItem` references only `selectedNamespace`. |
| T-30-02 | Information Disclosure | Secret echoed in Review summary DOM | mitigate | D-09: Review renders `••••••••` for passwords; `quay_dockerconfigjson` never rendered. Task 3 acceptance asserts the mask string and no dockerconfigjson display. |
| T-30-03 | Tampering | Client-side validation is the SOLE gate (backend does not check host:port / version format) | mitigate | Task 3 implements the D-04 regex gate blocking forward navigation; this is the only enforcement point, so it is implemented (not deferred). Residual: a crafted direct request to /deploy-stream bypasses the form — accepted, as the operator render will fail closed on malformed values and this is an authenticated same-origin internal tool. |
| T-30-04 | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — all MUI components come from the already-loaded CDN UMD bundle; no new dependencies added (D-01). No legitimacy gate required. |
| T-30-SC | Tampering | dependency supply chain | accept | No new packages installed; CDN script tags unchanged. N/A this phase. |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py operator_module/main.py` succeeds (no Python touched, but confirms repo integrity).
- Manual: serve `/welcome`; the 5-step Stepper renders; step gating, validation, and masked Review behave per acceptance criteria above; clicking Install flips to the progress region.
- No new CDN `<script src=` tags; no `localStorage` write of secrets.
</verification>

<success_criteria>
WIZ-01 (5-step form), WIZ-02 (node-prereq snippet + checkbox), WIZ-03 (quay creds), WIZ-04 (WEKA connection), WIZ-05 (WEKA creds), WIZ-06 (masked review + namespace), WIZ-07 (inline validation blocks invalid Next), WIZ-08 (hard-block removed) are all observable in `welcome.html`. The `buildVariables()` helper, named `handleInstall`, and `showProgress`/`progressActive` state form the handoff contract for 30-03's SSE wiring.
</success_criteria>

<output>
Create `.planning/phases/30-wizard-stepper-live-progress/30-02-SUMMARY.md` when done
</output>
