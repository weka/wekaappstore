---
phase: 30-wizard-stepper-live-progress
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - app-store-gui/webapp/templates/welcome.html
autonomous: true
requirements: [PROG-01, PROG-03]
must_haves:
  truths:
    - "After clicking Install, the view shows a per-stage list seeded from the SSE init event, each stage transitioning Pending (grey) → Installing (yellow) → Ready (green) or Failed (red) driven by the component SSE events that 30-01's backend fix now streams for app-store-install"
    - "complete.ok===true arrives only at appStackPhase==Ready (post 30-01) so the success/chain branch is meaningful; complete.ok===false arrives at appStackPhase==Failed and is reachable for both apps"
    - "On a stage failure (complete.ok===false or error event) the customer sees the specific failed-stage error message inline and a Retry button that re-opens the same EventSource with the identical variables"
    - "After app-store-install emits complete.ok===true the client automatically opens a second EventSource to /deploy-stream?app_name=cluster-init and appends its stage(s) to the same list"
    - "When cluster-init emits complete.ok===true (genuinely Ready post 30-01) the client fetches /cluster-status, reads redirect_url, and navigates there (falling back to /)"
    - "Implements decisions D-05 (reuse the blueprint.html SSE consumer for init/component/complete/error), D-06 (component.phase → stage color mapping), D-07 (stage-failure error shown inline with a Retry button), D-10 (automatic cluster-init chain after app-store-install Ready), D-11 (Ready-gated /cluster-status redirect_url navigation), and D-12 (no ClusterInitMiddleware change needed — /deploy-stream and /welcome already exempt)"
  artifacts:
    - path: "app-store-gui/webapp/templates/welcome.html"
      provides: "SSE progress view (init/component/complete/error), failure+retry, cluster-init chain, Ready-gated redirect"
      contains: "EventSource"
  key_links:
    - from: "welcome.html handleInstall"
      to: "/deploy-stream?app_name=app-store-install"
      via: "EventSource with variables=JSON.stringify(buildVariables())"
      pattern: "app-store-install"
    - from: "welcome.html cluster-init complete handler"
      to: "/cluster-status"
      via: "fetch then window.location.href = redirect_url"
      pattern: "cluster-status"
---

<objective>
Fill in the `handleInstall` entry point (created in 30-02) to open the `/deploy-stream` SSE backend, render per-stage install progress (Pending → In-progress → Done/Failed) driven by the `component` events that 30-01's backend fix now streams, handle stage failure with a clear inline error + Retry, then automatically chain the untouched `cluster-init` CR and redirect to the App Store when it reaches Ready.

Purpose: This turns the collected wizard variables (30-02's `buildVariables()`) into a watched, two-phase install with real per-component progress and a seamless Ready-gated redirect — the customer-visible payoff of the milestone. With 30-01 applied, `component` events flow and `complete.ok=true` means genuine `appStackPhase==Ready` for both apps, so the success/chain/redirect branches are correct.
Output: A modified `welcome.html` whose `handleInstall` opens the SSE stream, a unified stage list reflects live phases, failures retry idempotently, and the cluster-init chain + Ready-gated `/cluster-status` redirect complete the flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md
@.planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md
@.planning/phases/30-wizard-stepper-live-progress/30-01-SUMMARY.md
@.planning/phases/30-wizard-stepper-live-progress/30-02-SUMMARY.md

<interfaces>
<!-- SSE event contract emitted by /deploy-stream (main.py:2972-3144) AS CORRECTED BY 30-01 (D-13).
     Default `message` channel framing `data: {json}\n\n` with `: ping\n\n` keepalives — use
     es.onmessage, NOT addEventListener('init',...). -->

init      → { type:'init', items:[name,...], message }        // seed stage list (~11 components for app-store-install)
component → { type:'component', name, phase, message }         // per-stage update; phase ∈ Pending|Installing|Upgrading|Ready|Failed.
                                                               //   POST 30-01: NOW STREAMS for app-store-install (the guard no longer short-circuits).
complete  → { type:'complete', ok:true|false, result, message } // POST 30-01: ok=true fires ONLY at appStackPhase==Ready (main.py:3119-3120);
                                                               //   ok=false fires at appStackPhase==Failed (main.py:3122-3128). Genuine terminal state for BOTH apps.
error     → { type:'error', message }                          // apply-time exception or deadline timeout; hard fail; show message + Retry

cluster-init note: app-store-cluster-init is a multi-component appStack and, POST 30-01,
ALSO reaches the poll loop — so it may emit `component` events too and its `complete` is
likewise Ready-gated. Render its stage(s) in the same list. (If its componentStatus is
coarse, a single "Cluster Init" stage flipping pending→ready on complete is acceptable —
the handler must tolerate both the multi-component and the single-stage case.)

complete.message / component.message are already secret-redacted server-side
(_redact_secrets, main.py:3116/3126) — safe to display verbatim inline (D-07).

EventSource open (app-store-install):
  const params = new URLSearchParams({ app_name: 'app-store-install', variables: JSON.stringify(buildVariables()) });
  new EventSource(`/deploy-stream?${params.toString()}`);

EventSource open (cluster-init, no variables):
  new EventSource(`/deploy-stream?app_name=cluster-init&namespace=${selectedNamespace}`);

Redirect (after cluster-init complete.ok===true — which now genuinely means Ready, post 30-01):
  fetch('/cluster-status?namespace=' + selectedNamespace) → { phase, redirect_url } ; window.location.href = redirect_url || '/';
  NOTE: do NOT add any poll-until-ready loop — complete.ok===true IS the Ready signal now;
  the /cluster-status call is only to read redirect_url (D-11).

From 30-02 (30-02-SUMMARY.md): buildVariables() returns the D-08 variables dict; handleInstall is the
named entry point this plan fills in; showProgress/progressActive flip the view to the progress region;
activeStep/form state already exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage-list progress view + app-store-install SSE consumer (PROG-01)</name>
  <files>app-store-gui/webapp/templates/welcome.html</files>
  <read_first>
    - app-store-gui/webapp/templates/welcome.html (the 30-02 wizard, the named `handleInstall` entry point, `buildVariables`, `showProgress`/`progressActive` state, the right-column status Paper at lines 395-491)
    - app-store-gui/webapp/templates/blueprint.html (lines 279-346 — the canonical SSE consumer + sectionClass; copy this logic, adapt to React state)
    - app-store-gui/webapp/main.py (lines 3080-3132 — the CORRECTED guard + poll loop; confirm component events + Ready/Failed-gated complete after 30-01)
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-05 incl. CORRECTION, D-06 stage colors, D-13 backend fix)
    - .planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md (Pattern 1 — event table, sectionClass, framing notes; Shared Patterns — SSE event contract)
  </read_first>
  <action>
    Add `stages` state: an array of `{ name, phase }` objects (phase one of `pending|installing|upgrading|ready|failed`). Add `installError` state (string|null) for the failed-stage message. Reuse the `progressActive` flag from 30-02 to switch the view to the progress region.

    Replace the minimal `handleInstall` body (from 30-02) with a function that: sets `showProgress`/`progressActive` true, clears `installError`, opens an `EventSource` to `/deploy-stream` with `app_name='app-store-install'` and `variables=JSON.stringify(buildVariables())` (the namespace is carried INSIDE the variables object per 30-02's buildVariables, matching main.py:2990 — it is not a separate query param for this app). Use `es.onmessage` (default `message` channel — do NOT use `addEventListener('init',...)`; keepalive `: ping` comment lines do not fire onmessage so no guard needed).

    Handle the four event types replicating blueprint.html:298-345 in React idiom. POST 30-01 these now genuinely flow for app-store-install:
    - `init`: seed `stages` from `msg.items` (each `{name, phase:'pending'}`).
    - `component`: update the matching stage by `msg.name` to `msg.phase` (lowercased); if `msg.name` is not in the list, append it (mirrors blueprint.html:315-321); stash `msg.message` as the stage's tooltip/title.
    - `complete`: if `msg.ok !== false` (genuine Ready post 30-01), mark all current stages `ready` and trigger the cluster-init chain (Task 2 wires the chain — for this task, on success just mark stages ready and `es.close()`); if `msg.ok === false` (genuine Failed), set `installError = msg.message`, mark the failed stage `failed`, `es.close()`.
    - `error`: set `installError = msg.message`, `es.close()`.
    Also handle `es.onerror` → `es.close()`.

    Render a full-width stage list (replacing the stepper once `progressActive`, per D-03) where each stage row shows its name and a status chip/color via a `stageColor(phase)` map that mirrors blueprint.html's `sectionClass` (D-06): `ready/healthy` → green, `failed/error` → red, `installing`/`upgrading` → yellow (in-progress; note the analog uses yellow for installing — match it and add an `upgrading` case), default/`pending` → grey. Use MUI `sx` color props rather than Tailwind classes; every `sx={{...}}` MUST be Jinja-guarded `sx={% raw %}{{...}}{% endraw %}`. You may reuse the existing LinearProgress/Alert visual elements in the right-column Paper, but the per-stage list is the new required element.
  </action>
  <verify>
    <automated>grep -c 'EventSource' app-store-gui/webapp/templates/welcome.html</automated>
  </verify>
  <acceptance_criteria>
    - `welcome.html` opens an `EventSource` to a URL containing `app_name=app-store-install` (grep: `app-store-install`).
    - `handleInstall` calls `buildVariables()` and `JSON.stringify` and passes it as the `variables` query param.
    - The onmessage handler branches on `msg.type === 'init'`, `'component'`, `'complete'`, `'error'` (grep all four present).
    - A `stages` state array exists and a `stageColor`/`sectionClass`-equivalent function maps `ready`→green, `failed`→red, `installing`→yellow, default→grey.
    - The handler uses `es.onmessage` (NOT `addEventListener('init'` — grep returns nothing for `addEventListener`).
    - All new `sx={{`/`style={{` are wrapped in `{% raw %}`.
    - Behavioral (manual, requires 30-01 applied): clicking Install opens the stream; the stage list seeds from the init event and rows flip color as `component` events arrive; the terminal `complete` arrives only at Ready.
  </acceptance_criteria>
  <done>handleInstall opens the app-store-install SSE stream; init/component/complete/error handled in React; per-stage list renders with phase→color mapping; success branch keyed on the now-Ready-gated complete.</done>
</task>

<task type="auto">
  <name>Task 2: Failure + Retry (PROG-03) and cluster-init chain + Ready-gated redirect (D-10/D-11)</name>
  <files>app-store-gui/webapp/templates/welcome.html</files>
  <read_first>
    - app-store-gui/webapp/templates/welcome.html (the handleInstall + stage list from Task 1; the EXISTING cluster-init EventSource pattern at lines 266-296; the existing redirect logic at lines 182-187; the existing Retry button at line 479)
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-07 failure+retry, D-10 chain, D-11 redirect, D-12 middleware already exempts paths, D-13 — complete is now Ready-gated for both apps)
    - .planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md (Pattern 1 Retry, Pattern 3 chain+redirect)
    - app-store-gui/webapp/main.py (lines 3080-3144 — complete/error emission, Ready/Failed gating; lines 2612-2642 — /cluster-status redirect_url extraction)
  </read_first>
  <action>
    **Failure + Retry (PROG-03, D-07):** When `installError` is set (from a `complete.ok===false` — now genuinely a Failed appStackPhase — or `error` event), render it inline in the progress view (not a modal) — show the failed stage name and the redacted `msg.message` verbatim (it is already secret-safe per main.py:3116/3126). Add a "Retry" `Button` (model: welcome.html:479) whose onClick clears `installError` and re-invokes `handleInstall`, re-opening the same `EventSource` with the identical `buildVariables()` output. Idempotency is guaranteed by the operator CR upsert path (Phase 27 D-09) — no new endpoint, no new variables. Both the `complete.ok===false` and `error` failure paths are kept and reachable post 30-01.

    **Cluster-init chain (D-10):** In the app-store-install `complete` handler, when `msg.ok !== false` (genuine Ready), after `es.close()` open a SECOND `EventSource` to `/deploy-stream?app_name=cluster-init&namespace=${selectedNamespace}` (cluster-init takes no x-variables — omit the `variables` param per D-10). Append cluster-init's stage(s) to the same `stages` list. POST 30-01 cluster-init may emit `component` events (it is a multi-component appStack reaching the poll loop) — handle them via the same `component` branch; if its status is coarse, tolerate a single "Cluster Init" stage that flips pending→ready on its `complete`. Either way the handler must not assume per-component granularity.

    **Redirect (D-11):** When the cluster-init `EventSource` emits `complete.ok===true` (which post 30-01 genuinely means `appStackPhase==Ready`), `fetch('/cluster-status?namespace=' + selectedNamespace)`, read `redirect_url` from the JSON (main.py:2637-2642), then `window.location.href = (data.redirect_url || '/')`. Do NOT add any poll-until-ready loop or premature `/` redirect — the cluster-init `complete.ok===true` IS the Ready signal; the `/cluster-status` call only supplies `redirect_url`. This mirrors the existing redirect mechanic at welcome.html:182-187. If the cluster-init stream emits `complete.ok===false` or `error`, set `installError` and offer Retry (Retry re-runs handleInstall — acceptable since both CRs are idempotent; document the choice in the SUMMARY).

    No backend, operator, or middleware changes IN THIS PLAN — the only backend change is 30-01's guard fix. `ClusterInitMiddleware` already exempts `/deploy-stream`, `/welcome`, and `/cluster-status` (D-12). All new `sx`/`style` literals Jinja-guarded.
  </action>
  <verify>
    <automated>grep -c 'cluster-init' app-store-gui/webapp/templates/welcome.html</automated>
  </verify>
  <acceptance_criteria>
    - `welcome.html` opens a second `EventSource` to a URL containing `app_name=cluster-init` (grep: `app_name=cluster-init`).
    - The app-store-install success branch (`msg.ok !== false`) triggers the cluster-init chain (the cluster-init EventSource is opened from inside the app-store-install complete handler, not from a button).
    - `welcome.html` contains a Retry `Button` whose onClick re-invokes `handleInstall` and clears `installError`.
    - The failed-stage path renders `installError` inline (grep: `installError` referenced in JSX) — not via `alert(` or a `Dialog`.
    - The cluster-init `complete` success path calls `fetch('/cluster-status` and assigns `window.location.href` from `redirect_url` with a `|| '/'` fallback, with NO poll-until-ready loop (grep: no `setInterval` newly added around the redirect; the redirect fires directly off the complete event).
    - cluster-init stage(s) are appended to `stages` and the handler tolerates both component-event and single-stage cases.
    - All new `sx={{`/`style={{` wrapped in `{% raw %}`.
    - Behavioral (manual, requires 30-01 applied): a forced failure shows the specific message + Retry; Retry re-opens the stream; on app-store-install Ready the Cluster Init stage(s) appear; on cluster-init Ready the browser navigates to redirect_url.
  </acceptance_criteria>
  <done>Failure shows inline error + working Retry (both ok=false and error paths); cluster-init chains automatically after app-store-install Ready; Ready-gated redirect to /cluster-status redirect_url on cluster-init complete with no poll-until-ready workaround.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ /deploy-stream (GET query string) | EventSource is GET-only; the `variables` JSON (including quay_password, weka_password) travels in the URL query string |
| /deploy-stream SSE ↔ browser DOM | Per-component and completion messages stream back into the progress view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Information Disclosure | Secrets (quay_password, weka_password) passed as `/deploy-stream` query-string parameters because EventSource only supports GET | accept (documented) | EventSource cannot send a body, so `variables` JSON goes in the URL — this exactly matches the existing battle-tested blueprint.html pattern (lines 279-284). The connection is same-origin to the App Store backend the customer already trusts. Residual risk: secrets may appear in browser history and server access logs. Accepted for v8.0 because (a) it is the established pattern, (b) the App Store is an authenticated internal admin tool, (c) changing to POST would require a new backend route beyond this phase's scope. Flag for a future hardening item (move to POST + token-fetch pattern). |
| T-30-06 | Information Disclosure | Secret values surfaced in the streamed SSE messages displayed inline (D-07) | mitigate | complete.message / component.message are secret-redacted server-side by _redact_secrets (main.py:3116/3126, Phase 29). The client displays them verbatim only because they are already redacted — no client-side secret rendering. Task 2 displays installError (the redacted message) directly. Note: 30-01 widens which apps reach this redacted poll loop but adds no new un-redacted sink (see T-30-09 in 30-01). |
| T-30-07 | Information Disclosure | Secrets persisted client-side across the install/retry cycle | mitigate | buildVariables() reads from in-memory React `form` state only (30-02 D-02). Retry re-reads the same in-memory state; nothing is written to localStorage. No new persistence introduced. |
| T-30-08 | Denial of Service | A failed install leaves a dangling open EventSource | mitigate | Every terminal branch (complete, error, onerror) calls es.close(); Retry opens a fresh stream. No leaked connections. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no new CDN dependencies. N/A. |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py operator_module/main.py` succeeds (no Python touched in this plan).
- Manual on a cluster (with 30-01 applied): complete the wizard → app-store-install stages progress live via component events → on Ready the Cluster Init stage(s) chain → on cluster-init Ready the browser redirects to the App Store URL.
- Manual failure path: forced stage failure (appStackPhase Failed → complete.ok=false) shows the specific redacted message inline + Retry re-runs idempotently.
- grep confirms `app_name=app-store-install`, `app_name=cluster-init`, `/cluster-status`, `EventSource` all present; no `addEventListener`, no `alert(` for errors, no secret written to localStorage, no poll-until-ready loop around the redirect.
</verification>

<success_criteria>
PROG-01 (per-stage Pending→In-progress→Done/Failed from the now-streaming componentStatus SSE events) and PROG-03 (clear specific failure message + Retry, both ok=false and error reachable) are observable. The cluster-init chain (D-10) and Ready-gated redirect (D-11, ROADMAP success criterion 5) complete the two-phase install. Depends on 30-01's backend fix for the events to flow.
</success_criteria>

<output>
Create `.planning/phases/30-wizard-stepper-live-progress/30-03-SUMMARY.md` when done
</output>
