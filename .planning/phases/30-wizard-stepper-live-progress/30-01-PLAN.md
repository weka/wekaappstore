---
phase: 30-wizard-stepper-live-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/webapp/main.py
autonomous: true
requirements: [PROG-01]
must_haves:
  truths:
    - "The /deploy-stream generator reaches the componentStatus poll loop for any appStack CR, including app-store-install and cluster-init (it no longer short-circuits to complete on NAMESPACE_PRESERVING_APPS membership)"
    - "app-store-install streams per-component SSE events (Pending/Installing/Ready/Failed) as the operator patches componentStatus, and complete.ok=true fires only at appStackPhase==Ready"
    - "The poll loop queries the namespace where the namespace-preserving CR actually lives so the lookup does not 404"
    - "Implements decision D-13 (narrow the main.py:3080 guard to `if not cr_name:`, leaving the line-3075 namespace-override skip unchanged) and resolves the D-05 correction (app-store-install and cluster-init now stream component events instead of short-circuiting to complete)"
  artifacts:
    - path: "app-store-gui/webapp/main.py"
      provides: "Corrected line-3080 guard (if not cr_name:) so multi-component appStacks stream component events; namespace-override suppression at line 3075 unchanged"
      contains: "if not cr_name:"
  key_links:
    - from: "main.py /deploy-stream generator guard (~3080)"
      to: "componentStatus poll loop (~3084-3132)"
      via: "guard narrowed from 'not cr_name or app_name in NAMESPACE_PRESERVING_APPS' to 'not cr_name'"
      pattern: "if not cr_name:"
---

<objective>
Fix the `/deploy-stream` SSE generator so `app-store-install` and `cluster-init` actually stream per-component `component` events instead of short-circuiting to a single immediate `complete`. This is the verified precondition for PROG-01 / Success Criterion 4 — without it the frontend progress view (planned in 30-03) has no events to render.

Purpose: The generator at `main.py:3080` conflates two concerns: "do not override the user-selected namespace" (correct, line 3075) and "do not poll componentStatus" (the bug). Both `app-store-install` and `app-store-cluster-init` ARE multi-component appStacks whose operator patches `componentStatus` over time — they must reach the poll loop. (D-13.)
Output: A one-line guard change in `main.py` so any appStack CR reaches the poll loop, with the namespace-match concern handled so the poll does not 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md
@.planning/phases/30-wizard-stepper-live-progress/30-PATTERNS.md

<interfaces>
<!-- The verified blocker. main.py:3075-3132 (read in full before editing). -->

CURRENT (main.py:3080) — short-circuits BEFORE the poll loop for namespace-preserving apps:
    if not cr_name or app_name in NAMESPACE_PRESERVING_APPS:
        yield sse_event({"type": "complete", "ok": True, ...})
        return

REQUIRED (D-13):
    if not cr_name:
        yield sse_event({"type": "complete", "ok": True, ...})
        return

UNCHANGED — the namespace-override suppression stays exactly as-is (main.py:3075):
    ns_for_apply = "" if app_name in NAMESPACE_PRESERVING_APPS else namespace

Namespace-match concern: the poll loop calls
    get_namespaced_custom_object(group="warp.io", version="v1alpha1",
        namespace=namespace, plural="wekaappstores", name=cr_name)   # main.py:3097-3101
For namespace-preserving apps applied with ns_for_apply="", the CR lands in its
manifest-declared metadata.namespace. Both app-store-install and app-store-cluster-init
declare/land in `default`, and the deploy-stream `namespace` variable already defaults to
"default" (main.py:2990). The poll must query the namespace where the CR actually lives.

After this change, for BOTH apps:
  - component events flow as the operator patches componentStatus (main.py:3107-3117)
  - complete.ok=true fires only at appStackPhase==Ready (main.py:3119-3120)
  - complete.ok=false fires at appStackPhase==Failed (main.py:3122-3128)
  - error fires on apply-time exception or timeout (main.py:3129-3142)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the line-3080 guard so appStack CRs reach the componentStatus poll loop (D-13)</name>
  <files>app-store-gui/webapp/main.py</files>
  <read_first>
    - app-store-gui/webapp/main.py (lines 3070-3145 IN FULL — the apply call at 3076, the namespace-override line 3075, the guard at 3080, and the entire poll loop 3084-3132 including the get_namespaced_custom_object call at 3097-3101 and the appStackPhase Ready/Failed/timeout branches)
    - app-store-gui/webapp/main.py (line 191 — NAMESPACE_PRESERVING_APPS definition; line 2990 — how `namespace` is derived from user_vars, defaulting to "default")
    - .planning/phases/30-wizard-stepper-live-progress/30-CONTEXT.md (D-13 — the exact guard change and the namespace-match requirement; D-05 CORRECTION)
  </read_first>
  <action>
    Change the guard at `main.py:3080` from `if not cr_name or app_name in NAMESPACE_PRESERVING_APPS:` to `if not cr_name:`. This is the ONLY change to that guard — non-appStack blueprints (no `cr_name`) still report submission complete immediately; any appStack CR (including `app-store-install` and `cluster-init`) now falls through to the componentStatus poll loop.

    Do NOT touch line 3075 (`ns_for_apply = "" if app_name in NAMESPACE_PRESERVING_APPS else namespace`) — the namespace-override suppression is correct and must stay; these apps keep their fixed per-component `targetNamespace`.

    Ensure the poll loop queries the namespace where the CR actually lives. Verify that the `namespace` variable used in `get_namespaced_custom_object(..., namespace=namespace, name=cr_name)` (main.py:3100) resolves to `default` for namespace-preserving apps — it already does because `namespace` defaults to `"default"` (main.py:2990) and both `app-store-install` and `app-store-cluster-init` declare/land in `default`. If, on reading the manifest, the WekaAppStore CR's `metadata.namespace` differs from this `namespace` value, set the poll's lookup namespace to the CR's declared `metadata.namespace` (read it off the `docs` already parsed at render time, alongside the existing `cr_name` capture at main.py:3062-3068) so the lookup cannot 404. Keep the change minimal — if the declared namespace equals `namespace` (the expected case for both apps), no further change is needed beyond the guard.

    Update the stale comment at main.py:3078-3079 ("Namespace-preserving apps and non-appStack blueprints have no per-component operator status to poll") so it reflects the corrected behavior (only non-appStack blueprints — i.e. no `cr_name` — skip the poll).
  </action>
  <verify>
    <automated>python -m py_compile app-store-gui/webapp/main.py operator_module/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'if not cr_name:' app-store-gui/webapp/main.py` matches the deploy-stream guard line (the `or app_name in NAMESPACE_PRESERVING_APPS` clause is gone from that guard).
    - `grep -c 'app_name in NAMESPACE_PRESERVING_APPS' app-store-gui/webapp/main.py` returns exactly 1 (only the line-3075 `ns_for_apply` use remains; the line-3080 use is removed).
    - Line 3075 `ns_for_apply = "" if app_name in NAMESPACE_PRESERVING_APPS else namespace` is unchanged (grep confirms it still present verbatim).
    - The poll's `get_namespaced_custom_object` call references a namespace that resolves to where the CR lives (`default` for both apps) — confirm by source reading that no hardcoded wrong namespace was introduced.
    - `python -m py_compile app-store-gui/webapp/main.py operator_module/main.py` exits 0.
    - Behavioral (manual, on a cluster): an `app-store-install` deploy-stream now emits one or more `component` events with `name`/`phase` before the terminal `complete`, and `complete.ok=true` arrives only when `appStackPhase==Ready`.
  </acceptance_criteria>
  <done>The line-3080 guard is `if not cr_name:`; line 3075 is unchanged; the poll loop is reachable for app-store-install and cluster-init and queries the correct namespace; py_compile passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator componentStatus ↔ /deploy-stream SSE | Per-component phase/message read from the CR status and streamed to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Information Disclosure | Widening the poll loop to app-store-install/cluster-init could stream component.message values that contain secrets | mitigate | No new exposure: the poll loop already wraps every emitted component message in `_redact_secrets(comp.get("message",""), user_vars)` (main.py:3116) and the Failed message in `_redact_secrets` (main.py:3126), per Phase 29. The guard change only makes already-redacted events reachable for two more apps — it adds no new un-redacted sink. |
| T-30-10 | Denial of Service | The poll loop runs until appStackPhase terminal or deadline; widening it to long-running installs holds an SSE connection open longer | accept | The per-blueprint deadline (x-deploy-timeout 2700s for app-store-install, Phase 29 PROG-02) and keepalive pings (main.py:3095) already bound and sustain the connection. This is the intended behavior for a long install; no change. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs; single-line guard change to existing code. N/A. |
</threat_model>

<verification>
- `python -m py_compile app-store-gui/webapp/main.py operator_module/main.py` succeeds.
- `grep -c 'app_name in NAMESPACE_PRESERVING_APPS' app-store-gui/webapp/main.py` == 1 (line 3075 only).
- Manual on a cluster: app-store-install deploy-stream emits component events and a Ready-gated complete.
</verification>

<success_criteria>
PROG-01's precondition is met: the backend streams per-component events for app-store-install and cluster-init. The frontend progress view (30-03) can now render real per-stage progress and Ready-gated success/failure branches.
</success_criteria>

<output>
Create `.planning/phases/30-wizard-stepper-live-progress/30-01-SUMMARY.md` when done
</output>
