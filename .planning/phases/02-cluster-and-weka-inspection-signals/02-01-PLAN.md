---
phase: 02-cluster-and-weka-inspection-signals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_inspection_contract.py
  - app-store-gui/webapp/planning/models.py
  - app-store-gui/webapp/planning/validator.py
autonomous: true
requirements:
  - CLSTR-05
  - PLAN-04
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - Phase 2 has a typed inspection and fit-signal contract before service extraction starts.
    - The contract makes per-domain completeness and blocker semantics explicit instead of leaving them as ad hoc dict conventions.
    - Missing required inspection facts cause fit decisions to fail closed in tests and validation logic.
  artifacts:
    - app-store-gui/webapp/planning/models.py
    - app-store-gui/webapp/planning/validator.py
    - app-store-gui/tests/planning/test_inspection_contract.py
  key_links:
    - app-store-gui/webapp/planning/models.py defines the planner-facing fit findings and inspection snapshot types.
    - app-store-gui/webapp/planning/validator.py rejects malformed or incomplete fit payloads before YAML generation or apply handoff.
---

<objective>
Establish the typed inspection and fit-signal contract for Phase 2.

Purpose: Lock the planner-facing data model for inspection freshness, per-domain status, blockers, and fail-closed fit findings before implementation spreads through backend services.
Output: Extended planning models and validator logic, plus focused tests proving the new contract rejects partial or malformed data where blocking semantics are required.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-CONTEXT.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-RESEARCH.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-VALIDATION.md
@app-store-gui/webapp/planning/models.py
@app-store-gui/webapp/planning/validator.py
@app-store-gui/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed inspection-domain and fit-finding models</name>
  <files>app-store-gui/webapp/planning/models.py</files>
  <action>Extend the Phase 1 planning contract with explicit types for inspection snapshots, per-domain status, freshness metadata, blocker details, and richer `fit_findings`. Preserve backwards-compatible intent where reasonable, but make the Phase 2 planner contract strong enough to express GPU type and memory, CPU and RAM availability, namespace and storage-class visibility, and WEKA storage context without relying on free-form notes.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_contract.py -q</verify>
  <done>The typed model can represent all Phase 2 fit domains and carries enough metadata for freshness, completeness, and blocking decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten validation around partial-data and fail-closed rules</name>
  <files>app-store-gui/webapp/planning/validator.py</files>
  <action>Update validation logic so fit payloads with missing required fields, invalid domain statuses, or malformed blocker metadata fail deterministically. Enforce the Phase 2 rule that required inspection data cannot silently degrade into an assumed-fit result, especially for GPU specifics and WEKA-dependent storage plans.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_contract.py -q</verify>
  <done>The validator rejects incomplete or malformed fit structures with deterministic errors that make SAFE-04 enforceable.</done>
</task>

<task type="auto">
  <name>Task 3: Seed reusable contract fixtures and focused tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_inspection_contract.py</files>
  <action>Add canonical fixtures for complete, partial, and blocked inspection snapshots and planner-facing fit findings. Write tests that prove the contract supports all Phase 2 domains and that blocked findings are emitted when required facts are missing, stale, or explicitly partial.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_contract.py -q</verify>
  <done>The Phase 2 contract is covered by reusable fixtures and a fast test loop that later plans can rely on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_inspection_contract.py -q`
- [ ] The fit contract carries per-domain status, freshness or snapshot metadata, and explicit blockers instead of free-form notes alone
- [ ] Missing required inspection facts cannot validate as an assumed-fit result
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The planner-facing fit model now covers Phase 2 domains and fail-closed semantics
- Later Phase 2 implementation plans can consume a stable typed contract rather than inventing response fields ad hoc
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cluster-and-weka-inspection-signals/02-01-SUMMARY.md`
</output>
