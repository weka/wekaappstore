---
phase: 02-cluster-and-weka-inspection-signals
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/inspection/__init__.py
  - app-store-gui/webapp/inspection/cluster.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_cluster_inspection.py
autonomous: true
requirements:
  - CLSTR-01
  - CLSTR-02
  - CLSTR-03
user_setup: []
must_haves:
  truths:
    - Kubernetes inspection for namespaces, storage classes, CPU, RAM, and GPU inventory is extracted into a bounded read-only service instead of staying embedded in one large FastAPI file.
    - GPU inventory includes model and memory details when visible, and partial metadata is surfaced explicitly rather than silently dropped.
    - CPU and RAM availability are computed in the same planner-facing snapshot shape used by later fit-signal derivation.
  artifacts:
    - app-store-gui/webapp/inspection/cluster.py
    - app-store-gui/webapp/main.py
    - app-store-gui/tests/planning/test_cluster_inspection.py
  key_links:
    - app-store-gui/webapp/inspection/cluster.py becomes the shared source of truth for read-only Kubernetes inspection.
    - app-store-gui/webapp/main.py uses the extracted inspection service instead of bespoke inline aggregation.
---

<objective>
Extract bounded Kubernetes inspection for namespaces, storage classes, CPU, RAM, and GPU inventory.

Purpose: Turn the existing cluster-status logic into a reusable planner-grade inspection service that exposes bounded snapshots instead of one-off UI fields.
Output: Shared inspection module, backend integration, and mocked tests covering namespace, storage class, CPU, RAM, GPU count, GPU model, and GPU memory extraction.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-CONTEXT.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-RESEARCH.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-VALIDATION.md
@app-store-gui/webapp/main.py
@app-store-gui/webapp/planning/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Kubernetes inspection helpers into a shared module</name>
  <files>app-store-gui/webapp/inspection/__init__.py, app-store-gui/webapp/inspection/cluster.py, app-store-gui/webapp/main.py</files>
  <action>Move the read-only namespace, storage-class, node, pod-request, CPU, GPU, and related aggregation logic out of `main.py` into a dedicated inspection module. Keep the implementation bounded to Kubernetes client reads already used by the repo; do not introduce shelling out or mutation paths.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_cluster_inspection.py -q</verify>
  <done>A shared inspection service exists and `main.py` uses it instead of duplicating the aggregation logic inline.</done>
</task>

<task type="auto">
  <name>Task 2: Add planner-grade CPU, RAM, and GPU inventory extraction</name>
  <files>app-store-gui/webapp/inspection/cluster.py, app-store-gui/tests/planning/test_cluster_inspection.py</files>
  <action>Extend the extracted service so it emits the Phase 2 inventory shape: namespaces, storage classes, CPU totals and free capacity, RAM totals and free capacity, and GPU inventory with count, model, and memory when visible. Treat missing GPU model or memory as explicit partial-data state rather than best-effort guessing.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_cluster_inspection.py -q</verify>
  <done>The cluster inspection service returns bounded planner-grade snapshots for CLSTR-01 through CLSTR-03 without hiding incomplete GPU metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Prove bounded read-only behavior with mocked tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_cluster_inspection.py</files>
  <action>Add mocked tests that verify the service reads only through the Kubernetes Python client, returns deterministic results from representative fixture topologies, and never depends on live cluster state or shell commands. Include coverage for mixed CPU/GPU nodes and failed list operations surfacing partial domain status.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_cluster_inspection.py -q</verify>
  <done>The inspection service is testable, deterministic, and bounded to read-only Kubernetes API access.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_cluster_inspection.py -q`
- [ ] `main.py` no longer owns the primary CPU, RAM, GPU, namespace, and storage-class aggregation logic directly
- [ ] GPU inventory exposes partial-data status when model or memory cannot be derived
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend can produce bounded Kubernetes inspection snapshots for Phase 2 domains
- Cluster inspection logic becomes reusable input for fit-finding and planner integration rather than a settings-only helper
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cluster-and-weka-inspection-signals/02-02-SUMMARY.md`
</output>
