---
phase: 02-cluster-and-weka-inspection-signals
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/planning/__init__.py
  - app-store-gui/webapp/planning/models.py
  - app-store-gui/webapp/planning/validator.py
  - app-store-gui/webapp/planning/inspection_tools.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_inspection_integration.py
  - app-store-gui/tests/planning/test_plan_contract.py
autonomous: true
requirements:
  - CLSTR-05
  - PLAN-04
  - SAFE-01
  - SAFE-02
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - Planner-visible fit findings are derived from bounded inspection snapshots rather than handwritten notes.
    - Correlation IDs and stage-classified failures are propagated through inspection and planning seams consistently enough to debug and audit decisions.
    - Storage-dependent or GPU-dependent plans fail closed when required inspection facts are partial, stale, or unavailable.
  artifacts:
    - app-store-gui/webapp/main.py
    - app-store-gui/webapp/planning/inspection_tools.py
    - app-store-gui/tests/planning/test_inspection_integration.py
  key_links:
    - app-store-gui/webapp/main.py exposes the integrated inspection and fit-signal responses with correlation metadata.
    - app-store-gui/tests/planning/test_inspection_integration.py proves end-to-end Phase 2 semantics across contract, service, and request layers.
---

<objective>
Integrate fit findings, correlation IDs, and stage-classified failures into the backend planning seam.

Purpose: Complete Phase 2 by wiring bounded inspection snapshots into planner-facing fit findings and request-level diagnostics without disturbing the Phase 1 YAML or apply contract.
Output: Backend integration for fit findings and inspection responses, correlation ID propagation, stage-tagged error surfaces, and end-to-end mocked tests.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-CONTEXT.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-RESEARCH.md
@app-store-gui/webapp/main.py
@app-store-gui/webapp/planning/models.py
@app-store-gui/webapp/planning/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive planner-facing fit findings from inspection snapshots</name>
  <files>app-store-gui/webapp/planning/models.py, app-store-gui/webapp/planning/validator.py, app-store-gui/webapp/planning/__init__.py</files>
  <action>Implement the Phase 2 translation layer from bounded inspection snapshots into planner-facing fit findings that include per-domain status, freshness, blockers, and notes. Ensure GPU-dependent and storage-dependent plans fail closed when required facts are incomplete, and keep the fit surface compatible with later chat-planning work rather than locking it to one endpoint only.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_integration.py -q</verify>
  <done>The planner can consume structured fit findings derived from inspection snapshots instead of relying on free-form fit notes.</done>
</task>

<task type="auto">
  <name>Task 2: Add correlation ID propagation and stage-classified failures</name>
  <files>app-store-gui/webapp/main.py, app-store-gui/webapp/planning/inspection_tools.py</files>
  <action>Add a lightweight correlation-ID and diagnostics layer so planning-session requests, validation responses, and inspection failures can be traced consistently. Classify failures by at least the roadmap stages `inspection`, `validation`, `yaml_generation`, and `apply_handoff`, and surface those stages in deterministic response structures or logs.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_integration.py -q</verify>
  <done>The backend emits stable correlation identifiers and stage-tagged failures that satisfy SAFE-01 and SAFE-02 without broad infrastructure changes.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the Phase 2 behavior with end-to-end mocked tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_inspection_integration.py, app-store-gui/tests/planning/test_plan_contract.py</files>
  <action>Write integration tests that combine the extended contract, cluster inspection, WEKA inspection, and request-level response handling. Include assertions for freshness metadata, fail-closed blockers, correlation ID continuity, and stage-classified failures while keeping the test environment fully mocked and deterministic.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_inspection_integration.py tests/planning/test_plan_contract.py -q</verify>
  <done>The final Phase 2 integration path is covered by deterministic tests that prove the goal and success criteria rather than only module existence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_inspection_integration.py -q`
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_plan_contract.py -q`
- [ ] Failures are tagged with a stage and correlation metadata is stable across the inspected response path
- [ ] GPU- and WEKA-dependent fit findings fail closed when required inspection domains are partial or unavailable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Planner-facing fit findings now include the Phase 2 inspection signals and safety metadata
- Request-level diagnostics can distinguish inspection, validation, YAML generation, and apply handoff failures
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cluster-and-weka-inspection-signals/02-04-SUMMARY.md`
</output>
