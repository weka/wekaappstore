---
phase: 02-cluster-and-weka-inspection-signals
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/inspection/weka.py
  - app-store-gui/webapp/planning/inspection_tools.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_weka_inspection.py
autonomous: true
requirements:
  - CLSTR-04
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - WEKA storage inspection is exposed through a bounded read-only adapter, not through unrestricted cluster or shell access.
    - Filesystem inventory and capacity data are surfaced when operator-visible sources provide them, and blocked or partial states are explicit when they do not.
    - NemoClaw-facing inspection access is auditable and limited to supported inspection intents.
  artifacts:
    - app-store-gui/webapp/inspection/weka.py
    - app-store-gui/webapp/planning/inspection_tools.py
    - app-store-gui/tests/planning/test_weka_inspection.py
  key_links:
    - app-store-gui/webapp/inspection/weka.py provides the bounded WEKA inspection seam.
    - app-store-gui/webapp/planning/inspection_tools.py defines the read-only, auditable tool surface consumed by planning flows.
---

<objective>
Add bounded WEKA inspection and a safe NemoClaw-facing inspection tool surface.

Purpose: Surface WEKA capacity and filesystem inventory through a narrow read-only adapter that planning code can trust without expanding into generic WEKA administration.
Output: WEKA inspection module, bounded tool wrapper, backend integration points, and mocked tests for complete, partial, and unavailable WEKA data.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-CONTEXT.md
@.planning/phases/02-cluster-and-weka-inspection-signals/02-RESEARCH.md
@app-store-gui/webapp/main.py
@weka-csi-config/weka-operator/crds/weka.weka.io_wekaclusters.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a bounded WEKA inspection adapter</name>
  <files>app-store-gui/webapp/inspection/weka.py, app-store-gui/webapp/main.py</files>
  <action>Create a WEKA inspection module that reads operator-visible Kubernetes resources or other already-available repo-safe sources to gather storage capacity, free capacity, and filesystem inventory. Keep it strictly read-only and return explicit partial or unavailable domain states when required fields are absent.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_weka_inspection.py -q</verify>
  <done>The backend has a bounded WEKA inspection seam that can surface storage capacity and filesystem inventory without mutating cluster state or guessing missing facts.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap inspection access behind an auditable planning tool surface</name>
  <files>app-store-gui/webapp/planning/inspection_tools.py, app-store-gui/webapp/inspection/weka.py</files>
  <action>Add a narrow planner-facing tool wrapper that exposes only supported inspection operations and records enough request metadata to be auditable. The wrapper must not accept arbitrary Kubernetes, WEKA, or shell commands; it should call only shared bounded inspection services.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_weka_inspection.py -q</verify>
  <done>NemoClaw-facing inspection access is bounded to a small, typed, read-only API that can be audited and reviewed.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic WEKA inspection tests for complete and partial states</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_weka_inspection.py</files>
  <action>Seed fixtures for operator-present, operator-missing, complete WEKA status, and partial filesystem metadata cases. Write tests that prove the adapter returns the expected capacity and filesystem inventory when data exists and explicit blockers or partial states when data is incomplete.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_weka_inspection.py -q</verify>
  <done>The WEKA inspection seam is deterministic under mocks and enforces the bounded-tool safety model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_weka_inspection.py -q`
- [ ] The WEKA inspection path is read-only and bounded to shared service functions
- [ ] Missing WEKA capacity or filesystem facts produce explicit partial or blocked states rather than assumed fit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend can produce bounded WEKA storage inspection data for planning
- NemoClaw-facing inspection access remains auditable and read-only
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cluster-and-weka-inspection-signals/02-03-SUMMARY.md`
</output>
