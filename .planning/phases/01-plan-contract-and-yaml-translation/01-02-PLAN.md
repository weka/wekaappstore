---
phase: 01-plan-contract-and-yaml-translation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app-store-gui/webapp/planning/__init__.py
  - app-store-gui/webapp/planning/models.py
  - app-store-gui/webapp/planning/validator.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_plan_contract.py
autonomous: true
requirements:
  - PLAN-02
  - PLAN-03
  - PLAN-06
  - PLAN-07
user_setup: []
must_haves:
  truths:
    - The backend has a typed structured-plan contract that is narrower than raw YAML and captures the Phase 1 required fields.
    - Validation rejects malformed or unsupported structured plans with deterministic, testable error payloads.
    - Validation distinguishes hard blockers from safe normalization warnings without silently rewriting install-critical intent.
  artifacts:
    - app-store-gui/webapp/planning/models.py
    - app-store-gui/webapp/planning/validator.py
    - app-store-gui/tests/planning/test_plan_contract.py
  key_links:
    - app-store-gui/webapp/planning/models.py defines the data shape that app-store-gui/webapp/planning/validator.py enforces.
    - app-store-gui/tests/planning/test_plan_contract.py proves the validator behavior against the shared fixtures in app-store-gui/tests/conftest.py.
---

<objective>
Implement the typed structured-plan contract and layered validator for Phase 1.

Purpose: Make the backend authoritative for plan shape and repo/operator rule enforcement before any YAML compilation or apply handoff is allowed.
Output: Planning models, validator logic, and contract tests covering accepted plans, warning-producing normalization, and deterministic rejection cases.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-CONTEXT.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-RESEARCH.md
@weka-app-store-operator-chart/templates/crd.yaml
@operator_module/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the structured planning models</name>
  <files>app-store-gui/webapp/planning/__init__.py, app-store-gui/webapp/planning/models.py</files>
  <action>Create typed plan models for the Phase 1 contract, including blueprint family, namespace strategy, components, prerequisites, fit findings, unresolved questions, reasoning summary, and warning/result structures. Keep the shape intentionally narrower than arbitrary YAML and model only what is required to generate one canonical `WekaAppStore` resource.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_plan_contract.py -q -k models</verify>
  <done>The planning package exports typed models that can express valid structured plans and invalid edge cases used by the tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement layered validator behavior</name>
  <files>app-store-gui/webapp/planning/validator.py, app-store-gui/webapp/planning/models.py</files>
  <action>Implement layered validation for schema rules, repo/operator contract rules, and normalization rules described in the phase research. Enforce unique component names, valid dependency references, exactly one deployment method per component, supported readiness-check types, and blocking unresolved install-critical questions. Expose deterministic errors and explicit warnings rather than broad exceptions or silent normalization.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_plan_contract.py -q</verify>
  <done>Validator entrypoints return stable success, warning, and error structures for the full Phase 1 contract surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the contract with focused tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_plan_contract.py</files>
  <action>Fill out the contract test suite to cover valid plans, malformed plans, unsupported planner output, safe normalization warnings, and repo/operator validation failures. Keep the assertions concrete enough that future refactors cannot change error semantics accidentally.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_plan_contract.py -q</verify>
  <done>The contract suite passes and directly demonstrates coverage for PLAN-02, PLAN-03, PLAN-06, and PLAN-07.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_plan_contract.py -q`
- [ ] The validator only normalizes explicitly allowed defaults and emits warnings when it does so
- [ ] Invalid dependency, readiness, and deployment-method cases return deterministic validation output
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Structured plans are validated before YAML generation is attempted
- The contract test suite captures both acceptance and rejection behavior for Phase 1 plan payloads
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plan-contract-and-yaml-translation/01-02-SUMMARY.md`
</output>
