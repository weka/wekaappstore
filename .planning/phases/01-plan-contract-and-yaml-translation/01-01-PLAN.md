---
phase: 01-plan-contract-and-yaml-translation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app-store-gui/requirements.txt
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_plan_contract.py
  - app-store-gui/tests/planning/test_compiler.py
  - app-store-gui/tests/planning/test_apply_gateway.py
autonomous: true
requirements:
  - PLAN-06
  - PLAN-07
  - PLAN-08
  - APPLY-06
  - APPLY-07
user_setup: []
must_haves:
  truths:
    - Phase 1 outcomes can be verified through automated tests instead of ad hoc manual checks alone.
    - Validation, canonical YAML generation, and apply-handoff behavior are all exercised against shared canonical plan fixtures.
    - Later Phase 1 implementation plans have a stable verification loop that proves contract and runtime-path behavior, not just scaffolding presence.
  artifacts:
    - app-store-gui/tests/conftest.py
    - app-store-gui/tests/planning/test_plan_contract.py
    - app-store-gui/tests/planning/test_compiler.py
    - app-store-gui/tests/planning/test_apply_gateway.py
  key_links:
    - app-store-gui/requirements.txt includes pytest so the Phase 1 suite can run in CI or local development.
    - app-store-gui/tests/conftest.py provides reusable valid and invalid plan payloads consumed by all planning tests.
---

<objective>
Establish the Phase 1 test harness and shared planning fixtures before implementation work lands.

Purpose: Give all later plans a stable, fast verification loop and shared payload fixtures so Phase 1 can be executed with deterministic feedback.
Output: Pytest dependency entry, planning test tree, and common fixtures covering valid plans, invalid plans, canonical YAML expectations, and apply-gateway seam inputs.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-CONTEXT.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-RESEARCH.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-VALIDATION.md
@app-store-gui/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Python planning test entrypoint</name>
  <files>app-store-gui/requirements.txt</files>
  <action>Add `pytest` to the GUI Python dependencies in the least disruptive way possible. Do not introduce unrelated packaging changes or broader repo-level tooling; Phase 1 only needs an app-local test runner that later plans can invoke with simple `pytest` commands.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest --version</verify>
  <done>`pytest` is installable from `app-store-gui/requirements.txt` and the command resolves without adding unrelated dependency churn.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared planning fixtures and placeholder suites</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_plan_contract.py, app-store-gui/tests/planning/test_compiler.py, app-store-gui/tests/planning/test_apply_gateway.py</files>
  <action>Create the `tests/planning/` tree and a `conftest.py` that defines canonical valid-plan payloads, invalid-plan variants, expected warning cases, and representative compiled `WekaAppStore` documents. Seed each test module with concrete Phase 1 assertions that later plans will fill in, rather than empty placeholders, so the suite expresses the intended contract from the start.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning -q</verify>
  <done>The test tree exists, fixtures are reusable across modules, and the initial suite can execute even if some tests are marked or structured around not-yet-implemented modules.</done>
</task>

<task type="auto">
  <name>Task 3: Align the seeded tests with the validation contract</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_plan_contract.py, app-store-gui/tests/planning/test_compiler.py, app-store-gui/tests/planning/test_apply_gateway.py</files>
  <action>Make sure the seeded tests encode the exact Phase 1 boundary from the roadmap and validation notes: deterministic validation failures, canonical single-resource YAML output, and shared apply-path handoff. Avoid writing tests for chat UX, cluster inspection, or coexistence analysis because those belong to later phases.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_plan_contract.py tests/planning/test_compiler.py tests/planning/test_apply_gateway.py -q</verify>
  <done>The initial test corpus cleanly maps to Phase 1 requirements and gives downstream plans a concrete target surface to implement against.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest --version`
- [ ] `cd app-store-gui && python -m pytest tests/planning -q`
- [ ] The created fixtures and test names clearly map to PLAN-06, PLAN-07, PLAN-08, APPLY-06, and APPLY-07 only
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Later Phase 1 plans can rely on a real `tests/planning/` harness instead of creating their own one-off fixtures
- The seeded test modules encode the Phase 1 contract without bleeding into later roadmap phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plan-contract-and-yaml-translation/01-01-SUMMARY.md`
</output>
