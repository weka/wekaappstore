---
phase: 01-plan-contract-and-yaml-translation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app-store-gui/webapp/planning/__init__.py
  - app-store-gui/webapp/planning/apply_gateway.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_apply_gateway.py
autonomous: true
requirements:
  - APPLY-06
  - APPLY-07
user_setup: []
must_haves:
  truths:
    - Planner-generated YAML can be handed off through the same backend document-apply path as existing file-backed and rendered-content YAML.
    - The shared apply gateway preserves the existing `WekaAppStore` CRD and operator reconciliation path rather than creating planner-specific execution logic.
    - Canonical planner output preserves existing namespace and custom-resource apply semantics during backend handoff.
  artifacts:
    - app-store-gui/webapp/planning/apply_gateway.py
    - app-store-gui/tests/planning/test_apply_gateway.py
  key_links:
    - app-store-gui/webapp/planning/apply_gateway.py encapsulates the document loading, namespace normalization, and CustomObjectsApi handoff currently duplicated in app-store-gui/webapp/main.py.
    - app-store-gui/tests/planning/test_apply_gateway.py proves the gateway preserves `WekaAppStore` runtime compatibility under mocked apply conditions.
---

<objective>
Extract a shared backend apply gateway for canonical YAML handoff.

Purpose: Remove duplicated apply behavior and create a single reusable handoff service that planner-generated YAML can use without bypassing the current runtime path.
Output: A planning apply-gateway module plus seam tests that verify `WekaAppStore` documents still flow through the existing backend/operator contract.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-CONTEXT.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-RESEARCH.md
@app-store-gui/webapp/main.py
@operator_module/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract document-apply primitives into a gateway module</name>
  <files>app-store-gui/webapp/planning/__init__.py, app-store-gui/webapp/planning/apply_gateway.py</files>
  <action>Create a reusable gateway that accepts already-loaded or rendered YAML content, applies the existing namespace override rules, preserves cluster-scoped handling, and routes `WekaAppStore` resources through `CustomObjectsApi`. Keep the gateway implementation tightly aligned with the current backend behavior so later wiring can be a narrow refactor instead of a semantic rewrite.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_apply_gateway.py -q -k gateway</verify>
  <done>The new gateway module can normalize and apply representative documents without depending on planner-specific endpoint code.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve runtime-path semantics in mocked seam tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_apply_gateway.py</files>
  <action>Add mocked tests that prove the gateway routes canonical `warp.io/v1alpha1` `WekaAppStore` documents through the namespaced custom-resource path, preserves non-CR fallback behavior, and keeps component target namespaces consistent with the existing backend rules. Make sure the assertions guard against introducing an alternate planner-only execution model.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_apply_gateway.py -q</verify>
  <done>The apply-gateway suite demonstrates compatibility with the current backend/operator contract for canonical planner output.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the extracted gateway ready for thin backend integration</name>
  <files>app-store-gui/webapp/planning/apply_gateway.py, app-store-gui/tests/planning/test_apply_gateway.py</files>
  <action>Shape the gateway API so `main.py` can later switch both file-backed and rendered-content apply helpers over to it with minimal churn. Avoid prematurely editing `main.py` in this plan; preserve file ownership so the final integration plan can wire compiler output and gateway usage in one coherent change.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_apply_gateway.py -q</verify>
  <done>The gateway surface is stable enough that `main.py` can adopt it in a follow-up plan without redesigning the extracted service.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_apply_gateway.py -q`
- [ ] The gateway exposes one apply path for canonical YAML content and does not invent planner-only runtime branches
- [ ] Mocked tests prove `WekaAppStore` resources still target the existing CRD/operator path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Duplicated apply semantics are captured in one reusable gateway module
- The extracted gateway is ready to become the single handoff point for file-backed, rendered, and planner-generated YAML
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plan-contract-and-yaml-translation/01-03-SUMMARY.md`
</output>
