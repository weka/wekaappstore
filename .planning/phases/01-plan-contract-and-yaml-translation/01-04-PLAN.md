---
phase: 01-plan-contract-and-yaml-translation
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - app-store-gui/webapp/main.py
  - app-store-gui/webapp/planning/__init__.py
  - app-store-gui/webapp/planning/compiler.py
  - app-store-gui/webapp/planning/models.py
  - app-store-gui/webapp/planning/validator.py
  - app-store-gui/tests/conftest.py
  - app-store-gui/tests/planning/test_compiler.py
  - app-store-gui/tests/planning/test_apply_gateway.py
  - app-store-gui/tests/planning/test_plan_contract.py
autonomous: true
requirements:
  - PLAN-08
  - APPLY-06
  - APPLY-07
user_setup: []
must_haves:
  truths:
    - The backend can turn a validated structured plan into one canonical `WekaAppStore` YAML preview deterministically.
    - Planner-generated output reaches the existing backend apply path through the shared gateway rather than a new execution model.
    - Equivalent valid plans render stable canonical YAML while preserving explicit valid namespace and component intent.
  artifacts:
    - app-store-gui/webapp/planning/compiler.py
    - app-store-gui/webapp/main.py
    - app-store-gui/tests/planning/test_compiler.py
  key_links:
    - app-store-gui/webapp/main.py validates structured plans, invokes app-store-gui/webapp/planning/compiler.py, and hands canonical YAML to app-store-gui/webapp/planning/apply_gateway.py.
    - app-store-gui/tests/planning/test_compiler.py proves canonical YAML generation against the same typed plan contract implemented in app-store-gui/webapp/planning/models.py.
---

<objective>
Compile validated plans into canonical `WekaAppStore` YAML and wire the backend handoff.

Purpose: Complete the Phase 1 flow from validated structured plan to canonical YAML preview and shared apply-path submission without changing the operator runtime model.
Output: Compiler module, deterministic YAML tests, and backend wiring that uses the validator and shared apply gateway for planner-generated plans.
</objective>

<execution_context>
@/Users/christopherjenkins/.codex/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-CONTEXT.md
@.planning/phases/01-plan-contract-and-yaml-translation/01-RESEARCH.md
@weka-app-store-operator-chart/templates/crd.yaml
@app-store-gui/webapp/main.py
@app-store-gui/webapp/planning/models.py
@app-store-gui/webapp/planning/validator.py
@app-store-gui/webapp/planning/apply_gateway.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical `WekaAppStore` compiler</name>
  <files>app-store-gui/webapp/planning/compiler.py, app-store-gui/webapp/planning/models.py, app-store-gui/webapp/planning/validator.py</files>
  <action>Implement compilation from validated structured plan objects into a canonical Python dict for exactly one `warp.io/v1alpha1` `WekaAppStore` resource, then render stable YAML from that dict. Preserve explicit valid user/planner intent, inject only allowed defaults, and keep field ordering deterministic so equivalent plans produce equivalent previews.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_compiler.py -q</verify>
  <done>The compiler can emit canonical dict and YAML outputs for representative valid plans, and it refuses to compile plans that still contain blocking validation issues.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the FastAPI backend to validate, compile, and apply through shared services</name>
  <files>app-store-gui/webapp/main.py, app-store-gui/webapp/planning/__init__.py</files>
  <action>Add or refactor backend entrypoints in `main.py` so structured plan payloads are validated first, then compiled into canonical YAML previews, and finally submitted through the shared apply gateway when approved. Reuse the gateway for existing rendered-content and file-backed apply flows where practical, but keep the Phase 1 scope narrow: no chat session UX, inspection tooling, or approval UX beyond what is needed for backend handoff.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning/test_plan_contract.py tests/planning/test_compiler.py tests/planning/test_apply_gateway.py -q</verify>
  <done>`main.py` uses the new planning services instead of planner-specific inline logic, and canonical YAML handoff stays on the existing runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Lock determinism and handoff behavior with end-to-end Phase 1 tests</name>
  <files>app-store-gui/tests/conftest.py, app-store-gui/tests/planning/test_compiler.py, app-store-gui/tests/planning/test_apply_gateway.py, app-store-gui/tests/planning/test_plan_contract.py</files>
  <action>Finish the tests so they prove canonical YAML output, warning-emitting normalization, rejection of malformed plans, and backend handoff through the shared apply gateway. Include assertions that the compiler emits a single `WekaAppStore` document and that the apply seam does not bypass the existing CRD/operator model.</action>
  <verify>cd app-store-gui &amp;&amp; python -m pytest tests/planning -q</verify>
  <done>The full Phase 1 planning suite passes and demonstrates the end-to-end contract from structured plan validation to canonical YAML handoff.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app-store-gui && python -m pytest tests/planning/test_compiler.py -q`
- [ ] `cd app-store-gui && python -m pytest tests/planning -q`
- [ ] Canonical output is exactly one `WekaAppStore` resource with stable field ordering and explicit valid intent preserved
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Valid structured plans compile into deterministic canonical YAML previews
- Planner-generated YAML is handed to the same backend/operator runtime path used by existing apply flows
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plan-contract-and-yaml-translation/01-04-SUMMARY.md`
</output>
