---
phase: 13-kubernetes-manifests-and-sidecar-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/agent-sandbox/mcp-rbac.yaml
  - k8s/agent-sandbox/mcp-skill-configmap.yaml
autonomous: true
requirements:
  - K8S-02
  - NCLAW-04

must_haves:
  truths:
    - "ServiceAccount weka-mcp-server-sa exists with automountServiceAccountToken enabled"
    - "ClusterRole grants minimum permissions for all 8 MCP tools (read cluster-wide, write WekaAppStore CRs)"
    - "ClusterRoleBinding binds ClusterRole to SA in wekaappstore namespace"
    - "SKILL.md content is available as a ConfigMap for mounting into the OpenClaw container"
  artifacts:
    - path: "k8s/agent-sandbox/mcp-rbac.yaml"
      provides: "ServiceAccount + ClusterRole + ClusterRoleBinding"
      contains: "weka-mcp-server-sa"
    - path: "k8s/agent-sandbox/mcp-skill-configmap.yaml"
      provides: "ConfigMap with SKILL.md content"
      contains: "weka-mcp-skill-md"
  key_links:
    - from: "k8s/agent-sandbox/mcp-rbac.yaml"
      to: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      via: "serviceAccountName: weka-mcp-server-sa"
      pattern: "weka-mcp-server-sa"
    - from: "k8s/agent-sandbox/mcp-skill-configmap.yaml"
      to: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      via: "volume mount referencing ConfigMap name"
      pattern: "weka-mcp-skill-md"
---

<objective>
Create the RBAC manifests (ServiceAccount, ClusterRole, ClusterRoleBinding) and SKILL.md ConfigMap that the MCP sidecar and OpenClaw container require. These are standalone YAML files applied with kubectl before the Sandbox CR is updated.

Purpose: The MCP sidecar needs a dedicated ServiceAccount with scoped permissions for all 8 tools, and OpenClaw needs SKILL.md mounted as a file for agent tool registration.
Output: Two new YAML files in k8s/agent-sandbox/ ready for kubectl apply.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TOPOLOGY.md
@.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-CONTEXT.md
@.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-RESEARCH.md

<interfaces>
<!-- RBAC rules derived from source code audit in 13-RESEARCH.md -->
<!-- Each tool's K8s API calls mapped to apiGroups/resources/verbs -->

Required ClusterRole rules (from research audit):
- "" : namespaces, nodes, pods : get, list, watch (inspect_cluster)
- "storage.k8s.io" : storageclasses : get, list, watch (inspect_cluster)
- "apiextensions.k8s.io" : customresourcedefinitions : get, list, watch (get_crd_schema, inspect_cluster)
- "nvidia.com" : clusterpolicies : get, list, watch (inspect_cluster)
- "apps" : daemonsets : get, list, watch (inspect_cluster)
- "warp.io" : wekaappstores : get, list, watch, create, update, patch, delete (inspect_cluster, apply, status)
- "weka.weka.io" : wekaclusters : get, list, watch (inspect_weka)
- nonResourceURLs: /version, /healthz : get (inspect_cluster)

SKILL.md source: mcp-server/SKILL.md (250 lines, well under 1MB ConfigMap limit)

ConfigMap name convention: weka-mcp-skill-md
ServiceAccount name: weka-mcp-server-sa (locked decision)
Namespace: wekaappstore (locked decision)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RBAC manifest with ServiceAccount, ClusterRole, and ClusterRoleBinding</name>
  <files>k8s/agent-sandbox/mcp-rbac.yaml</files>
  <action>
Create `k8s/agent-sandbox/mcp-rbac.yaml` as a multi-document YAML file (separated by `---`) containing three resources:

1. **ServiceAccount** (`v1`):
   - name: `weka-mcp-server-sa`
   - namespace: `wekaappstore`
   - label: `app: weka-mcp-server`
   - `automountServiceAccountToken: true`

2. **ClusterRole** (`rbac.authorization.k8s.io/v1`):
   - name: `weka-mcp-server-cr`
   - label: `app: weka-mcp-server`
   - Rules (from research RBAC audit of all 8 tools):
     - `""` apiGroup: `namespaces`, `nodes`, `pods` — verbs: `get`, `list`, `watch`
     - `storage.k8s.io`: `storageclasses` — verbs: `get`, `list`, `watch`
     - `apiextensions.k8s.io`: `customresourcedefinitions` — verbs: `get`, `list`, `watch`
     - `nvidia.com`: `clusterpolicies` — verbs: `get`, `list`, `watch`
     - `apps`: `daemonsets` — verbs: `get`, `list`, `watch`
     - `warp.io`: `wekaappstores` — verbs: `get`, `list`, `watch`, `create`, `update`, `patch`, `delete`
     - `weka.weka.io`: `wekaclusters` — verbs: `get`, `list`, `watch`
     - `nonResourceURLs`: `/version`, `/healthz` — verb: `get`

3. **ClusterRoleBinding** (`rbac.authorization.k8s.io/v1`):
   - name: `weka-mcp-server-crb`
   - label: `app: weka-mcp-server`
   - roleRef: ClusterRole `weka-mcp-server-cr`
   - subjects: ServiceAccount `weka-mcp-server-sa` in namespace `wekaappstore`
   - CRITICAL: `subjects[0].namespace: wekaappstore` MUST be set (common pitfall from research — mismatch causes silent 403 failures)

Add a top comment: "# RBAC for WEKA App Store MCP sidecar — minimum permissions for all 8 tools"
Add inline comments mapping each rule to the tool(s) that require it.

Do NOT include `delete` verb for WekaAppStore CRs — the apply tool only uses create/update/patch. Wait, the research includes `delete` in the synthesized rules. Include `delete` to match the research audit (the operator pattern also includes it for lifecycle management). Per research: `get, list, watch, create, update, patch, delete`.
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-rbac.yaml -n wekaappstore 2>&1 | grep -E "created|configured|unchanged"</automated>
  </verify>
  <done>mcp-rbac.yaml passes kubectl dry-run with all three resources (ServiceAccount, ClusterRole, ClusterRoleBinding) validated. Each ClusterRole rule maps to a specific MCP tool via inline comments.</done>
</task>

<task type="auto">
  <name>Task 2: Create SKILL.md ConfigMap manifest</name>
  <files>k8s/agent-sandbox/mcp-skill-configmap.yaml</files>
  <action>
Create `k8s/agent-sandbox/mcp-skill-configmap.yaml` containing a ConfigMap:

1. Read the content of `mcp-server/SKILL.md` (the source file).
2. Create the ConfigMap:
   - apiVersion: v1
   - kind: ConfigMap
   - name: `weka-mcp-skill-md`
   - namespace: `wekaappstore`
   - label: `app: weka-mcp-server`
   - data key: `SKILL.md`
   - data value: the full content of `mcp-server/SKILL.md` embedded verbatim using the YAML block scalar (`|`) syntax

Add a top comment: "# ConfigMap delivering SKILL.md to OpenClaw container for agent tool registration"
Add a comment noting this is mounted at `/home/node/.openclaw/SKILL.md` via subPath in the Sandbox CR (Plan 02).

The ConfigMap is mounted into the OpenClaw container (not the MCP sidecar) because OpenClaw reads SKILL.md at registration time via the `skill` field in openclaw.json.
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-skill-configmap.yaml -n wekaappstore 2>&1 | grep -E "created|configured|unchanged"</automated>
  </verify>
  <done>mcp-skill-configmap.yaml passes kubectl dry-run. ConfigMap contains the full SKILL.md content (250 lines) under the `SKILL.md` data key.</done>
</task>

</tasks>

<verification>
Both YAML files pass `kubectl apply --dry-run=client`:
- `kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-rbac.yaml -n wekaappstore`
- `kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-skill-configmap.yaml -n wekaappstore`
</verification>

<success_criteria>
- mcp-rbac.yaml contains SA + ClusterRole + ClusterRoleBinding with correct names and permissions for all 8 tools
- mcp-skill-configmap.yaml contains full SKILL.md content as a ConfigMap
- Both files pass kubectl dry-run validation
- ClusterRoleBinding subjects namespace is explicitly set to wekaappstore
</success_criteria>

<output>
After completion, create `.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-01-SUMMARY.md`
</output>
