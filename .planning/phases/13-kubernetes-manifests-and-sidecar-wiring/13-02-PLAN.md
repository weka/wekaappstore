---
phase: 13-kubernetes-manifests-and-sidecar-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/agent-sandbox/openclaw-sandbox.yaml
  - scripts/validate-phase13.sh
autonomous: true
requirements:
  - K8S-01
  - K8S-03
  - K8S-04
  - K8S-05
  - NCLAW-02

must_haves:
  truths:
    - "MCP sidecar container is defined in the Sandbox CR pod spec with correct image, env vars, and readiness probe"
    - "git-sync container continuously syncs warp-blueprints repo into a shared volume"
    - "Init container generates openclaw.json from env vars at pod startup before any regular container starts"
    - "openclaw.json contains correct transport (streamable-http) and url (http://localhost:8080/mcp) and skill path"
    - "Blueprint files are accessible to the MCP sidecar at BLUEPRINTS_DIR via volume mount"
    - "Readiness probe on /health:8080 gates MCP sidecar Ready status"
  artifacts:
    - path: "k8s/agent-sandbox/openclaw-sandbox.yaml"
      provides: "Complete Sandbox CR with init container, MCP sidecar, git-sync, volumes, serviceAccountName"
      contains: "weka-mcp-sidecar"
    - path: "scripts/validate-phase13.sh"
      provides: "Dry-run and live validation script for Phase 13 manifests"
      contains: "validate-phase13"
  key_links:
    - from: "k8s/agent-sandbox/openclaw-sandbox.yaml (init container)"
      to: "/home/node/.openclaw/openclaw.json"
      via: "printf command writing JSON to shared emptyDir"
      pattern: "openclaw-json-generator"
    - from: "k8s/agent-sandbox/openclaw-sandbox.yaml (mcp sidecar)"
      to: "/health endpoint on port 8080"
      via: "readinessProbe httpGet"
      pattern: "readinessProbe.*health.*8080"
    - from: "k8s/agent-sandbox/openclaw-sandbox.yaml (git-sync)"
      to: "blueprints emptyDir volume"
      via: "git-sync continuous sync to /blueprints"
      pattern: "git-sync"
    - from: "k8s/agent-sandbox/openclaw-sandbox.yaml (mcp sidecar BLUEPRINTS_DIR)"
      to: "blueprints volume with subPath"
      via: "volumeMount with subPath dereferencing git-sync symlink"
      pattern: "BLUEPRINTS_DIR"
---

<objective>
Modify the existing OpenClaw Sandbox CR to add the MCP sidecar container, git-sync container, openclaw.json init container, all required volumes, and serviceAccountName. Also create a validation script for dry-run and live cluster checks.

Purpose: This is the core wiring that puts the MCP server inside the OpenClaw pod, enables blueprint delivery, and generates runtime config. After this plan, the Sandbox CR is a complete deployable manifest.
Output: Updated openclaw-sandbox.yaml with full sidecar wiring + validate-phase13.sh script.
</objective>

<execution_context>
@/Users/christopherjenkins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopherjenkins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TOPOLOGY.md
@.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-CONTEXT.md
@.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-RESEARCH.md

<interfaces>
<!-- Existing Sandbox CR structure (source: k8s/agent-sandbox/openclaw-sandbox.yaml) -->
Current Sandbox CR has:
- spec.podTemplate.spec.containers[0]: name=openclaw (gateway on port 18789, GPU limits, secrets)
- spec.podTemplate.spec.volumes[0]: name=openclaw-config (emptyDir)
- spec.volumeClaimTemplates[0]: name=openclaw-workspace (2Gi PVC)
- securityContext.fsGroup: 1000
- nodeSelector: nvidia.com/gpu: "true"
- tolerations: nvidia.com/gpu NoSchedule

Phase 13 additions (from TOPOLOGY.md):
- serviceAccountName: weka-mcp-server-sa
- initContainers[0]: openclaw-json-generator (busybox:1.36)
- containers[1]: weka-mcp-sidecar (wekachrisjen/weka-app-store-mcp)
- containers[2]: git-sync (registry.k8s.io/git-sync/git-sync:v4.5.0)
- volumes: blueprints (emptyDir), skill-md (ConfigMap)
- OpenClaw container gets new volumeMount for SKILL.md ConfigMap

MCP sidecar connection (from TOPOLOGY.md):
- openclaw.json: {"transport":"streamable-http","url":"http://localhost:8080/mcp","skill":"/home/node/.openclaw/SKILL.md"}
- Gateway at localhost:18789, MCP sidecar at localhost:8080 (shared pod network)

git-sync v4.5.0 args (from research):
- --repo=https://github.com/weka/warp-blueprints.git
- --root=/blueprints --link=blueprints --period=60s
- Continuous mode (no --one-time flag) per locked decision

BLUEPRINTS_DIR path resolution (from research pitfall #1):
- git-sync writes to /blueprints/blueprints (symlink)
- MCP sidecar volumeMount uses subPath: blueprints to dereference
- BLUEPRINTS_DIR=/app/blueprints (matching subPath mount)

MCP Dockerfile user (from research): uid 10001 (mcpuser)
git-sync default user: uid 65533
OpenClaw user: uid 1000
Pod fsGroup: 1000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sandbox CR with init container, MCP sidecar, git-sync, and volumes</name>
  <files>k8s/agent-sandbox/openclaw-sandbox.yaml</files>
  <action>
Read the existing `k8s/agent-sandbox/openclaw-sandbox.yaml` and modify it to add all Phase 13 components. The file structure must remain a valid Sandbox CR (`agents.x-k8s.io/v1alpha1`).

**Add to `spec.podTemplate.spec`:**

1. `serviceAccountName: weka-mcp-server-sa` — at the spec level (not inside containers).

2. `initContainers` array with one entry:
   - name: `openclaw-json-generator`
   - image: `busybox:1.36`
   - command: `["sh", "-c"]` with args using `printf` to write openclaw.json (NOT heredoc — avoids variable expansion pitfall from research):
     ```
     printf '{"name":"weka-app-store-mcp","description":"MCP server for the WEKA App Store.","transport":"streamable-http","url":"http://localhost:%s/mcp","skill":"%s"}\n' "$MCP_PORT" "$SKILL_MD_PATH" > /home/node/.openclaw/openclaw.json && cat /home/node/.openclaw/openclaw.json
     ```
   - env: MCP_PORT=8080, SKILL_MD_PATH=/home/node/.openclaw/SKILL.md
   - volumeMounts: `openclaw-config` at `/home/node/.openclaw`
   - securityContext: runAsNonRoot=true, runAsUser=1000 (matches fsGroup so file is readable by OpenClaw)
   - resources: requests cpu=10m memory=16Mi, limits cpu=100m memory=32Mi

3. Add to existing `containers` array (after the openclaw container):

   **weka-mcp-sidecar:**
   - name: `weka-mcp-sidecar`
   - image: `wekachrisjen/weka-app-store-mcp`
   - imagePullPolicy: Always
   - ports: containerPort 8080, name mcp-http, protocol TCP
   - env:
     - MCP_TRANSPORT=http
     - MCP_PORT=8080
     - BLUEPRINTS_DIR=/app/blueprints
     - KUBERNETES_AUTH_MODE=incluster
   - readinessProbe: httpGet /health port 8080, initialDelaySeconds=5, periodSeconds=5, failureThreshold=3
   - livenessProbe: httpGet /health port 8080, initialDelaySeconds=15, periodSeconds=15
   - resources: requests cpu=100m memory=256Mi, limits cpu=500m memory=512Mi
   - securityContext: runAsNonRoot=true, runAsUser=10001, allowPrivilegeEscalation=false, drop ALL capabilities
   - volumeMounts:
     - `blueprints` at `/app/blueprints` with `subPath: blueprints` (dereferences git-sync symlink per research pitfall #1)
     - `openclaw-config` at `/home/node/.openclaw` with `readOnly: true` (only needs to read, not write per research anti-pattern)

   **git-sync:**
   - name: `git-sync`
   - image: `registry.k8s.io/git-sync/git-sync:v4.5.0`
   - args: --repo=https://github.com/weka/warp-blueprints.git, --root=/blueprints, --link=blueprints, --period=60s
   - Do NOT include --one-time (locked decision: continuous sync)
   - resources: requests cpu=50m memory=64Mi, limits cpu=200m memory=128Mi
   - securityContext: runAsNonRoot=true, runAsUser=65533 (git-sync default user)
   - volumeMounts: `blueprints` at `/blueprints`

4. Modify existing `openclaw` container:
   - Add volumeMount for SKILL.md: `skill-md` at `/home/node/.openclaw/SKILL.md` with `subPath: SKILL.md` and `readOnly: true`

5. Add to existing `volumes` array:
   - `blueprints`: emptyDir {}
   - `skill-md`: configMap with name `weka-mcp-skill-md`

Keep existing volumes (`openclaw-config` emptyDir) and volumeClaimTemplates (`openclaw-workspace` PVC) unchanged.

Add inline comments explaining:
- Why serviceAccountName is set (K8S-02)
- What the init container does (K8S-05)
- Why readinessProbe matters (K8S-03)
- Why subPath is used for blueprints (git-sync symlink dereferencing)
- Why openclaw-config is readOnly in the sidecar (write happens in init container only)
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f k8s/agent-sandbox/openclaw-sandbox.yaml -n wekaappstore 2>&1 | grep -E "created|configured|unchanged"</automated>
  </verify>
  <done>Updated openclaw-sandbox.yaml passes kubectl dry-run. Contains: serviceAccountName, 1 init container (openclaw-json-generator), 3 regular containers (openclaw, weka-mcp-sidecar, git-sync), 3 volumes (openclaw-config, blueprints, skill-md), and 1 PVC template.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 13 validation script</name>
  <files>scripts/validate-phase13.sh</files>
  <action>
Create `scripts/validate-phase13.sh` following the pattern established by `scripts/validate-topology.sh` (same PASS/FAIL/WARN format, same NAMESPACE parameter handling).

The script has two modes:
1. **Dry-run mode** (default, no cluster needed): Validates YAML syntax and structure of all manifest files
2. **Live mode** (`--live` flag): Runs kubectl checks against a live cluster

**Dry-run checks (always run):**

1. `kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-rbac.yaml` — validates RBAC YAML
2. `kubectl apply --dry-run=client -f k8s/agent-sandbox/mcp-skill-configmap.yaml` — validates ConfigMap YAML
3. `kubectl apply --dry-run=client -f k8s/agent-sandbox/openclaw-sandbox.yaml` — validates Sandbox CR YAML
4. Verify openclaw-sandbox.yaml contains required elements using grep:
   - `serviceAccountName: weka-mcp-server-sa`
   - `name: openclaw-json-generator` (init container)
   - `name: weka-mcp-sidecar` (container)
   - `name: git-sync` (container)
   - `readinessProbe` (K8S-03)
   - `BLUEPRINTS_DIR` (K8S-04)

**Live checks (only with --live flag):**

5. `kubectl auth can-i list nodes --as=system:serviceaccount:wekaappstore:weka-mcp-server-sa` — RBAC validation
6. `kubectl auth can-i create wekaappstores.warp.io --as=system:serviceaccount:wekaappstore:weka-mcp-server-sa --all-namespaces` — write permissions
7. Check MCP sidecar container is running: `kubectl get pod -l <sandbox-selector> -n $NAMESPACE -o jsonpath` for container status
8. Check MCP sidecar health: `kubectl exec <pod> -c weka-mcp-sidecar -- curl -sf http://localhost:8080/health`
9. Check openclaw.json generated correctly: `kubectl exec <pod> -c openclaw -- cat /home/node/.openclaw/openclaw.json`
10. Check SKILL.md mounted: `kubectl exec <pod> -c openclaw -- cat /home/node/.openclaw/SKILL.md | head -5`

Use the same sandbox selector resolution pattern as validate-topology.sh (read from `kubectl get sandbox openclaw-sandbox -n $NAMESPACE -o jsonpath='{.status.selector}'`).

Script header: `#!/usr/bin/env bash`, set -euo pipefail, usage comment.
Make executable: `chmod +x scripts/validate-phase13.sh`.
Exit 0 on all PASS, exit 1 on any FAIL. WARNs do not cause failure.
  </action>
  <verify>
    <automated>bash scripts/validate-phase13.sh 2>&1 | tail -5</automated>
  </verify>
  <done>validate-phase13.sh exists, is executable, and dry-run mode passes all structural checks against the authored manifests. Live mode checks are defined but only run with --live flag.</done>
</task>

</tasks>

<verification>
1. `kubectl apply --dry-run=client -f k8s/agent-sandbox/openclaw-sandbox.yaml -n wekaappstore` passes
2. `bash scripts/validate-phase13.sh` dry-run checks all pass
3. Sandbox CR contains all required additions: serviceAccountName, initContainers, 3 containers, 3 volumes
</verification>

<success_criteria>
- openclaw-sandbox.yaml is a valid Sandbox CR with MCP sidecar, git-sync, init container, and all volumes wired
- Readiness probe defined on MCP sidecar /health:8080 (K8S-03)
- BLUEPRINTS_DIR env var set with correct subPath mount (K8S-04)
- openclaw.json init container uses printf with env vars (K8S-05, NCLAW-02)
- SKILL.md ConfigMap volume mounted into OpenClaw container (NCLAW-04 wiring)
- validate-phase13.sh dry-run mode confirms all structural requirements
</success_criteria>

<output>
After completion, create `.planning/phases/13-kubernetes-manifests-and-sidecar-wiring/13-02-SUMMARY.md`
</output>
