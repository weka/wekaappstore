---
phase: 18-operator-wiring-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06]
tags: [docs, readme, variable-substitution, user-facing]

must_haves:
  truths:
    - "README has a top-level section `## Variable substitution in AppStack manifests` placed between `## Common configuration` and `## Upgrading`"
    - "The new section includes a syntax reference table covering ${VAR}, $$, ${namespace} auto-default, and undefined-variable error semantics"
    - "The new section includes a worked AIDP-style multi-component example with ${namespace} + ${milvusHost} and a valuesFiles reference"
    - "The new section explicitly documents `> Note:` no-recursive-resolution with side-by-side WRONG/CORRECT snippets using fully-resolved values (NO milvus.${namespace}.svc.cluster.local nested ${} pattern)"
    - "The new section explicitly states operator-control fields (helmChart.*, releaseName, targetNamespace, readinessCheck.*) are NOT templated and recommends omitting targetNamespace"
    - "The new section documents kopf.PermanentError on undefined ${VAR} and kopf.TemporaryError(delay=30) on missing ConfigMap/Secret"
  artifacts:
    - path: "README.md"
      provides: "User-facing variable substitution documentation"
      contains: "## Variable substitution in AppStack manifests"
  key_links:
    - from: "README.md TOC ordering"
      to: "Common configuration -> Variable substitution -> Upgrading"
      via: "section heading sequence"
      pattern: "## Variable substitution in AppStack manifests"
---

<objective>
Add a complete user-facing README section documenting AppStack variable substitution. Authors who land on the repo can discover, understand, copy, and adapt the feature without reading the operator source. Section sits between `## Common configuration` and `## Upgrading` so it appears in the natural reading flow for anyone configuring an AppStack CR.

Purpose: DOC-01..06 from REQUIREMENTS.md. Phase 20 (AIDP migration in a separate repo) consumes this section as its canonical migration template.
Output: One modified file: `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-operator-wiring-and-docs/18-CONTEXT.md
@.planning/phases/18-operator-wiring-and-docs/18-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert `## Variable substitution in AppStack manifests` section between `## Common configuration` and `## Upgrading`</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate the exact line numbers of `## Common configuration` and `## Upgrading`; CONTEXT.md notes them at ~line 78 and ~line 80 but the executor must verify before editing)
    - README.md:108-149 (existing `## Readiness checks for components` section — heading depth and prose tone reference)
    - .planning/phases/18-operator-wiring-and-docs/18-CONTEXT.md "Specifics" section (locked README skeleton outline)
    - .planning/phases/18-operator-wiring-and-docs/18-PATTERNS.md "README.md" section (heading depth + worked-example shape guidance)
  </read_first>
  <behavior>
    - The section is inserted as a peer to `## Common configuration` and `## Upgrading` — heading depth `## ` (two hashes).
    - Section ordering inside (D-10 locked): (1) one-paragraph why → (2) syntax reference table → (3) worked example CR → (4) no-recursion `> Note:` callout with WRONG/CORRECT snippets → (5) operator-control-fields callout → (6) error semantics.
    - The worked example is AIDP-style: at least two components — one helm component with `valuesFiles` referencing a ConfigMap/Secret, one `kubernetesManifest` component with `namespace: ${namespace}`. Uses `${namespace}` and `${milvusHost}`. Values in the worked example are fully-resolved strings (e.g., `milvus.aidp-prod.svc.cluster.local`) — NEVER nested `${}`.
    - The WRONG snippet shows `milvusHost: milvus.${namespace}.svc.cluster.local` with a `# WRONG (this does not work)` comment heading.
    - The CORRECT snippet shows `milvusHost: milvus.aidp-prod.svc.cluster.local` with a `# CORRECT (use fully-resolved values)` comment heading.
    - The operator-control-fields callout names every field explicitly: `helmChart.repository`, `helmChart.name`, `helmChart.version`, `releaseName`, `targetNamespace`, `readinessCheck.*`. It includes the hard recommendation: "**Recommendation:** Omit `targetNamespace` and let the operator default to `metadata.namespace`."
    - The error-semantics paragraph names BOTH error types: `kopf.PermanentError` for undefined `${VAR}` and `kopf.TemporaryError(delay=30)` for missing ConfigMap/Secret.
    - Existing README sections above and below are byte-identical except for one new blank line + the new section.
  </behavior>
  <action>
    Step 1: Locate insertion point. Run `grep -n "^## Common configuration\\|^## Upgrading" README.md` to confirm line numbers.

    Step 2: Insert the section below IMMEDIATELY BEFORE the line `## Upgrading`. Keep one blank line between `## Common configuration`'s last content line and `## Variable substitution in AppStack manifests`, and one blank line between the end of the new section and `## Upgrading`.

    The section text below is mostly locked from CONTEXT.md "Specifics" with prose filled in. Use this verbatim:

    ```markdown
    ## Variable substitution in AppStack manifests

    The operator performs single-pass `${VAR}` substitution over `kubernetesManifest:` strings and `valuesFiles:` content (loaded from ConfigMaps and Secrets) before they are applied or merged into Helm values. This makes a single AppStack CR portable across namespaces and environments — the same blueprint deploys into `staging`, `aidp-prod`, or `aidp-test` by changing `metadata.namespace` and the `spec.appStack.variables` map, without forking the YAML.

    ### Syntax

    | Syntax | Behavior |
    | --- | --- |
    | `${VAR}` | Substituted with the value of `VAR` from `spec.appStack.variables`. Strict — undefined references fail loudly. |
    | `$$` | Literal dollar sign. Use this when a manifest needs a real `$` (for example a database password starting with `$`). |
    | `${namespace}` | Auto-defaults to the CR's `metadata.namespace`. You can override by listing `namespace:` explicitly under `spec.appStack.variables`. |
    | undefined `${VAR}` | Raises `kopf.PermanentError` naming the variable, the component, and the manifest/valuesFiles location. The CR must be fixed and re-applied. |
    | invalid key (e.g., `my-host`) | Rejected at admission by the CRD schema, and again at the operator. Variable names must match the Python identifier pattern `[_a-zA-Z][_a-zA-Z0-9]*`. |

    ### Worked example

    The example below mirrors the AIDP migration pattern: two components, `${namespace}` auto-defaulting, an explicit `${milvusHost}` consumed both inside a `kubernetesManifest` and inside a ConfigMap-loaded `valuesFiles` reference.

    ```yaml
    apiVersion: warp.io/v1alpha1
    kind: WekaAppStore
    metadata:
      name: ai-research
      namespace: aidp-prod
    spec:
      appStack:
        variables:
          milvusHost: milvus.aidp-prod.svc.cluster.local
        components:
          - name: vector-db
            helmChart:
              repository: oci://registry.example.com/charts
              name: milvus
              version: 4.2.1
            valuesFiles:
              - kind: ConfigMap
                name: milvus-values
                key: values.yaml
            # The ConfigMap above contains:
            #   externalAccess:
            #     enabled: true
            #     host: ${milvusHost}
            # which the operator renders to milvus.aidp-prod.svc.cluster.local
            # before merging into Helm values.

          - name: ingress
            kubernetesManifest: |
              apiVersion: networking.k8s.io/v1
              kind: Ingress
              metadata:
                name: ai-research-ingress
                namespace: ${namespace}
              spec:
                rules:
                  - host: ai-research.example.com
                    http:
                      paths:
                        - path: /
                          pathType: Prefix
                          backend:
                            service:
                              name: ai-research
                              port:
                                number: 80
    ```

    Apply the same blueprint to a different namespace by changing `metadata.namespace` (and the `milvusHost` value to match):

    ```yaml
    metadata:
      name: ai-research
      namespace: aidp-test
    spec:
      appStack:
        variables:
          milvusHost: milvus.aidp-test.svc.cluster.local
        components:
          # ... unchanged ...
    ```

    ### Variable values are NOT recursively resolved

    > **Note:** Variable values are taken literally — no recursive resolution. The operator runs a single substitution pass; `${...}` tokens that appear inside variable VALUES are NOT expanded.

    ```yaml
    # WRONG (this does NOT work):
    spec:
      appStack:
        variables:
          milvusHost: milvus.${namespace}.svc.cluster.local   # nested ${} is NOT expanded
    ```

    ```yaml
    # CORRECT (use fully-resolved values):
    spec:
      appStack:
        variables:
          milvusHost: milvus.aidp-prod.svc.cluster.local
    ```

    If you have many environments, generate the fully-resolved CR per environment from your own templating tool (Helm chart wrapper, kustomize, sed, etc.) — the operator deliberately stops at single-pass substitution to keep failure modes predictable.

    ### Operator-control fields are NOT templated

    The fields below are operator-control fields and are NOT subject to `${VAR}` substitution. Putting `${...}` in any of them is a no-op that will silently fail to resolve at deploy time:

    - `helmChart.repository`, `helmChart.name`, `helmChart.version`
    - `releaseName`
    - `targetNamespace`
    - `readinessCheck.*` (`type`, `name`, `namespace`, `timeout`)

    **Recommendation:** Omit `targetNamespace` and let the operator default to `metadata.namespace`. Templating is not supported on this field; setting it pins the component to a specific namespace and defeats the portability the variables block provides.

    ### Errors

    - **Undefined variable** (e.g., `${unset}` appears in a manifest with no matching key in `spec.appStack.variables`): `kopf.PermanentError` is raised. The error message names the variable, the component, and the source location. The reconcile does NOT retry — the CR must be edited and re-applied.
    - **Missing referenced ConfigMap or Secret** (e.g., `valuesFiles[].name` does not exist in the cluster yet): `kopf.TemporaryError(delay=30)` is raised. The operator retries every 30 seconds — useful when the ConfigMap is created after the CR.
    - **Malformed `${...}`** (e.g., `${}`, `${123}`, bare `$` at end of line): `kopf.PermanentError` is raised. The error message names the malformed placeholder.
    - **Invalid variable key** (e.g., `my-host` with a hyphen): `kopf.PermanentError` is raised at the start of reconcile, before any deployment work. Variable names must match `[_a-zA-Z][_a-zA-Z0-9]*`.
    ```

    Important formatting notes:
    - The fenced code blocks inside the worked example use ` ```yaml ` for syntax highlighting.
    - The `> **Note:**` block-quote MUST be on its own paragraph (blank line before and after) so GitHub renders it as a callout.
    - The "WRONG" and "CORRECT" snippets are SEPARATE fenced YAML blocks with a `# WRONG` / `# CORRECT` comment line as the first line of each — the comments are part of the rendered code, not Markdown headings.
    - The recommendation line uses CAPITAL O ("**Recommendation:** Omit `targetNamespace`") — the acceptance grep is case-insensitive (W-5 fix) so either capitalization is accepted, but stay consistent with the surrounding bold-header convention.

    Threat note (T-18-04): the example uses placeholder names (`${milvusHost}`, `${namespace}`, `${unset}`) — never realistic-looking secret keys. The error-semantics paragraph names variable-name placeholders only, never values.
  </action>
  <verify>
    <automated>grep -c "^## Variable substitution in AppStack manifests$" README.md</automated>
    Expected: `1`. Plus the full grep gate set in acceptance_criteria.
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Variable substitution in AppStack manifests$" README.md` == 1
    - Section ordering: `awk '/^## Common configuration|^## Variable substitution in AppStack manifests|^## Upgrading/{print NR, $0}' README.md` shows the three lines in order Common -> Variable -> Upgrading
    - DOC-01 (worked ${VAR} example): `grep -c '\\${milvusHost}' README.md` >= 2
    - DOC-02 ($$ escape) — table row present: `grep -c '`\\$\\$`' README.md` >= 1
    - DOC-03 (${namespace} auto-default): `grep -c '\\${namespace}' README.md` >= 3
    - DOC-04 (kopf.PermanentError mention): `grep -c 'kopf.PermanentError' README.md` >= 2
    - DOC-04 (kopf.TemporaryError mention): `grep -c 'kopf.TemporaryError' README.md` >= 1
    - DOC-05 (no-recursion callout): `grep -c '> \\*\\*Note:\\*\\* Variable values are taken literally' README.md` == 1
    - DOC-05 (WRONG snippet present): `grep -c '# WRONG' README.md` >= 1
    - DOC-05 (CORRECT snippet present): `grep -c '# CORRECT' README.md` >= 1
    - DOC-05 (the WRONG snippet contains the broken nested-${} pattern): `grep -c 'milvus\\.\\${namespace}\\.svc\\.cluster\\.local' README.md` == 1
    - DOC-05 (the CORRECT snippet contains a fully-resolved value): `grep -c 'milvus\\.aidp-prod\\.svc\\.cluster\\.local' README.md` >= 1
    - DOC-06 (operator-control fields named): `grep -c 'helmChart\\.repository' README.md` >= 1; `grep -c 'releaseName' README.md` >= 1; `grep -c 'readinessCheck' README.md` >= 1
    - DOC-06 (drop targetNamespace recommendation — case-insensitive per W-5 fix because the rendered text begins with capital O in `**Recommendation:** Omit \`targetNamespace\``): `grep -ci 'omit \`targetNamespace\`' README.md` >= 1
    - The Common configuration and Upgrading sections were not modified except for separation: `git diff README.md | grep -c '^-## Common configuration'` == 0; `git diff README.md | grep -c '^-## Upgrading'` == 0
  </acceptance_criteria>
  <done>README has the new top-level section with all six DOC-01..06 anchors present and the existing sections unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md (public docs) -> blueprint authors | Examples in README are copied by AIDP migration authors and into customer environments. Misleading examples become production patterns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure (via misleading examples) | README.md worked example, error-semantics paragraph | mitigate | Examples use placeholder variable names only (`${milvusHost}`, `${namespace}`, `${unset}`) — never realistic-looking secret-bearing names like `${apiKey}`, `${dbPassword}`. The `$$` escape example (in the syntax table row) calls out passwords abstractly without modeling a credential format that authors would copy. The WRONG/CORRECT snippets demonstrate the no-recursive-resolution gotcha so authors don't ship CRs that fail at deploy time. |
</threat_model>

<verification>
## Phase-level checks

After Task 1:
1. `grep -c "^## Variable substitution in AppStack manifests$" README.md` == 1
2. Open `README.md` in a Markdown previewer (e.g., `gh pr view --web` after the eventual PR, or `glow README.md` locally) and confirm:
   - The new heading sits between `## Common configuration` and `## Upgrading` in the rendered TOC.
   - The syntax table renders as a 2-column table (Syntax | Behavior).
   - The `> **Note:**` block renders as a GitHub callout (gray-background block-quote).
   - The WRONG and CORRECT YAML blocks render with syntax highlighting.
3. The Common configuration and Upgrading sections are byte-identical to pre-Phase-18 except for one inserted blank line: `git diff --stat README.md` shows insertions only (no deletions outside the new section's neighborhood).
</verification>

<success_criteria>
- All DOC-01..06 grep anchors green
- Section sits in the correct TOC position
- Existing sections untouched
- Phase 20 AIDP author can copy the worked example into `aidp/appstack/weka-aidp-appstack.yaml` and adapt by changing only `metadata.namespace` and `milvusHost`
</success_criteria>

<output>
After completion, create `.planning/phases/18-operator-wiring-and-docs/18-02-SUMMARY.md` documenting:
- Insertion line range in README.md
- The six grep anchors that downstream verification (gsd-verify-work) and the Phase-18 success-criteria 5 will check
- Confirmation that no content outside the new section was modified
</output>
</content>
